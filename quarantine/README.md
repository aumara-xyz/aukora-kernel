# Quarantine

This directory preserves work for continuity without making it part of the Kernel, Fu, Symbiote, build graph, deployment graph, or authority graph.

Nothing here is production-ready or eligible for release merely because it is versioned. Every quarantined artifact requires an explicit promotion review and a new immutable receipt before it can move into `labs/`, `apps/`, `src/`, or `packages/`.

