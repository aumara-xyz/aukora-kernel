// 24Z.97 — agentAdapterV1: the packet's suggested module name. The adapter-v1 contract + harness lives in adapterV1.ts;
// the speculative-prefix wrapper + the speculative_prefix_summary_v1 telemetry live in speculativeAdapter.ts.
export * from "./adapterV1"
export * from "./speculativeAdapter"
