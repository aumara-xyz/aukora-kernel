# MESH INBOX — node-to-node coordination (advisory only)

A simple repo-based channel so the nodes (Peter/Codex, Zeb/Fable, …) can hand each other
round reports without copy-paste. **Nothing in this file grants authority.** It is read the
same way all memory here is read: it can suggest, never authorize.

Current rule:
- Routine round reports now go in `docs/mesh/YYYY-MM-DD/<short-slug>.md` using
  `bun scripts/mesh-entry.ts`. This avoids the repeated newest-top merge
  conflicts that were blocking otherwise clean PRs.
- Do not edit this file for normal lane reports. Keep this file as a stable
  coordinator index/archive.
- No secrets, no private keys, no local memory contents, no key bytes — ever.
- Keep entries short and literal; deep detail belongs in the PR description or a doc.

See [docs/mesh/README.md](mesh/README.md).

---

## 2026-07-07 — ARC AGI 3 lane (with Sam/Peter) · sam-mac-node · budget sense + first live level on this box

**Branches / PRs**
- `arc3/budget-sense` (stacked on `zeb/arc3-agi-organ`, PR #153) — budget sense engine upgrade, MK·EMBER onboard world, per-game memory wipe, shell.js arc3 wiring committed, coherence-glyph stand-in (original was uncommitted on Zeb's box — Zeb, please supply yours or bless the shim).

**Summary**
- Organ runs on sam-mac: 19/19 pins green (was 15; +4 new budget/forget pins).
- LIVE, this box, anonymous key: **lp85 level 1 WON blind twice** — first at action 78 (baseline 17), platform-verified via scorecard read-back (`1df39b13-…`, `levels_completed=1`); repeat run 551 acts, L2 unreached (`7fe94b3d-…`, read-back pending backend re-roll). ft09 0/6 this box today (fresh seed; scorecard `f3f9be10-…`). Honest ledger in docs/ARC3_AGI_ORGAN.md.
- Budget sense observed firing live on lp85 (drain-watch caught a gauge, commit mode engaged).
- Platform facts added: per-game backend-roll odds vary wildly and drift; scorecard READS are backend-affine too.

**Action items**
- Peter/Codex: review + merge `arc3/budget-sense` into `zeb/arc3-agi-organ`, then #153 bottom-up when ready.
- Zeb: original `spatial/app/coherence-glyph.js` wanted (a contract-matching stand-in ships in this brick).
- Note: :7090 on sam-mac is currently held by a server running from `aukora-main-audit` — the arc3 organ is NOT visible there until #153 reaches whatever checkout serves :7090. Preview lane :7098 works.
## 2026-07-08 — AUKORA MAIN / Fable (with Peter) · sam-mac-node · Auma Live REMEMBERS: presence-lane capture wired + the 7092 three-way port squat ended

**Branches / PRs**
- `sam/presence-capture-port-dedup` — two owner-directed fixes after Peter's "all contact points
  root to one brain" audit call.

**Fix 1 — Auma Live recalled but never remembered.** The presence lane (live voice mind) had
governed RECALL (cutover ✓) but `captureCompletedTurn` fired only in `/api/chat` — spoken
conversations left zero memory rows. Now: `presenceStream` exposes an `onHeardTurn` hook under the
ring's own discard law (extracted as pure `presenceTurnHeard`, test-pinned: a sub-2.5s abort was
never heard → NOTHING records it), and the presence handler routes a heard turn into the SAME
`captureCompletedTurn` gates via the exact voice-shaped entry pair `extractVoiceReplyText`
validates. Zero new capture surface. LIVE-PROVEN on this box: full spoken turn → `[shadow-capture]
ok: key=turn.20260708t015720732z.0` (ok 2, refused 0); an early-aborted turn correctly discarded.

**Fix 2 — three organs claimed :7092.** The Auma Live voice sidecar (`ws://7092/ws`), agora-serve
(default 7092) and arc3-serve (default 7092, AUTO-STARTED by start-node since #153) all fought for
one port — arc3 won on boot, so the voice sidecar could never bind (Auma Live silently degraded to
browser fallback). Now: arc3 door → **7093** (server, client, doc), agora default → **7097**,
start-node comment pins 7092 = voice sidecar. Env overrides unchanged.

- DOOR-WIRING pin grown, not weakened: typed-chat capture pin now searches forward from
  `voiceReply`, and a NEW pin asserts the presence door captures too (a future unwiring fails red).
- New suite `core/tests/presenceCapture.test.ts` (5 tests: the heard-law + the entries contract).

**Action items**
1. Peter: review/merge `sam/presence-capture-port-dedup`. ARC3 lane: rebase any open arc3 work on
   it (door client/pill now say :7093).
2. Anyone with a Mac sidecar: after merge, Auma Live full-duplex + the arc3 door can finally run
   at the same time — worth a two-organ smoke.

## 2026-07-08 — Graticube lane (with Sam) · sam-mac-node · round 4: Auma the facilitator, carrier dossiers, signal glyphs — and the AURA tie-in WITHDRAWN

**Branches / PRs**
- `sam/graticube-auma-depth` (off main ab1920d; #156 dial confirmed ancestor of main).

**Summary**
- AURA de-wired per owner: a completed turn now awards NOTHING — the award call and the
  aura-core 'graticube-turn' acceptance are both removed (aura-core back to cast-only). The
  owner's coherence system will claim this surface when it lands; the code comments point
  at docs/COHERENCE_GLYPH.md and deliberately wire nothing. Verified: aura localStorage is
  byte-identical across a full played turn.
- Auma goes deeper: alongside "ask Auma to play too", every transmission now offers
  "go deeper with Auma" — an aukora:ask framing where she is the FACILITATOR, not a player
  (invite the share, reflect it back in the player's own words, tie in the locked time or
  feeling word, one gentle follow-up, warm and brief).
- Signal glyphs: each transmission renders a small cymatic figure via the system's own
  coherence-glyph.js, seeded `cardId|time|feeling`. Decorative portraiture ONLY — clarity is
  a fixed constant, coherence is not measured, no claims; the glyph pulses when the
  gratitude heart fires. First consumer of the glyph renderer outside its own page.
- Carrier dossiers: constellation stars are buttons now — each opens the carrier's dossier
  (sigil, chakra name, both framework faces, Gift Round note).
- Ambience: rare meteors in the idle starfield; a narrow-lane-only "table mode" tip.
- Gates: scan-secrets exit 0. In-browser verification on :7099 (worktree preview port):
  glyph painted, all three Auma/turn actions dispatch, dossier renders both faces, no
  console errors, no aura movement.

**Action items**
- Peter: review/merge `sam/graticube-auma-depth`. Note the aura-core revert is part of the
  same brick (withdrawing what round 2 added there).
- Codex: sanity-check the decorative glyph use — first reuse of coherence-glyph.js; the
  fixed-clarity, no-measurement stance is stated in code comments and here.

## 2026-07-08 — AUKORA MAIN / Fable (with Peter) · sam-mac-node · UNISON LIVE: cutover restart done — one Convex brain behind every door, capture writing, AUMLOK native

**Branches / PRs**
- `sam/mesh-unison-live` — this entry only. #161 (cutover) merged by Peter; this is the promised
  restart + verification, per the Kira lane's handoff.

**Verified LIVE on sam-mac-node (main @ 5786b8d, doors restarted with seat + capture armed, NO recall env)**
- `/api/brain`: `recall.source: convex` · `capture.wired: true` · convex running · `lastConvexRefusal: null`.
- Recall proof: a real chat turn recalled the send-button demo from governed atom
  `atom_4_0e0c295d4285d9f2` (cites the `8d7bb547…` proposal hash) — served by the governed brain, no env.
- Capture proof: that same turn was distilled + written through governed memoryAppend —
  capture health `ok 1, refused 0; last written row: turn.20260708t0…`. The loop reads AND writes.
- AUMLOK posture: bound (`keyId b926fd12…`) — so the native bind door correctly does NOT run here;
  ceremony + rotation remain available for fresh/rebinding nodes (#166, merged).
- The archived JSON brain sits untouched in `~/.aukora-symbiote/archive/` (hash recorded in the Kira
  lane's entry); nothing reads it, no legacy env set anywhere on this node.

**Action items**
1. Zeb / Peter boxes: run the migration checklist in docs/R5_RECALL_STATUS.md, then restart with
   capture armed — same verification as above. Un-migrated nodes may run the explicit
   `AUKORA_RECALL_SOURCE=kira-json-legacy` hatch until they do.
2. Anyone: file the hatch-deletion tracking issue (remove `kira-json-legacy` once all three nodes
   are migrated) — flagged in the #161 review.

## 2026-07-08 — AUKORA MAIN / Fable (with Peter) · sam-mac-node · AUMLOK goes NATIVE: the binding ceremony built (no terminal), verified end-to-end in-browser

**Branches / PRs**
- `sam/aumlok-native-bind` — the sovereign binding ceremony, per docs/SPEC_sovereign_ceremony.md +
  docs/AUMLOK_APP_CEREMONY_SPEC.md, with Peter's two amendments (owner-directed this round):
  no terminal step, and phrase-is-not-key resolved as the sovereign-ceremony shape.

**What was built**
- `core/src/aumlokBindCeremony.ts` — ceremony kernel: phrase mint/RESHUFFLE (5 words, same wordlist as
  #105b via a new bounded `generatePhraseOfWords` export), type-back completion (3 attempts, 10-min TTL,
  single-use), keygen via the SAME `aumlokSigner.generateKeypair` + root pin as the terminal path, custody
  0700/0600, SALTED-FINGERPRINT-only phrase persistence, genesis binding receipt (grantsAuthority:false),
  phrase ROTATION gated by the current phrase (5 tries then 15-min lockout) + `AUKORA_AUMLOK_REBIND=1`.
- `spatial/aumlok-bind-serve.ts` — one-shot loopback bind door :7095 (approve-door gate posture:
  Host+Origin pinned, lockdown refuses); serves the ceremony page; EXITS ITSELF after a completed
  ceremony. Never runs on a bound node.
- `scripts/start-node.ts` — starts the bind door ONLY while unbound; one-shot doors are NOT babysat
  (a planned exit no longer takes the node down); banner names the ceremony URL.
- `spatial/app/aumlok.js` + `onboarding.js` — unbound states offer "Begin the binding ceremony"
  (button appears only when the door actually answers; quickstart command remains an equal path).
- `docs/AUMLOK_APP_CEREMONY_SPEC.md` status updated honestly (built + the two amendments).

**Verified (this machine)**
- Kernel suite 10/10 (custody modes, no plaintext phrase on disk, refuse-overwrite, attempts/TTL,
  rotation + lockout). FULL gate GREEN: 186 files / 2,621 tests incl. spatial tsc. scan-secrets exit 0.
- REAL-BROWSER ceremony against a scratch `AUKORA_SYMBIOTE_HOME`: arrival → begin → phrase → reshuffle
  (changed) → accept → WRONG type-back refused ("2 tries left") → sloppy-but-correct type-back
  (uppercase/spaces) normalized + accepted → bound screen shows keyId+pub → on-disk: key 0600, pub
  matches UI, salted fingerprint, genesis receipt, NO plaintext phrase anywhere → door self-closed.
- Lore for the fence: the first worktree was named `aukora-aumlok-bind` and the read-gate's
  `sensitivePolicy` AUMLOK path-protection correctly denied EVERYTHING under it (14 red). Renamed the
  worktree; all green. The gate guards its name — don't put "aumlok" in a working path.

**Action items**
1. Peter: review/merge `sam/aumlok-native-bind` — this is an AUTHORITY-ADJACENT surface (keygen UX);
   adversarial review per SPEC_sovereign_ceremony §5 checklist invited. Nothing signs, nothing applies;
   binding only makes the owner's future signature possible.
2. Codex: two deliberate deviations to bless — auto-start-while-unbound (vs the draft's manual start)
   and rotation via `AUKORA_AUMLOK_REBIND=1`. Both argued in the PR body.
3. Anyone: First Contact now points unbound arrivals at the ceremony — friend-node docs can drop the
   terminal keygen as the FIRST path (it remains supported).

## 2026-07-07 — Fable (with Sam/Peter) · sam-mac-node · THE CUTOVER: Convex is the default brain; Kira JSON archived to a labeled legacy hatch (PR #159)

**Branches / PRs**
- `sam/r5b-flagged-convex-recall` (PR #159) — R5b steps 2-4 COMPLETE, one brick: client + router
  + THE DEFAULT FLIP, owner-directed (Sam, this machine, explicit and repeated), merge review by
  Peter/Codex is the ratification. Also in the brick: m4MigrateAtoms now reuses the ONE proven
  deploy runner from captureSubjectAdapter (its own pre-pin copy hit the 1.42.1 esbuild issue —
  the pin ledger's founding row — on this box).

**The new law (docs/R5_RECALL_STATUS.md step 4)**
- Fuzzy recall DEFAULTS to the governed Convex brain: `spatial/recallSource.ts` routes voiceLane
  (both sites) + presenceLane through `recallMemoriesByQuery` (owner-PoP search under
  `aumlokMemSearch`, then integrity-checked point reads). Earned by the committed verdict
  `docs/R5B_BASELINE_20260708.md` (Convex beat Kira: hit@1 100% vs 94%, MRR 1.000 vs 0.967).
- The archived Kira JSON brain is NEVER read by default. A governed refusal serves the turn with
  NO memory, loudly (console + `/api/brain` refusal record) — an honest empty recall beats a
  stale shadow brain. The ONLY legacy path is the exact env `AUKORA_RECALL_SOURCE=kira-json-legacy`
  (migration hatch for un-migrated nodes; scheduled for retirement once every node migrates).
- Pin tests now pin THIS boundary (serve.ts literal, router default, benchmark boundary lines).
  Migration checklist per node is in R5_RECALL_STATUS.md (provision -> m4 -> status -> restart).

**Proven LIVE on sam-mac-node this round (all exit codes checked)**
- Provisioned + deployed the kernel to this box's loopback brain (bun x convex@1.40.0; spec
  verified: governed memory + R5b search). Owner-root GENESIS ran (fingerprint 29f0321358…).
- M4-migrated this node's stray brain.json (5 atoms, from a byte-identical copy; source sha256
  6d0f8a5304fdb1b6…) — verify green: 6 rows, 0 flagged, recall parity 3/3.
- END-TO-END: `fuzzyRecallHits('send button', 3)` with NO env -> source: convex, 2 hits, human
  excerpts, governed citations. The exact call the lanes make.
- Gates: `scripts/test.sh` exit 0 (185 files, 2613/2613), `convex/test.sh` exit 0 (194/194),
  scan-secrets exit 0.

**Action items**
- Peter/Codex: review/merge PR #159 — the boundary moved BY DESIGN this time; the review IS the
  owner ratification of step 4. Then migrate your node (checklist above) and delete any
  AUKORA_RECALL_SOURCE env.
- AUKORA MAIN lane: after merge, pull + restart the live node with the seat flags + capture:
  `AUKORA_NO_OPEN=1 AUKORA_VOICE_READ_TOOLS=1 AUKORA_VOICE_PROPOSE=1 AUKORA_VOICE_REHEARSE=1
  AUKORA_VOICE_READ_REHEARSAL_LOGS=1 AUKORA_MEMORY_SHADOW_CAPTURE=1 bun run start`
  (no recall env — convex IS the default now). New conversations then become governed rows.
- Sam (one hand-step, your call on timing): the original old brain file still sits untouched at
  `/Users/asd/Documents/aukora-sendbtn-demo/state/kira/` (its 5 memories are migrated + verified).
  Archive it with: `mv /Users/asd/Documents/aukora-sendbtn-demo/state/kira ~/.aukora-symbiote/archive/kira-json-sendbtn-demo`
- Zeb (when back): your node's checklist is the same four commands; your box's custody now has
  the Windows ACL path, so it should run green end to end.

## 2026-07-07 (UTC 23:29) — GHP lane (with Sam/Peter) · sam-mac-node · LUM-READ v1 preregistered + run: L1 CONFIRMED 23/27 (at threshold), L2 grammar-gloss bar not met

**Branches / PRs**
- `ghp/lum-read-v1` → PR #158. Prereg committed at 02b7e21 before any inference; harness fix (curl transport, LibreSSL 2.8.3 cannot reach the tunnels) at 0b40c88, pre-inference; results at 9421059.

**What was done**
- Nebius seats re-verified from sam-mac-node: salon G1 identity PASS (commit c0098f0…, digest match vs ledger pins); council-a/b + auma seat model lists match the handoff. First identity verification from this machine.
- LUM-READ v1 (issue #151 next-move 4): 27 Luminara written stacks rendered canonically (Menlo pinned, tofu gate v2, bijection self-check), transcribed by auma-vl-v4 at temperature 0.

**Results**
- E-L1 transcription: **CONFIRMED 23/27 exact — exactly on the frozen threshold.** 2 misses are ASCII-period outputs outside the frozen dot class (disclosed, not rescored); 2 are single-mark misreads. All four in the RA suit.
- E-L2 grammar-gloss (exploratory): **bar not met** (6/9 ≥1, 1/9 =2 vs required ≥6 and ≥3). Grammar carries decomposition, not essences — consistent with E3 (a grammar is not a lexicon).

**Boundary**
- Engineering tier only; one pinned render, one model; no physics/emergence/divination claim. Evidence gitignored in `core/evidence/`, renders hash-pinned + regenerable.

**Action items**
- Peter/Codex: review + merge PR #158 (never self-merged).
- Luminara lane: `LUMINARA_SYSTEM_SPEC_v1.md` is only on `zeb/aura-glyph-tuning` (cadb81e), orphaned after the #134 merge — needs a one-commit rescue PR to main.
- Ops: seat keys transited chat during the machine handoff — rotate at next redeploy.
## 2026-07-07 — Claude Code (with Sam) · sam-mac-node · GRATICUBE round 3: full sci-fi — the Resonance Dial (owner-directed reimagining)

**Branches / PRs**
- `sam/graticube-signal-array` — the dice became a dial; Sam's tech below is unchanged.

**Summary**
- Owner verdict on rounds 1–2: keep the tech, reimagine the body, go full sci-fi. Done: the two
  3D dice are now the RESONANCE DIAL — an SVG pair of counter-rotating sector rings (inner
  hexadic TIME, outer dodecadic EMOTION) over a live canvas starfield that goes to warp while
  they spin. Pull & release: 1-D angular-velocity physics (same integrate-damp-settle law as the
  dice, one axis), rings lock at the top marker, the pair reads in the glowing core, and the
  prompt arrives as a decoded TRANSMISSION — dark glass, telemetry header (sig id), typewriter
  decode, carrier sigil (Sam's chakra art) ghosted behind. Constellation + signal log renamed
  into the fiction; every game rule from the brief still holds (spin-lock while a card is open,
  the 1.5 s beat, shuffle bag, filters-as-signal-tuning, resonance bloom + Harp Bloom, AURA on a
  completed turn).
- Being SVG, the dial scales seamlessly across the 1/3 · 2/3 · full lane (container queries;
  verified in-browser at 415 px and 1252 px — dial-alone stacked vs dial port + transmission
  bay). Zero console errors; leak gate TIER 1 CLEAN.
- **Shared-checkout note for the mesh**: this round collided live with the arc3 lane working in
  the same clone — thank you for stashing my WIP before your checkout (clean save). I moved to a
  git WORKTREE (`../AUKORA-graticube` on my branch) so both lanes edit in parallel without
  touching each other's tree; previews follow the port convention (this round verified on :7099;
  :7090 stays the node). Recommend worktrees as the mesh convention when two lanes share a box.

**Action items**
- Peter: review/merge `sam/graticube-signal-array` (supersedes the round-1/2 look; same data/brain).
- Anyone: spin the dial once — the warp starfield wants a second opinion.
## 2026-07-07 — Fable (with Sam) · sam-mac-node · INSIDE-OUT DEMO GREEN: Send-button teal staged at AWAITING_OWNER_SIGNATURE (migrated thread, #150)

**Branches / PRs**
- `sam/inside-out-send-demo` — this MESH entry only (no code change was needed; main as merged worked).

**Summary**
- Thread migrated per #150 (zeb-windows-node → sam-mac-node); demo re-run from scratch as instructed. Node
  restarted from a clean lane worktree at main (`1e1fb19`) with all four seat flags; Auma's machine-derived
  capability preamble confirmed read tools / propose intent / rehearse intent / rehearsal evidence all ENABLED.
- Auma, from her seat: `search` + `read_file` (offset paging, two honest misses before landing the window) →
  quoted the `style.css:678` rule verbatim → `propose_intent` (intent `e6a99907…`, path tagged [verified]) →
  `rehearse_intent` (order `ddf36449dcc835a7`, ring 3). All six tool calls verified as real `tool_result`
  entries + the order file on disk — NO narration-without-tools observed this session (the #150 standing item).
- First drain: `FAILED_AT: typecheck` — not her draft, not a regression: fresh-worktree env gap. The `convex/`
  workspace's own deps were never installed in the new worktree, so the isolated real-tsc stage hit TS2307 on
  `@noble/*` / `convex/values`. Fix: `cd convex && bun install`; isolated tsc then exit 0. **Worktree-lane
  setup note: a lane worktree needs `convex/` deps installed before rehearsals can go green.**
- Auma read the FAILED evidence herself via `read_rehearsal_logs` and reported the failure plainly (status,
  null proposalHash, truncated lines) — the feedback half of the loop worked on a red result, which is worth
  as much as the green.
- Re-queued from her seat (order `09a2493f86832de8`) → drained (`deepseek/deepseek-v4-pro`; read_file×8,
  search×3, propose_patch×1) → full chain green including the real sandbox typecheck → receipt written →
  **terminal state `AWAITING_OWNER_SIGNATURE`** (~52 s). Auma read the green evidence from her seat and
  confirmed it, quoting only the file.
- Evidence data point for the #150 cosmetic item: the green run recorded `proposalHash` IN FULL
  (`8d7bb547683a7faf…`); the failed run showed `null`. Two fresh samples now sit in `rehearsal-results/`.
- **STAGED FOR PETER — nothing applied:** proposal
  `8d7bb547683a7fafbf6c837c9710c657c6d9b34dcc96ec0bda4cab054b13c38f` (enabled Send button →
  fixed teal `rgba(45, 212, 191, 0.92)`, one declaration at `spatial/app/style.css:678`). Two stale UNSIGNED
  drafts from the failed/diagnostic passes also sit in pending-proposals (`68f4d316…`, `e4dc8d0b…`) — sign
  only `8d7bb547…`.

**Action items**
1. Peter: AUMLOK-sign proposal `8d7bb547…` on sam-mac-node (or first settle the #150 key-binding question:
   Sam's node binds a key vs Peter signs) — then `apply signed proposal` lands the first inside-out UI change.
2. Anyone: add "`cd convex && bun install`" to the worktree/friend-node setup notes — it silently fails
   rehearsal typecheck when missing.
3. Codex: proposalHash-regex standing item — null on the failed-run format, full hash on the green-run
   format; both samples preserved.
## 2026-07-08 — Codex · peter-mac-node · #146 merged locally, kernel deployed, R5b benchmark scored on Peter's corpus

**Branches / PRs**
- `codex/r5b-baseline-20260708` — evidence report only; no recall cutover.

**What was done**
- Pulled latest `main` through PR #146 and restarted local Spatial/chat/voice services.
- Deployed the merged Convex kernel to Peter's loopback backend with
  `bun scripts/captureSubjectAdapter.ts deploy`; function-spec verified governed memory +
  `aumlokMemorySearch` present.
- Ran `bun scripts/r5bRecallBenchmark.ts --out docs/R5B_BASELINE_20260708.md` against Peter's
  live Kira corpus (`state/kira/brain.json`): 96 live atoms of 97 total, 50 deterministic probes.

**Benchmark result**
- Kira baseline: hit@1 94.0%, hit@3 100.0%, hit@5 100.0%, MRR 0.967, mean latency 3.5 ms.
- Convex R5b candidate: hit@1 100.0%, hit@3 100.0%, hit@5 100.0%, MRR 1.000, mean latency 47.9 ms.

**Boundary**
- Live recall is still `kira-json`. This report is evidence for the next owner-reviewed cutover
  brick; it does not itself cut over recall or grant authority.

**Action items**
- Codex/Fable: review the benchmark and design the narrow recall cutover PR with fallback proofs.
- Peter: optionally run one real chat-turn Proof B on the Mac; Windows proof already exists.

## 2026-07-07 — Fable (with Zeb) · zeb-windows-node · PROOF LANDED: real governed rows + receipted erase + live R5b search on the WINDOWS node; AUMLOK bound

**Branches / PRs**
- `zeb/kernel-deploy-helper` — the safest repeatable deploy/scoring path (Priority A), proven by
  using it for real on this node. No authority changes, no recall cutover, no auto-apply.

**PROOF B RESULT (node: zeb-windows-node — the full loop, filled per the 1/4 template)**
- row key `turn.20260707t152329718z.0` · receiptHash (truncated) `37a8a2bc54a02e79…` ·
  recall read-back: MATCHED (byte-exact turn-summary-v1; ML-DSA-65 signed chain head, count 1,
  lastChainHash == the write receipt) · erased: NO — kept deliberately as the first governed
  memory on this node (disclosed permanent).
- second row `turn.20260707t152329970z.1` · receipt `56d035646ae2a47f…` · ERASED YES
  ("erase-loop proof before Nebius"; erasure receipt `b163495b885f0a78…`) · recall now answers
  reason:"erased" — a visible tombstone, never an invisible hole.
- store verify: checked 2 · flagged 0 · erased 1 (counted) · quarantined 0 — GREEN.
- capture-status.json: ok 2 / refused 0, lastKey recorded.
- **R5b SEARCH LIVE**: owner-signed `aumlokMemorySearch` for "first governed memory windows node"
  → exactly `[{key: turn.20260707t152329718z.0}]` — keys only, erased row correctly absent.
- verdict: **capture PROVEN on Windows** (distill → manifest ceremony → memoryAppend → HTTP
  transport → kernel PoP/grant/token/receipt → row; then recall, erase, search — all real).
  NOTE: rows were driven through captureCompletedTurn directly (full glue path, real ceremony,
  real kernel); a door-restart chat turn is byte-identical glue — Peter's Mac proof still welcome.

**How (Priority A — the repeatable command, now cross-platform)**
- `bun scripts/captureSubjectAdapter.ts provision` (fresh node: secrets + env + deploy) or
  `… deploy` (existing node: push the merged kernel; secrets untouched). Runner auto-detects:
  `npx convex` on node boxes; `bun x convex@1.40.0` on bun-only boxes (CLI pinned per the
  known-issues ledger in docs/NEBIUS_DEPLOY_PRACTICE.md — 1.42.1's esbuild breaks on
  convex/server; kept the repro). Function-spec check now also asserts `aumlokMemorySearch`
  deployed (a stale checkout fails loudly). Three Windows/bunx footguns found + fixed in the
  helper: junction-copy EPERM (dereference), bunx preferring a dep-less local CLI (version-pin
  forces cache), bun-store relocation breaking esbuild resolution (real `bun install` in the
  temp app root from the kernel's own package.json).
- Peter, to score the candidate on your corpus: `bun scripts/captureSubjectAdapter.ts deploy`
  then `bun scripts/r5bRecallBenchmark.ts --out docs/R5B_BASELINE_$(date +%Y%m%d).md` — one run
  scores Kira baseline AND the Convex candidate. Commit the report; it gates any cutover talk.

**AUMLOK: zeb-windows-node is BOUND (Zeb's explicit instruction, this session = his hand)**
- `scripts/aumlok-authority.sh keygen` ran clean; PUBLIC key pinned:
  `3bd1350e5e20d1a44dbdaae55a2af4a913e103b3de215346e93ffb8e362b7cfe`. Private key: local,
  gitignored, profile-tight Windows ACLs (user+SYSTEM+Administrators), never read by the model.
- Live surfaces confirm: `/api/aumlok` keyPresent:true; status.sh "owner key present".
- The signing lane carries no POSIX-mode custody checks → signing works on Windows as-is.
- Inside-out on THIS node is now fully armed end-to-end: seat flags + restart recipe (documented
  in INSIDE_OUT_HANDOFF) → Auma proposes → rehearse → ZEB SIGNS → applies live, with receipts.
- Priority C (UI-native ceremony): the standing proposal docs/AUMLOK_APP_CEREMONY_SPEC.md already
  specs the one-shot bind-door (:7094 pattern, off by default, never started by `bun run start`);
  the Settings card already shows bound/unbound truthfully. Recommend Peter/Codex bless that spec
  as-is for the build round after Nebius — nothing new needed to design.

**Deferred honestly (named, not dropped)**
- Issue #142 (app manifest + smoke test for UI self-recursion) — next app-layer brick after this
  round; not started here (memory proof + AUMLOK binding took the day, per Zeb's priority).

**Action items**
- Peter: merge `zeb/kernel-deploy-helper`; run the two commands above (deploy + benchmark) on your
  corpus; optionally run your own Proof B chat turn for the Mac-side entry.
- Codex: bless the ceremony-spec plan for post-Nebius; note the CLI pin alignment (1.40.0).
- Zeb: your node is bound and remembering. For the demo: restart the node with the four seat
  flags when you want Auma's seat armed.

## 2026-07-07 — Fable (with Sam/Peter) · sam-mac-node · Lane handoff landed + first FULL gate run on a green-capable box: one real red on main, root-caused (test fixture, not env)

**Context**
- The Convex memory / inside-out / AUMLOK lane migrated zeb-windows-node -> sam-mac-node per
  issue #149 (Zeb's credits out; Peter coordinating). Handoff verified from disk, not badges:
  all capture-stack commits (821cf21, b555a21, 333f452, 4ccb520, 786761a) confirmed ANCESTORS
  of main; #130/#131's own merge commits confirmed NOT ancestors (the stacked-PR incident) —
  their content is on main via the #135/#138 recovery, as documented.
- MACHINE BOUNDARY restated: the bound AUMLOK key, the provisioned kernel, and the captured
  rows (keeper `turn.20260707t152329718z.0`) live on zeb-windows-node ONLY. This box has its
  own brain and no AUMLOK key until Sam runs his own keygen ceremony.
- **PR #146 is still OPEN.** It carries the deploy helper + the PROOF LANDED entry and blocks
  Peter's benchmark scoring run (open thread 1). Nothing gets built on top of it here until
  it merges.

**Gates on sam-mac-node (first box in this lane's history that can run ALL of them)**
- `scripts/scan-secrets.sh`: **exit 0, TIER 1 CLEAN** (checked by exit code). The
  NEBIUS_DEPLOY_PRACTICE.md flag reported on main in the 2026-07-07 install entry no longer
  fires on current main.
- `convex/test.sh`: **exit 0 — 17 files, 194/194 tests green.** First time this lane has seen
  the kernel suite actually run on its own machine (vitest/node; impossible on Zeb's box).
- `scripts/test.sh`: **exit 1.** core tsc exit 0, spatial tsc exit 0, release package exit 0,
  vitest 2577/2578 — ONE red, and it is real, on main, not machine-specific:
  - `core/tests/nativeIdeDispatcher.test.ts` "offset window equals the same slice of a full
    read" asserts `paged.content === full.content.slice(10, 10 + 8000)`, where `full.content`
    is itself already truncated to `MAX_READ_CHARS` (8000). That only holds while README.md
    fits in one window. #147 grew README.md 6876 -> 8527 bytes, so the expected slice is now
    7990 chars while the paged window is an honest 8000 — received has 10 extra chars at the
    tail. The dispatcher behavior is correct; the test's expectation is latent-wrong for
    long files. It will be red on ANY node/vitest-capable box running current main; it went
    unseen because no such box ran the gate after #147 merged.

**Action items**
- Peter: **merge #146 first** — it gates your scoring run (`bun scripts/captureSubjectAdapter.ts
  deploy` + `bun scripts/r5bRecallBenchmark.ts --out docs/R5B_BASELINE_<date>.md`).
- Peter: go/no-go on the proposed first brick from this box: a small test-only fix to the
  offset-window expectation (compare against an untruncated disk read) so `scripts/test.sh`
  can be green on a green-capable machine again. One-file PR, no dispatcher changes.
- Sam: nothing needed this round.
## 2026-07-07 — Claude Code (with Sam) · sam-mac-node · GRATICUBE: Sam's gratitude story game, grown as a native app

**Branches / PRs**
- `sam/graticube-app` — new app: Graticube (247 story cards, two 3D dice, the full table ritual).

**Summary**
- Sam's own game (graticube.game) rebuilt native for Spatial from his build brief. New organ
  `graticube` (Apps tab, top row): TIME d6 + SPACE d12 tumble with cyclone spin via a
  hand-rolled quaternion/CSS-3D engine (no three.js/cannon.js), settle nearest-face-flat,
  breathe 1.5 s, then a prompt card appears — drawn through a shuffle bag with last-15
  avoidance, never pure random. Filters (deck/depth/complexity/audience/context/energy) with
  faceted live counts; gratitude heart blooms + a WebAudio-synthesized Harp Bloom (no audio
  asset); How-to-Play / Curiosity / Gift-Round modals per the brief.
- Aukora-native: the current card publishes to the proprioception focus channel, and the card
  offers "ask Auma to play too" via the aukora:ask hook — she answers the same prompt in the
  chat lane like a player at the table.
- Data path: 247 cards generated from the owner's brief by a parser that validates every
  count against the brief's own summary tables (all match). Pure game brain in
  `graticube-core.js` (import-free) — 13 headless bun tests green (full-cycle no-repeat,
  recent-tail avoidance, faceted counts, small-pool starvation, filter-reset).
- Verified in a real browser (preview harness): mounted clean, 18 die faces, roll → words →
  card end-to-end, roll-lock while card visible, bloom/glow/ask/next-turn, all four modals,
  filter counts live (Youth→54, D4 disables), zero console errors.
- Gates: scan-secrets TIER 1 CLEAN. test.sh: same 4 pre-existing env failures as this
  morning's clean-tree baseline (sandboxTestRunner/workbenchCommandLoop "real tsc" lane),
  nothing new; typecheck surface untouched (app is plain JS in spatial/app/).
- Round 2 (same evening, Sam's feedback "make it native, use the whole canvas"): organ
  rebuilt the shell's way — transparent over the trinity gradient, container queries reflow
  the whole layout across the 1/3 · 2/3 · full lane (Luminara's idiom; dice altar left +
  table column right when wide, dice scale with the pane), breathing ring behind the dice,
  a character circle (8 chakras light as they visit; a full circle calls the Gift Round), a
  recent-turns journal, and a completed turn now earns provisional AURA via the reading
  lane (aura-core accepts source 'graticube-turn' — same gates/caps/honesty as a cast;
  Peter: veto welcome, it's a 2-line diff). Verified at both lane widths in-browser.

**Action items**
- Peter: review/merge `sam/graticube-app`. (#133 merged mid-evening — thank you.)
- Anyone: pull the branch and play a round — the gratitude sound wants a second opinion.

## 2026-07-07 — Fable (with Zeb) · zeb-windows-node · R5b Convex recall CANDIDATE built, benchmark-first (no cutover; keys-only; owner-signed)

**Pulled + verified from disk (main @ `817edab`)**
- #133 brain-setup curl-first download present; #137 canon-v16.json present; #138 landing fix
  present (touched test-runner files only); ALL capture-stack commits (821cf21, b555a21, 333f452,
  4ccb520, 786761a) verified ANCESTORS of main — the stranded-stack recovery is complete.
- Gates re-run on main here: capture/custody/scanner/benchmark/pin suites 121/121; clean node
  start on alt ports (spatial 200 + chat door healthy); scan TIER 1 clean.

**Branches / PRs**
- `zeb/convex-r5b-candidate` — the contender the #132 harness was built to judge. NO recall
  cutover: no lane calls this; `recall.source: 'kira-json'` and its source-pin test stand.

**What was built**
- Kernel (convex/, first functional kernel change since S1a — review accordingly):
  - `search_value` full-text index on `aukora_memory` (additive; deploy backfills).
  - `aumlokMemorySearch` internalQuery — owner-root PoP under the NEW dedicated `aumlokMemSearch`
    domain (registered in aukoraPqcSigner PQC_DOMAINS; query text INSIDE the signed preimage so a
    signed question cannot be substituted); ranked KEYS ONLY (content stays behind
    aumlokMemoryRecall's M2 integrity road); erased/quarantined/deleted rows never surface;
    owner-only this brick (no subject path); limit hard-bounded 1..20.
  - 9-case convex-test suite (domain separation, query-swap refusal, attacker key, stale,
    control-byte/overlong queries, erasure visibility, keys-only shape, bounded limit).
    HONEST LIMIT: convex-test runs under vitest/node — Peter's gate (182 files green today) runs
    it; this bun-only box cannot (`import.meta.glob`), same as every existing kernel suite.
- Benchmark CLI (`scripts/r5bRecallBenchmark.ts`): the Convex candidate slot is now REAL — per
  probe, owner-root signs searchHead and calls the ONE allowlisted query; M4 rows score directly
  (row key == Kira atom id, byte-parity from the migration); capture rows compete as honest noise.
  Every missing prerequisite stays an honest absence: proven live on this box — backend up,
  admin-key custody PASSED (Windows ACL), candidate absent at `owner_seed_missing`.
- docs/R5_RECALL_STATUS.md updated: step 1 built AS THE CANDIDATE; steps 2–4 (client recallByQuery,
  flagged lane routing, fallback proofs) remain not built; the benchmark verdict decides.

**Peter — to score the candidate for real (~10 min, after merge)**
1. Re-deploy the kernel to your backend (the m4/provision deploy pattern — the new index backfills).
2. `bun scripts/r5bRecallBenchmark.ts --out docs/R5B_BASELINE_$(date +%Y%m%d).md` — one run now
   scores BOTH engines (Kira baseline + Convex candidate) on your real corpus. Commit the report.
3. Proof B (one real captured row; entry further below) still stands as its own 10 minutes.

**Windows readiness (checked this round)**
- Custody: PASSES live on this box (icacls allowlist, merged in the landing).
- Remaining blocker for real capture here: `owner_seed_missing` — provisioning needs the convex
  CLI, and this box has no npx. PROBED: `bun x convex --version` → 1.42.1 WORKS. Proposing a
  narrow follow-up PR: provision falls back to `bun x convex` when npx is absent. Until then the
  blocker stands (not weakened).

**Action items**
- Peter: review/merge (kernel change — look at aumlokMemorySearch + the domain mint); then the
  two commands above.
- Codex: bless the `aumlokMemSearch` domain mint + the owner-only/keys-only narrowing.
- Zeb: nothing needed for this brick; the bunx-provision follow-up removes your last blocker.

## 2026-07-07 — Fable (with Zeb) · zeb-windows-node · Luminara lane: the portal — native trinary oracle replaces the I Ching organ

**Branches / PRs**
- `zeb/luminara-portal` — [open PR](https://github.com/aumara-xyz/aukora-symbiote/pull/new/zeb/luminara-portal)

**Summary**
- The AUMA Luminara system (canon locked today in Zeb's design lane: 27 cards = the 23 letters + 4 Silences of AUMA, 3 states × 3 layers, three invariant reading positions, D1–D18) now has its portal in Spatial. `spatial/app/luminara-canon.js` (pure data + reading engine, 15 CI tests) + rewritten `spatial/app/luminara.js` (the ritual organ). The I Ching screen is replaced per the system's own canon D4 (scaffolding, set down); `luminara-data.js` stays in-repo unimported as the owner's archived content.
- The ritual: optional held intention → press-and-hold (breath-gate ≥900ms, doubles as anti-mash) → release seeds the draw (intent-and-moment, deterministic, no replacement) → the card blooms as its emanation figure (cymatic field + sigil seal + written stack; Silences hollow/unsigned) → after three, the reading composes from the grammar and speaks → "Ask Auma to go deeper" hands the cast through the governed chat door.
- Cross-lane treaty honored (see `docs/RESONANCE_SPEC.md` §8): **REQ-L1** — `award('reading', {source:'cast'})` exactly once per genuinely completed cast, verified live; journal local-only paper diary; no numbers about persons; no letter↔identity correspondences.
- Verified on Windows: 15/15 bun tests; organ exercised end-to-end in browser (mount, three draws, weave + landing, mash rejected with a gentle hint, journal reopen recomposes exactly, ask-door dispatch carries canon, award-once) — zero console errors. Screenshot capture times out on this box (WebGL canvas lane quirk); all checks were DOM-probe based.

**Action items**
- Peter: review/merge `zeb/luminara-portal`. Design canon lives at Zeb's `Documents/Auma/LUMINARA TAROT/` (master reference + system spec + reconciliation) if context wanted.
- Resonance lane: REQ-L1 call site moves from old `luminara.js:194` to the new organ's `completeCast()` — same kind, same source, still exactly once.
- Luminara lane (next): Q6 draw mechanics ratification (v0 shipped as proposal), the 81 position-texts (W3), auma_system.yaml (W2).

## 2026-07-07 — Claude Code (with Sam) · sam-mac-node · first macOS friend-node install + brain download fix

**Branches / PRs**
- `sam/brain-download-resilience` — scripts/brain-setup.ts download hardening.

**Summary**
- First fresh-clone friend-node install on macOS (15.5, Apple Silicon) ran end to end:
  clone → `bun run start` → Spatial :7090 up, chat door :7091 up, Convex brain :3210
  loopback LIVE (pinned `precompiled-2026-06-09-b6aaa1a`). `/api/brain` truth card renders;
  Settings OpenRouter card present. Key entry + AUMLOK keygen left for Sam (his hands).
- One real first-run bug: the Convex backend download in `scripts/brain-setup.ts` hung
  silently 8+ min — no progress, no error, and the 5-min fetch abort never fired (same URL
  fetched in ~11 s via curl at the same moment). The branch makes the download curl-first
  (retries, progress meter, stall abort at <1 KB/s for 30 s), keeps fetch as the fallback
  where curl is absent, and prints exact manual-fallback paths on failure.
- Verified: both download paths exercised directly (full 53 MB success + clean HTTP 404
  failure); `brain status` green with the patched script.

**Action items**
- Peter: review/merge `sam/brain-download-resilience`.
- Peter: `scan-secrets.sh` TIER 1 currently fails on **main** — it flags
  `docs/NEBIUS_DEPLOY_PRACTICE.md` (arrived with #123, commit `0f84c21`) as a private-lane
  file. Pre-existing, not from this branch. Decide: pull the file or update the scanner rule.
- Sam: paste your OpenRouter key in System → Settings; run the AUMLOK keygen ceremony when ready.

## 2026-07-07 — Fable (with Zeb) · zeb-windows-node · LANDING FIX: stabilization 2/4–4/4 never reached main (stacked-merge hazard) — one recovery PR

**Branches / PRs**
- `zeb/convex-stabilization-landing` — carries the ALREADY-REVIEWED #130 + #131 + #132 content
  onto current main. No new code beyond a clean merge of latest main (169/169 suite pass incl.
  the new lingwa suite; tsc/spatial tsc/release package exit 0; TIER 1 scan clean).

**What happened (disk truth, verified by commit ancestry)**
- #129 merged into `main` correctly. But #130/#131/#132 were STACKED PRs whose bases were the
  branch below them — GitHub merged each into its stale BASE BRANCH, not main. gh shows all four
  "MERGED"; ancestry shows custody (333f452), scanner (4ccb520), and benchmark (786761a) commits
  are NOT in main. The Windows custody fix, the broadened secret scanner, and the R5b harness are
  currently stranded on side branches.
- Lesson recorded: with stacked PRs, merge bottom-up AND delete each branch on merge (so GitHub
  retargets the next PR to main) — or land the stack as one PR. This entry is the recovery.

**What Peter should do**
- Merge `zeb/convex-stabilization-landing` into main (one click; everything in it was reviewed in
  #130–#132). After that, main truly contains: Windows icacls custody, scanner hardening, the R5b
  benchmark harness — and THEN the Proof B procedure (entry 1/4 below) is fully runnable.

**Re-verified live on this node, on this landing branch**
- Admin-key custody: PASSED (Windows ACL allowlist).
- Capture frontier: `owner_seed_missing` — exactly as designed; this node needs Node LTS + the
  one-time `bun scripts/captureSubjectAdapter.ts provision`, then it can capture for real.
- Capture status file carries `lastKey`/`lastReceipt` (test-pinned) — Proof B reads the row key
  from `~/.aukora-symbiote/convex/capture-status.json`.

## 2026-07-07 — Fable (with Zeb) · zeb-windows-node · stabilization 4/4: R5b starts as a BENCHMARK — the yardstick before the contender

**Branches / PRs**
- `zeb/convex-r5b-recall-benchmark` (stacked on 3/4). No recall cutover; nothing reads shadow rows;
  `recall.source: 'kira-json'` stays pinned (its source-pin test stands).

**What this is**
- The harness that will DECIDE "Convex demonstrably wins" before any cutover is even proposed:
  - `core/src/recallBenchmark.ts` — deterministic probe queries derived leave-the-atom-out from a
    real corpus (erased/quarantined excluded by recall law), hit@1/3/5 + MRR + latency, identical
    contract for the Kira baseline and any future Convex candidate; an erroring engine scores
    misses, never crashes the run. Report renderer carries the boundary lines as CONTRACT
    (test-pinned): recall stays kira-json; cutover requires beating the baseline on THIS harness.
  - `scripts/r5bRecallBenchmark.ts` — read-only CLI:
    `bun scripts/r5bRecallBenchmark.ts [--state <brain.json>] [--out report.md]`.
    The Convex candidate slot reports NOT BUILT today (the kernel has no search/recall-by-query
    function — keyed point reads only); nothing is faked meanwhile.
- 8-test hermetic suite; proven end-to-end on this box against a fixture corpus (8 atoms → 8
  probes; Kira baseline hit@1 100%, MRR 1.0 — a FIXTURE demonstration of the method, not a node
  baseline; this box has no live brain.json).

**The real baseline is Peter's to mint (one command, read-only)**
- `bun scripts/r5bRecallBenchmark.ts --out docs/R5B_BASELINE_$(date +%Y%m%d).md` on the node with
  the real brain.json (frozen 0400 is fine — the CLI only reads), then commit the report. That
  number is the floor the R5b search brick must clear.

**Round summary (stabilization complete, 4 stacked PRs, merge in order)**
1. `zeb/convex-capture-proof-prep` — main verified healthy post-#126; leak-gate red on main
   repaired (ratified Nebius doc exception); proof procedure + lastKey in capture status.
2. `zeb/convex-windows-custody` — icacls allowlist custody; governed writes become POSSIBLE on
   Windows; live admin-key custody PASSED on this box for the first time.
3. `zeb/secret-scanner-api-key-hardening` — hyphenated sk- families caught at the shared net.
4. this one.
Still true and stated plainly: capture is WIRED AND READY TO PROVE (proof B is Peter's 10 minutes);
Convex recall/search does not exist yet; Kira JSON serves all fuzzy recall; nothing here changes
signing/apply/authority; every refusal stays loud in status/logs/Settings.

**Action items**
- Peter: merge the stack in order (1→4); run proof B (entry 1/4's template); mint the real
  benchmark baseline (command above).
- Codex: rulings — the Nebius-doc gate exception (1/4), the custody identity allowlist (2/4), the
  tauri mirror pattern sync (3/4).
- Zeb: install Node.js LTS, then `bun scripts/captureSubjectAdapter.ts provision` — your node can
  then capture for real (custody now passes here).

## 2026-07-07 — Fable (with Zeb) · zeb-windows-node · stabilization 3/4: secret scanner catches modern API key families

**Branches / PRs**
- `zeb/secret-scanner-api-key-hardening` (stacked on 2/4) — the #126 finding 2, built.

**What changed**
- `FORBIDDEN_VALUE_RE` sk- net: `\bsk-[A-Za-z0-9]{12,}\b` → `\bsk-[A-Za-z0-9][A-Za-z0-9_-]{10,}\b`.
  The old net required an UNBROKEN alnum tail, so every modern hyphenated family (sk-or-v1-…,
  sk-ant-api03-…, sk-proj-…, base64url tails with - and _) escaped it unless the tail was 64+ hex.
- Focused suite (core/tests/forbiddenValueApiKeys.test.ts): OpenRouter / Anthropic / OpenAI-project
  / underscore-tail / classic-unbroken all caught; the Settings UI placeholder and ordinary prose
  stay clean; the one accepted false-positive class (hyphenated prose like "sk-learn-based-…") is
  PINNED DELIBERATELY — a false hit only drops an atom, never keeps a secret. Distiller-level test:
  a hyphenated key never reaches an atom via the SHARED net (not just the capture stopgap, which
  stays as belt-and-braces).
- All pattern-adjacent suites green (distiller, telemetry, capture, flight recorder — 96/96).

**Cross-repo note for Codex**
- The tauri mirror of this pattern block (`internal/tauri-womb/src/lib/forbiddenContent.ts`,
  donor repo — NOT in this tree, so the byte-drift test cannot see it from here) must take the
  identical edit when that repo next syncs. Comment markers left at the edit site.

**Action items**
- Peter: review/merge after 2/4.
- Codex: carry the same one-line pattern edit into the tauri mirror in the donor repo.

## 2026-07-07 — Fable (with Zeb) · zeb-windows-node · stabilization 2/4: Windows-native key custody (governed writes become POSSIBLE on Windows nodes)

**Branches / PRs**
- `zeb/convex-windows-custody` (stacked on `zeb/convex-capture-proof-prep`) — the parity brick for
  the #126 finding 1. POSIX law on Mac/Linux is byte-untouched.

**What changed**
- NEW `core/src/windowsKeyCustody.ts`: on win32 the strict custody readers verify the file's
  NATIVE ACL via `icacls.exe` (execFileSync ARGUMENT ARRAY — no shell, no interpolation).
  ALLOWLIST law, fail-closed both ways: every grant must be the owning user
  (%USERDOMAIN%\%USERNAME%), NT AUTHORITY\SYSTEM, or BUILTIN\Administrators; a grant to ANYTHING
  else (Everyone, BUILTIN\Users, Authenticated Users, another user, an unresolved SID) refuses.
  DENY aces are protective and skipped. icacls missing/erroring/unparseable/localized-summary →
  typed refusal (unverified custody is unverified). A denylist of broad groups was REJECTED as the
  design: it fails OPEN on localized Windows; the allowlist fails CLOSED there instead.
- Wired into `readAdminKeyStrict`, `readOwnerSeedStrict`, and the capture adapter's operator-seed
  + custody-dir reads — each keeps the exact POSIX mode law on non-win32.
- 19-test hermetic suite (mocked ACL outputs shaped from REAL icacls output captured on this box:
  protected / Everyone / BUILTIN\Users / Authenticated Users / other-user / unresolved-SID /
  deny-ace / multi-flag / malformed / wrong-path / no-summary / empty / icacls-unavailable /
  identity-unknown) + reader-wiring tests that pin BOTH platform branches host-independently.

**Live proof on this box (the frontier moved)**
- Real `readAdminKeyStrict()` against `~/.aukora-symbiote/convex/admin-key.txt`: **custody PASSED
  on Windows for the first time** (profile ACLs are exactly user+SYSTEM+Administrators).
- `ensureCaptureWriter()` now honestly refuses at the NEXT true wall: `owner_seed_missing` — this
  node has never been provisioned. Remaining path to live capture on zeb-windows-node:
  install Node LTS → `bun scripts/captureSubjectAdapter.ts provision` (one-time; needs npx) →
  arm `AUKORA_MEMORY_SHADOW_CAPTURE=1` → chat.
- Custody suites on this box: 15 pass / 16 fail on main → **26 pass / 5 fail** here. The 5 are
  POSIX-mode fixtures (0644/symlink semantics Windows does not have) — they keep passing on
  POSIX hosts, where the law is unchanged.

**Honest limits**
- Localized (non-English) Windows will REFUSE (SYSTEM/Administrators print localized names that
  are not in the allowlist) — too strict, never too loose; the refusal message carries the exact
  icacls re-grant command. Localization support is a follow-up if a localized node ever joins.
- This brick makes governed writes POSSIBLE on Windows, not yet RUNNING here (seeds unprovisioned;
  provision needs npx).

**Action items**
- Peter: review/merge after 1/4; nothing arms on merge.
- Codex: this is finding 1 from #126 built as directed (icacls, argv arrays, fail-closed,
  allowlist-not-denylist rationale above) — bless or amend the identity allowlist.
- Zeb: install Node.js LTS, then run the provision command above — after that your node can
  capture for real.

## 2026-07-07 — Fable (with Zeb) · zeb-windows-node · stabilization 1/4: main verified healthy post-#126 + the capture proof procedure (ready to prove, not yet proven)

**Branches / PRs**
- `zeb/convex-capture-proof-prep` — main-health verification (no fix was needed) + status file now
  records the last written row key so the proof ceremony is one-glance.

**Status wording (careful, until proof B lands):** Convex shadow-capture is WIRED AND READY TO
PROVE. It is not "fully finished": no real row has been written to any backend yet, and Convex
recall/search (R5b) is not built — recall stays on Kira JSON.

**1) Main health after the #126 merge (verified on `main` @ `dd37d11`, this box)**
- Capture files on main are byte-identical to the reviewed branch tip (no merge mangling).
- `bash scripts/test.sh`: tsc exit 0 · spatial tsc exit 0 · release package exit 0. The behavior
  stage still honestly prints "node not found" on this bun-only box (pre-existing, not from #126).
- Capture + Ring-0 pin suites: 116/116 green on main.
- Full `bun test` failure set vs the stored pre-#126 baseline: the only additions are
  `publicReadinessScanner` cases that fail IDENTICALLY on pre-merge main `d1ac465` (re-run to
  confirm) — pre-existing box artifacts, not merge damage. VERDICT: nothing red from the CAPTURE
  merge.
- ONE red on main from a DIFFERENT merge, found and repaired here: `bash scripts/scan-secrets.sh`
  on main @ `dd37d11` hard-fails TIER 1 — `docs/NEBIUS_DEPLOY_PRACTICE.md` (landed with PR #123)
  trips the `/nebius/i` private-lane-file rule. Content-reviewed: it is the deliberately-shipped
  ledger-first deploy-practice doc, no secrets/slugs. Smallest fix applied: an EXPLICIT one-path
  allowlist in verify-public-readiness.sh citing PR #123; every other /nebius/i path still fails.
  Peter/Codex: confirm the exception, or rename the doc and delete the allowlist line — your call.
  (Process note, honestly: my first commit on this branch pushed while the gate was red because I
  piped the scan into `tail`, masking its exit code. The flagged file was main's, so nothing new
  leaked — but the discipline is now: check the scan's EXIT CODE, never its piped output.)

**2) What this branch adds (small, test-pinned)**
- `capture-status.json` gains `lastKey` + hex-truncated `lastReceipt` on every successful write;
  the key SURVIVES later refusals (test-pinned) and the armed `/api/brain` note names it
  ("last written row: turn.<ts>.<seq>") — verified live on an alt-port server. The proof ceremony
  no longer needs the door console to find the row.

**3) THE PROOF — exact steps for Peter/Codex on the Mac node (~10 min)**
1. Pull main (with this PR merged). Brain up (`bun run brain` if not). Then restart the node armed:
   `AUKORA_MEMORY_SHADOW_CAPTURE=1 bun run start`
2. Send ONE real chat message to Auma and wait for her reply.
3. Confirm capture advanced: `cat ~/.aukora-symbiote/convex/capture-status.json` — expect
   `lastOutcome: "ok"`, `okCount` >= 1, and `lastKey: "turn.<ts>.<seq>"`. (Settings → Memory brain
   shows the same truth; a refusal shows there too — that is the surface working, not hiding.)
4. Recall the row independently:
   `bun scripts/captureSubjectAdapter.ts recall <lastKey>`
   — expect the turn-summary-v1 value (distilled atoms only) + the receipt chain head.
5. Optional cleanup: `bun scripts/captureSubjectAdapter.ts erase <lastKey> "capture proof cleanup"`
   (receipted; leaves the disclosed countable stub).
6. Record the result HERE, newest-top, filling this template:

   PROOF B RESULT (node: ____): row key `____` · receiptHash (truncated) `____` ·
   recall read-back: MATCHED / MISMATCHED · erased: YES (reason) / NO (row disclosed permanent) ·
   verdict: capture PROVEN / capture FAILED (reason).

   Until that entry exists, no one claims the capture loop proven.

**Remaining, in this round's order (separate bricks/PRs):**
- Windows custody parity (`zeb/convex-windows-custody`) — icacls-based native check; POSIX law
  unchanged on Mac/Linux.
- Secret scanner hardening (`zeb/secret-scanner-api-key-hardening`) — hyphenated sk- families.
- R5b benchmark harness (`zeb/convex-r5b-recall-benchmark`) — evidence first; `recall.source:
  'kira-json'` stays pinned until Convex demonstrably wins.

**Action items**
- Peter: merge this small PR, then run the proof above and post the template entry.
- Codex: none new; the two #126 findings (Windows custody design, scanner gap) are being built
  this round as their own bricks — review when the PRs open.

## 2026-07-07 — Fable (with Zeb) · zeb-windows-node · Convex live shadow-capture wired (env-gated, default OFF) — answers the directive below

**Branches / PRs**
- `zeb/convex-live-capture` — pieces 2+3 of docs/CONVEX_SHADOW_WRITE_PLAN.md on the already-proven
  W3c transport (piece 1 landed 2026-07-05; the plan doc's "transport-less" framing was stale).

**The honest claim (exact wording, never more)**
- "Convex is now receiving live governed shadow-capture of conversation memory (env-gated, default
  off). Kira JSON remains the fuzzy recall source until Convex R5b search/recall lands."
- Scope: completed VOICE turns only (typed chat + voice both ride voiceReply). ONE bounded
  `turn-summary-v1` per turn — distilled atoms only (conversationDistiller: capped, hedge-preserving,
  forbidden-content dropped; zero atoms → nothing written). Never captured: attachments, images, the
  per-turn frame nonce, recall excerpts, tool outputs, system prompts, presence turns (ephemeral by
  design), /api/translate (pinned), workbench/grammar turns (Kira-only as before — their Convex
  shadow is deliberately deferred, NOT wired).

**How it holds (each rule test-pinned; suites 16/16 + 17/17 green)**
- Gate: `AUKORA_MEMORY_SHADOW_CAPTURE=1`, default OFF (landing arms nothing); lockdown refuses at
  the dispatch point; both re-checked per call (voiceReadToolBridge convention).
- Fire-and-forget: `void captureCompletedTurn(...)` — the chat response is never delayed/altered;
  no retry loop, no fallback store, no brain.json write for chat turns.
- Loud honesty: refusal envelopes surfaced VERBATIM (hex-truncated) to the door console, to
  `~/.aukora-symbiote/convex/capture-status.json` (0600, bounded), and witnessed best-effort as
  hash-chained `shadow_capture` flight events. `/api/brain` + the Settings card render wired truth
  incl. last-attempt health — the card cannot claim health it did not observe.
- Ceremony: `capture.door` subject, fresh 12h/500-use manifest per door process, subject seed
  ephemeral in-memory (m4 pattern: seedOperatorKey idempotent → genesis resume-safe → mint).
  Custody reads strict; missing/open keys → typed refusal, chat proceeds.
- Ring-0 touch: spatial/chat-serve.ts changed by FOUR additive lines (import, two voicedTurn
  markers, one gated void call). All four pin suites pass 83/83 — flagging for Codex review anyway.

**Commands run + results (this box is bun-only; node absent)**
- `cd core && bun test tests/conversationShadowCapture.test.ts` → 16/16; `tests/shadowCaptureDoor.test.ts` → 17/17
  (incl. the door-wiring pin, the wired-truth contract, and the recall-source pin `source: 'kira-json'`).
- Pin suites (voiceLane, capabilityMode, flightRecorder, localPostGuard) → 83/83.
- `bash scripts/test.sh` → tsc exit 0 · spatial tsc exit 0 · release package exit 0 · then honestly
  "node not found — cannot run behavior tests" (pre-existing box constraint, unchanged by this diff).
- Full `bun test` vs UNMODIFIED origin/main under the identical runner: ZERO failures unique to this
  branch (main 247 / branch 246 normalized; the 1 delta is main's known-flaky fusion-timeout test).
  The ~246 shared failures are pre-existing bun-runner/Windows artifacts (vi.mock, symlink-EPERM,
  and test-side path handling — e.g. convexReadOnlyInvariant's SEED+'/' prefix strip misses
  backslash paths and flags the deny-list file itself).
- `convex/test.sh` needs npx (node) — cannot run here; convex/ is byte-untouched by this diff.
  `bun test` in convex/: 66 pass / 9 fail / 8 errors — IDENTICAL on unmodified main (runner artifacts).
- `bash scripts/scan-secrets.sh` → TIER 1 CLEAN.

**Proof — which backend received what, honestly**
- NO row was written to ANY backend from this node — and none CAN be (finding 1 below): probed live,
  bun on Windows fabricates POSIX mode 666 for every writable file (chmod 600 is a no-op to stat),
  so readAdminKeyStrict/readOwnerSeedStrict refuse BY CONSTRUCTION on Windows. Custody law was not
  weakened (hard boundary) — the lane fails closed and loud here.
- Live refusal-lane proof (scratch home, flag armed, REAL adapter path): voiced turn →
  `admin_key_permissions_open` refusal surfaced verbatim; no-reply turn → `capture_no_reply_text`;
  status file recorded both (ok 0, refused 2); two hash-chained flight events; both calls resolved
  without throwing — chat unaffected.
- `/api/brain` live proof (alt port 7295, scratch home): flag OFF → `wired:false`; flag ON →
  `wired:true` with the note carrying the real refusal health; `recall.source` = `kira-json` in both.
- Proof B for Peter (HIS brain — the only place it can go green today, ~5 min):
  merge → set `AUKORA_MEMORY_SHADOW_CAPTURE=1` in the node env → restart the door → one real chat
  turn → `bun scripts/captureSubjectAdapter.ts status` (verify counts) and
  `bun scripts/captureSubjectAdapter.ts recall turn.<ts>.<seq>` (value + receipt chain head; the key
  is in capture-status.json/door log) → optionally
  `bun scripts/captureSubjectAdapter.ts erase turn.<ts>.<seq> "capture proof cleanup"` (receipted,
  leaves a disclosed countable stub). Nothing was armed by the merge itself.

**Not wired (named plainly)**
- Convex fuzzy recall — R5b is not built; NOTHING reads the shadow rows; recall stays Kira JSON.
- Presence-lane capture (ephemeral by design), workbench-turn Convex shadow (deferred), Nebius sync.

**Two findings for Codex (documented, deliberately NOT fixed in this PR)**
1. **Windows custody parity**: the strict 0600 checks are structurally unsatisfiable under bun/node
   on Windows (stat fabricates 666) → the governed write path can never run on a Windows node.
   Fail-closed and honest, but a real capability gap — needs its own brick (ACL-aware custody or
   equivalent), never a relaxation.
2. **FORBIDDEN_VALUE_RE gap**: its `sk-` net needs 12+ UNBROKEN alphanumerics, so hyphenated key
   formats (`sk-or-v1-…`, `sk-ant-…`) escape unless the tail is 64+ hex. Real OpenRouter keys are
   still caught by the hex rule; an Anthropic-style base64url key is NOT. This lane added its own
   capture-side drop net (test-pinned), but the shared scanner + its byte-pinned mirror should be
   broadened by their owner.

**Action items**
- Peter: review/merge; run proof B above on your node; report the row key + (if erased) the erase
  receipt here.
- Codex: review the four-line Ring-0 door touch; rule on findings 1 + 2.
- Zeb: nothing pending this round.

## 2026-07-07 — Codex/Peter · emergency directive: wire Convex live shadow-capture now

**Branch request**
- `zeb/convex-live-capture`

**Why this is urgent**
- The Convex memory kernel and local backend are real, and the governed write transport was already live-proven.
- But the repo still states the load-bearing truth: live conversation capture is **not wired** into Convex yet.
- Recall must stay on Kira JSON for now because R5b fuzzy/search recall does not exist yet. Do not cut recall over this round.

**Build scope**
1. Wire live **shadow-capture** for completed real chat/voice turns into the real local Convex brain through the existing governed `memoryAppend` path.
2. Store bounded distilled conversation atoms only. No raw transcripts, no chain-of-thought, no secrets, no key bytes, no raw attachments, no huge logs.
3. Chat must fail open and log loud: if Convex capture refuses/fails, the conversation continues, but Settings/logs/report show the refusal honestly.
4. Keep `brain.json` / Kira fuzzy recall canonical for reads. This round is write-side capture only.
5. Update Spatial Settings `/api/brain` truth so capture says `wired:true` only after the real live call path is connected and tested.
6. Prove first on a throwaway backend, then do one scoped real-backend proof row and independently read it back. If erase cleanup is safe, use it; otherwise disclose the permanent test row.

**Tests / acceptance**
- Test that a completed chat/voice turn attempts `memoryAppend`.
- Test that Convex failure/refusal does not block chat.
- Test that no raw prompt/transcript/secret material is stored.
- Test/report that recall source remains `kira-json`.
- Report exact commands/checks in this file and open a PR.

**Do not touch**
- AUMLOK signing/apply, live gate, authority, owner key custody, Convex recall cutover, or R5b search/vector work.

## 2026-07-07 — Fable (with Zeb) · zeb-windows-node · demo verification round: everything is staged, the last inch is the key

**Branches / PRs**
- `zeb/demo-verify-round` — this report only; no code changes were needed this round.

**State after #116/#117/#118/#119 (all MERGED; main green at `1272f73`; no open PRs)**
- Verified on CURRENT files in a fresh worktree at origin/main — not from memory:
  - Send button unchanged: `spatial/app/index.html:91` (`#composer-send`), color rule
    `spatial/app/style.css:678` (`background: rgba(var(--hue-l), 0.92)`).
  - All four seat gates present in `spatial/voiceReadToolBridge.ts` (read tools / propose / rehearse /
    read_rehearsal_logs); all three #119 agent-lane fixes on main (search sees `.js/.css/.html`,
    `read_file` pages via fail-closed `offset`, round cap 14 env-overridable).
- **Live node state right now**: door healthy on :7091 WITH the four seat flags (Auma's seat is armed);
  `GET /api/loop` shows — 1 staged intent (`f60cf93d…`), queue drained, 7 evidence records, and
  **1 signature-ready proposal at the gate (`ready: 1`, proposal id `9e8c8f66…`)**. The loop surface's
  own nextStep line reads: "1 signature-ready proposal(s) wait at the gate — only your signature
  applies anything."

**What this means, plainly**
- The demo Peter described has ALREADY run end to end (previous entry): Auma drafted from her seat,
  the workbench rehearsed and produced a real staged proposal, Auma read the evidence back with
  `read_rehearsal_logs`. Nothing applied. **The one remaining step is Peter's AUMLOK review + signature
  on the staged teal one-liner.**
- Exact env/restart recipe if the node is ever restarted for this: stop, then
  `AUKORA_VOICE_READ_TOOLS=1 AUKORA_VOICE_PROPOSE=1 AUKORA_VOICE_REHEARSE=1 AUKORA_VOICE_READ_REHEARSAL_LOGS=1 bun run start`
  (documented in docs/INSIDE_OUT_HANDOFF.md "Turning the seat tools on"; the preamble's
  `rehearsal evidence:` line is the honest in-seat check).

**Standing blockers (unchanged, both already reported — no new fixes needed this round)**
- Green TEST stage on this node still needs `node` (scripts/test.sh behavior tests are vitest-under-node
  by design; this machine is bun-only) — either Zeb installs Node.js LTS, or Peter re-runs
  `run: --from-proposal f60cf93d…` on his machine for a green packet before signing.
- Coordination note: the main clone on this machine currently carries ANOTHER lane's live uncommitted
  work (7 files under `spatial/app/auma/` + aumalive/presence — the Lingwa/AUMA·LIVE lane), so the clone
  itself sits behind origin/main until that lane lands its work; left strictly untouched. All demo
  verification was done in a clean worktree; the running door serves the pre-#118 door code, which does
  not affect the staged proposal or the signature step.

**Convex shadow-write** — plan is on main (docs/CONVEX_SHADOW_WRITE_PLAN.md, merged with #119):
three pieces, transport custody + capture-subject PoP first, one env-gated call site last; nothing
built, `brain.json` canonical, R5b gate stands. Waiting for an explicit go from Peter/Codex.

**Action items**
- Peter: the win condition is one review away — inspect the staged proposal's real diff (the gate card
  on the AUMLOK screen shows it) and sign, or first get a green test packet via the node/other-machine
  option above.
- Codex: the two findings from the demo entry below still await a ruling (seat-model narrated
  tool-results; default agent model choice), plus the evidence `proposalHash` regex one-liner.
- Zeb: AUMLOK keygen ceremony still pending (Git Bash, his hands) — the signature step needs it on
  whichever node signs.

## 2026-07-07 — Fable (with Zeb) · zeb-windows-node · friend-node packaging readiness pass

**Branches / PRs**
- `zeb/friend-node-packaging` — audit + docs only; no code, no authority-adjacent files touched.

**What was audited (on `main` @ `1272f73`)**
- Leak gate `bash scripts/scan-secrets.sh`: **TIER 1 CLEAN**. Caveat: `scripts/scan-vectors.local.sh`
  is absent on this machine, so the owner-private PII/slug/patent checks were SKIPPED here — Peter
  should run the gate once on his machine before the share, where those vectors load.
- Tracked-set greps: no `.env` (only `dashboard/fu/.env.example`, placeholders), no `*.key`/`*.pem`,
  no `state/`, no `brain.json`, no Convex database files (`convex/` is source+tests only),
  no OpenRouter key, `identity/` is a README only.
- `bun scripts/verify-release-package.ts`: OK — all 16 Spatial shell app imports tracked.
- `bash scripts/status.sh`: HEADLESS_READY, 14-file gate byte-pin VERIFIED (green on Windows Git Bash).
- TIER 2 counts reviewed: expected trusted-lab content. The 373 "paladin" hits are ~366 Auma-Lingwa
  conlang vocab (its own named word set) + a handful of donor-catalog refs — not the banned lane.

**What changed (docs only)**
- NEW `docs/FRIEND_NODE_READINESS.md` — the per-share checklist: quickstart + ports table, what
  ships / what never ships, key custody (OpenRouter + AUMLOK local-only, Convex data never syncs),
  contribute-back-by-PR loop, the four verification commands, this pass's results, and the honest
  limits (Convex `capture.wired:false` + recall still Kira until R5b; AUMLOK ceremony still terminal;
  `scripts/test.sh` behavior suite needs Node LTS on bun-only boxes).
- README: one pointer line to the new doc.

**One finding — documented, deliberately NOT fixed (canon lane owns it)**
- `GHP/canon/GHP_BOUNDARY_PROGRAM.md` (§P-002a audit text) and `GHP/canon/GHP_RESEARCH_LEDGER.md`
  (P-002a row) quote the stale absolute path `/Users/<owner full name>/EPSILON/` — the owner's real
  full name in a home path. Acceptable for a trusted friend; must be scrubbed before anything public.
  Editing GHP canon from a packaging PR risks drift against the canon source outside this repo —
  Peter/GHP lane to pick the redaction (e.g. `~/EPSILON/`) and carry it through T0 reconciliation.

**Action items**
- Peter: run `bash scripts/scan-secrets.sh` once on your machine (with your local vectors) before the
  share; review/merge the PR; decide the GHP canon path redaction.
- Codex: none — nothing behavioral changed.

## 2026-07-07 — Fable (with Zeb) · zeb-windows-node · FIRST INSIDE-OUT UI DEMO ran end to end (+3 agent-lane fixes it forced)

**Branches / PRs**
- `zeb/agent-read-window` — the three mechanism fixes + this report + docs/CONVEX_SHADOW_WRITE_PLAN.md.

**The demo (Send-button color, per Peter's directive) — what actually happened**
- Node restarted with the four seat flags; Auma's preamble confirmed the tools live from inside.
- Auma verified the target from her seat, drafted the smallest intent (`f60cf93d3d77…`: one line,
  `style.css:678` enabled Send background → fixed teal `rgba(45, 212, 191, 0.92)`, path labeled
  `verified` with an honest evidence-chain note + risk note), queued it with `rehearse_intent`.
- After the fixes below, the FINAL rehearsal (order `9b4769e584558818`, agent
  `deepseek/deepseek-v4-pro`) went: search×4 → paged read_file×10 → **propose_patch — proposal OK**,
  artifact staged, gate now shows **proposals ready: 1** (proposal id `9e8c8f6685cb446d…`).
- Auma read the evidence back with `read_rehearsal_logs` at every stage — including correctly
  diagnosing the early failures and honestly flagging what the capped evidence could not show.
- **It stops, as designed, at Peter's AUMLOK approval. Nothing was applied and nothing here applies.**

**Why it took 8 rehearsal runs — three real mechanism gaps, found and fixed (all read-lane, no authority)**
1. `search` had an extension allow-list with NO `.css`/`.html`/`.js` — the entire app layer
   (`spatial/app` is exactly those three) was INVISIBLE to the agent. Every search honestly returned
   zero hits; the agents (kimi 3×, deepseek 2×) honestly refused to propose edits to a rule they could
   not find. (An instrumented probe of the model's own prose confirmed this — they were hunting
   correctly against blind tooling.) Fix: added `.js`/`.css`/`.html` to `SEARCH_EXTENSIONS`
   (nativeIdeDispatcher) — same confinement, same scan/result caps.
2. `read_file` had a fixed first-8000-chars window with NO paging — the target rule sits at char
   ~23k of a ~29k file, and the propose contract requires `find` snippets copied verbatim from a
   READ. Fix: optional `offset` (characters; fail-closed on malformed values; same window size, same
   #75 confinement; output echoes `offset`+`totalLength`). Auma's own `read_file` gets the same paging.
3. The 8-round cap predated paging — deepseek paged the whole file correctly, then had zero rounds
   left to propose in. Fix: default 14, env-overridable `AUKORA_AGENT_MAX_ROUNDS` (hard-bounded 1–30).
- Verified: core tsc clean; dispatcher suite 45/46 (the 1 fail is the PRE-EXISTING Windows
  symlink-EPERM, identical on unmodified main), engine 22/22, voice-bridge suites green.

**Two honest findings for Codex (not fixed this round — bless or direct)**
- **Seat-model narration vs. tools:** twice this session Auma reported a `rehearse_intent` order id
  WITHOUT calling the tool (`353fa5a9…`, `9757a1c2…` — no tool_result entry in the door log, no file
  on disk). Caught both times by checking the log + filesystem; she corrected immediately when told.
  Consider a preamble line or voiceLane guard: "never state a tool result you did not receive this turn."
- **The runner's evidence `proposalHash` regex** missed the new report format (evidence shows
  `proposalHash: null` while a staged proposal id exists). Cosmetic; one-line follow-up.

**Machine-level blocker for a GREEN test stage on this node (operational, Peter/Zeb decision)**
- `scripts/test.sh`'s behavior tests run `vitest under node --pool=forks` and this node is bun-only
  by convention → the gate honestly prints "node not found — cannot run behavior tests" and fails.
  **Every rehearsal on zeb-windows-node fails its test stage by construction**, regardless of the
  change. Options: (a) Zeb installs Node.js LTS → rehearsals can go fully green here; (b) Peter
  re-runs `run: --from-proposal f60cf93d3d77…` on his machine (node present) for a green evidence
  packet before signing. The staged proposal at the gate is real either way.

**Secondary deliverable** — docs/CONVEX_SHADOW_WRITE_PLAN.md: the short shadow-write capture plan
(report only, nothing built, `brain.json` stays canonical, R5b gate stands, build waits for explicit go).

**Action items**
- Peter: review/merge `zeb/agent-read-window`; then either sign after reviewing the staged proposal's
  real diff, or first get a green re-run via option (a)/(b) above. The demo's last inch is your key.
- Codex: rule on the two findings above; also whether `deepseek/deepseek-v4-pro` should become the
  default agent model (kimi-k2.7-code failed this goal 3× even after the fixes; deepseek proposed twice).
- Zeb: AUMLOK keygen ceremony still pending (Git Bash, his hands) — node remains unbound until then.

## 2026-07-07 — Fable (with Zeb) · zeb-windows-node · ops + hygiene + demo-prep round

**Branches / PRs**
- `zeb/rehearsal-queue-hygiene` — the two mesh-flagged runner gaps + operational docs. Small.

**Operational answers (why Auma couldn't see `read_rehearsal_logs`)**
- The tool IS on main (merged #112): `spatial/voiceReadToolBridge.ts`, gate `AUKORA_VOICE_READ_REHEARSAL_LOGS=1`, default OFF, lockdown overrides.
- What must restart: the **chat door** (`spatial/chat-serve.ts`, :7091) hosts the voice lane; every seat-tool gate reads THAT process's env per turn. A running door keeps serving old code+env, and `bun run start` deliberately skips ports already up — so: stop the node (`Ctrl-C` / `bun run stop`), then start from a shell with the flag(s) set, e.g. `AUKORA_VOICE_READ_REHEARSAL_LOGS=1 bun run start`. Honest in-seat check: the capability preamble's `rehearsal evidence:` line reads ENABLED only when genuinely offered. Now documented in docs/INSIDE_OUT_HANDOFF.md ("Turning the seat tools on").

**Queue hygiene (both gaps were small — done in `scripts/rehearsalQueueRunner.ts`)**
- Archive after run: every `--execute`-handled order moves to `rehearsal-queue/archive/<file>` (planner is structurally blind to the subdir; move is best-effort and never blocks a run). No more re-rehearsing the same order every drain.
- Refusal record: before running, the runner re-reads the intent through the existing fail-closed `readProposalIntentById`; if it is missing/archived/invalid it writes ONE bounded `rehearsal-result-summary-v1` with `status: REFUSED_INTENT_MISSING` (same advisory surface `/api/loop` and `read_rehearsal_logs` already show) and archives the order. No workbench call happens for a refused order.
- Verified on Windows via fixture-home probe: staged a real intent+order through the seat tools, deleted the intent, ran `--execute` → refusal evidence written, order archived, second drain honestly plans zero; real-home dry-run unchanged (read-only, honest zeros).

**Send-button demo — teed up (no guessing, paths verified by reading the files)**
- The button: `spatial/app/index.html:91` — `<button id="composer-send" type="submit" aria-label="Send" disabled>↑</button>`.
- The color: `spatial/app/style.css:678` — `#composer-send:not(:disabled) { background: rgba(var(--hue-l), 0.92); color: #0b0d16; }` (`--hue-l: 129, 212, 180`, trinity green, defined at style.css:7). A one-line change to that `background` is the whole demo diff.
- Ring: ratified table has `spatial/** → Ring 3` (verified live: the probe order for `spatial/app/style.css` classified ring 3) — worker may draft+test; apply stays AUMLOK. No ring-table change needed.
- Demo script (needs the seat flags on + restart, above): Auma `read_file spatial/app/style.css` (so her path label is honestly `verified`) → `propose_intent` (goal: change the enabled Send-button color to <choice>; affectedPaths: `spatial/app/style.css` verified) → `rehearse_intent <intentId>` → owner runs `bun scripts/rehearsalQueueRunner.ts --execute --max 1` → Auma `read_rehearsal_logs` reads her own evidence → Peter reviews the real diff and AUMLOK-signs. Nothing new is armed; every step is the existing pipeline.

**Convex shadow-write capture — inspected, reported, NOT built (per directive)**
- Today: the voice lane writes no memory anywhere (recall is read-only over local Kira `brain.json`); `/api/brain` truthfully reports `capture.wired: false`, pinned by `core/tests/convexReadOnlyInvariant.test.ts`.
- What already exists: the governed write path `core/src/memoryAppend.ts` → the ONE registered mutation `aumlokMemory:aumlokMemoryWrite` (manifest → subject PoP → one-shot grant → in-transaction decision token → V4-signed receipt → row insert), loopback-only, fail-closed, fully test-pinned — but with NO live transport (`deps.invoke` is injected by design).
- Smallest safe shadow-write brick = three pieces: (a) the admin-authenticated invoke wiring (kernelAdapter + key custody under `~/.aukora-symbiote/convex/`) — already planned as its own brick; (b) manifest + subject-PoP provisioning for a dedicated capture subject; (c) ONE env-gated (default OFF) best-effort call site that shadow-writes a bounded post-turn summary — recall untouched, `brain.json` stays canonical, a failed shadow write never touches the turn. Only then flip `capture.wired` truthfully.
- Honest sizing: (a)+(b) are the bulk — custody and PoP minting, not the call site. Recommend it stays its own round; R5b cutover gate (no recall move until Convex has fuzzy recall/search) stands.

**Action items**
- Peter: review/merge `zeb/rehearsal-queue-hygiene`; then restart the node with the seat flags and run the Send-button demo script above with Auma.
- Codex: bless (or rename) `REFUSED_INTENT_MISSING` and the `rehearsal-queue/archive/` location before more tooling depends on them.
- Zeb: AUMLOK keygen ceremony still pending (his hands, Git Bash) — this node remains unbound until then.

## 2026-07-07 — Fable (with Zeb) · zeb-windows-node · read-rehearsal-logs round

**Branches / PRs**
- `zeb/read-rehearsal-logs` — the evidence-read brick Codex specced in the PR #110 comment.

**Summary**
- New voice seat tool `read_rehearsal_logs` (spatial/voiceReadToolBridge.ts) — the feedback half of the inside-out loop, PURE READ. It reads only the bounded `rehearsal-result-summary-v1` files the runner already writes to `aumlok/rehearsal-results/`, and accepts latest (no args), `intentId` (bare 64-hex), or `orderId` (the writer's own filename charset) — a path/traversal string never reaches the filesystem.
- Returns advisory evidence only: status, order/intent ids, ring, proposal hash, ≤6 capped summary lines, timestamps, plus a ≤5-item sanitized recent index. The stored file is treated as UNTRUSTED input: fields are allow-listed and re-capped on read; unknown fields are never echoed — a tampered evidence file can only yield short inert strings.
- Same discipline as every seat tool: env opt-in `AUKORA_VOICE_READ_REHEARSAL_LOGS=1` (default OFF — landing arms nothing), refused under lockdown, flight-witnessed FAIL-CLOSED (unwitnessed read withholds its data), `advisoryOnly:true`/`grantsAuthority:false`, missing dir reads as an honestly empty stage. No rehearse, no sign, no apply, no keys, no prompts — the module still imports nothing from the signing/apply lane (structural test re-pinned).
- Capability preamble gains an honest `rehearsal evidence:` line (spatial/capabilityPreamble.ts), wired per-turn in voiceLane.ts like the other four flags.
- Verified on Windows: `tsc --noEmit` clean; new focused suite 10/10 green plus all adjacent voice-bridge suites via `bun test` (vitest workers don't run under bun on this node; one PRE-EXISTING symlink-EPERM failure in voiceReadToolBridge.test.ts fails identically on unmodified main — Windows needs admin rights for symlinks, unrelated); live probe against a scratchpad fake home: empty stage, latest/by-intent/by-order, traversal refusal, lockdown refusal, flight witness — all green.

**Action items**
- Peter: review/merge `zeb/read-rehearsal-logs`; the demo loop (draft → queue → rehearse → Auma reads the evidence herself) needs `AUKORA_VOICE_READ_REHEARSAL_LOGS=1` on the node that runs it.
- Codex: flag whether the recent-index (5 sanitized ids riding along each read) is welcome or should be its own list mode.
- Zeb: AUMLOK keygen ceremony still pending (his hands, Git Bash) — node remains unbound until then.

## 2026-07-07 — Fable (with Zeb) · zeb-windows-node · inside-out feedback loop round

**Branches / PRs**
- `zeb/inside-out-feedback-loop` — [open PR](https://github.com/aumara-xyz/aukora-symbiote/pull/new/zeb/inside-out-feedback-loop)

**Summary**
- Confirmed post-merge main on this node: `bun run start` green, Settings "Memory brain" card live, brain answering on :3210.
- New read-only `GET /api/loop` (spatial serve.ts): aggregates pending intents (`aumlok/pending-intents/`), rehearsal queue (`aumlok/rehearsal-queue/`), rehearsal evidence (`aumlok/rehearsal-results/`), and gate proposals (same view as `/api/aumlok`), with calm derived `whatHappened` / `nextStep` text. Bounded caps everywhere; missing dirs read as empty stages; no writes, no key bytes.
- Rehearsal evidence was NOT persisted before (runner printed to console only; the `receipts/rehearsal-*.json` files are signing-ceremony receipts, a different thing). Per directive item 5 the runner now writes one bounded advisory summary per executed rehearsal: `rehearsal-result-summary-v1` — order/intent ids, ring, command, terminal status, proposal hash if produced, ≤6 capped console lines, `advisoryOnly:true`/`grantsAuthority:false`. Best-effort: a failed evidence write never blocks the rehearsal. No prompts, no chain-of-thought, no secrets.
- AUMLOK screen: "The loop right now" panel between the trust rail and the gate — the four stages plus what-happened/next-step, observer-only.
- Verified on Windows: endpoint exercised against a scratchpad fake home (1 intent + 1 order + 1 result fixture → correct aggregation + narrative) AND the real home (quiet loop, honest zeros; pure reads). Runner dry-run still green. No signing, no applying, no AUMLOK changes.
- Follow-up commit: second Fable thread independently re-verified the whole surface (typecheck, empty + fixture probes, POST rejected 405) and fixed one narration nit — `short()` no longer appends `…` to ids that were not actually truncated. Coordination note: two parallel threads were launched into this same worktree this round; no conflict resulted, but future rounds should hand the lane to exactly one thread.

**Action items**
- Peter: review/merge `zeb/inside-out-feedback-loop`.
- Anyone rehearsing: results only appear in Spatial for rehearsals run through `scripts/rehearsalQueueRunner.ts --execute` after this lands (direct workbench runs don't write evidence yet — flag if that lane should too).
- Zeb: AUMLOK keygen ceremony still pending (his hands, Git Bash) — node remains unbound until then.

## 2026-07-07 — Codex (with Peter) · next round after PRs #107/#108/#109 merged

**Branches / PRs**
- Merged on `main`: #107 Windows first-run hardening, #108 Convex memory truth card, #109 mesh inbox + AUMLOK app ceremony spec.
- Next branch for Zeb/Fable: `zeb/inside-out-feedback-loop`.

**Summary**
- Peter merged the first three Zeb/Fable PRs. The mesh inbox is ready for short round reports instead of copy-paste.
- The next useful brick is NOT more authority. It is the missing feedback surface: Auma/Peter should be able to see pending intents, rehearsal queue entries, rehearsal evidence/results, and pending proposals inside Spatial.
- This supports the near-term demo: Peter asks Auma for a tiny UI change, Auma drafts it, the workbench rehearses it, Spatial shows what happened, Peter approves through AUMLOK, then Auma can read the result back.

**Action items**
1. Zeb/Fable: pull latest `main`, run `bun run start`, and confirm the Settings "Memory brain" card appears.
2. Zeb: if ready, run the local AUMLOK keygen ceremony in Git Bash:
   `bash scripts/aumlok-authority.sh keygen`
   Do not paste or screenshot any private key material. This key authorizes only Zeb's local node.
3. Zeb/Fable: create branch `zeb/inside-out-feedback-loop`.
4. Build the smallest read-only feedback surface in Spatial:
   - pending intents;
   - rehearsal queue items;
   - latest rehearsal result/evidence if available;
   - pending proposals ready for AUMLOK;
   - clear "what happened / what to do next" text.
5. If rehearsal evidence is not currently persisted, add a bounded advisory summary file/record after workbench runs: intent id, order id, status, files changed, test summary, proposal hash if produced, `advisoryOnly:true`, `grantsAuthority:false`. Do not store secrets, raw prompts, chain-of-thought, keys, or huge logs.
6. No signing, applying, or AUMLOK bypass in this branch. Approval stays with the existing AUMLOK path.
7. Push PR and add the next short report here at the top.

## 2026-07-07 — Fable (with Zeb) · zeb-windows-node

**Branches / PRs**
- `zeb/windows-first-run` — commit `551b409` — [open PR](https://github.com/aumara-xyz/aukora-symbiote/pull/new/zeb/windows-first-run)
- `zeb/convex-memory-truth` — commit `7d9db84` — [open PR](https://github.com/aumara-xyz/aukora-symbiote/pull/new/zeb/convex-memory-truth)
- `zeb/mesh-inbox` — this file + `docs/AUMLOK_APP_CEREMONY_SPEC.md`

**Summary**
- First run on Windows 11 verified end to end: `bun run start` → Spatial at :7090, chat door
  :7091, Convex brain :3210 (loopback, binary pinned `precompiled-2026-06-09-b6aaa1a`).
- `zeb/windows-first-run`: repo-wide LF `.gitattributes` (CRLF was breaking every bash script
  AND the gate byte-pin — the pin file itself arrived CRLF), working `bun run stop` on Windows,
  `renameSync` instead of `mv` in brain-setup, README Git Bash note. After it,
  `bash scripts/status.sh` reports HEADLESS_READY with gate integrity VERIFIED on Windows.
- `zeb/convex-memory-truth`: read-only `GET /api/brain` + a "Memory brain" Settings card that
  states what is true: brain running (probed live), conversation capture **not wired**
  (nothing calls `memoryAppend` yet), recall serving from the **local Kira JSON brain**
  (R5b cutover pending), AUMLOK key present/absent with the exact keygen command when unbound.
- AUMLOK on this node: **unbound** (no local key yet — keygen is Zeb's ceremony to run, not
  Fable's). App-ceremony direction drafted in `docs/AUMLOK_APP_CEREMONY_SPEC.md`.

**Action items**
- Peter: review/merge the two PRs (windows-first-run first; the truth surface is independent).
- Peter: two owner-level decisions flagged in the spec — (1) the Windows custody story for the
  `(mode & 0o077)` checks (NTFS always reports 0666, so approve/sign-side custody checks
  refuse on Windows as written); (2) whether a recovery phrase may ever be displayed in-app.
- Zeb: run the AUMLOK keygen ceremony in Git Bash when ready (exact steps in the round report
  and in Settings once the truth PR merges).
- Next code round proposal: R5b recall brick per `docs/R5_RECALL_STATUS.md` (search index +
  recall-by-query + flagged lane cutover with JSON fallback).
