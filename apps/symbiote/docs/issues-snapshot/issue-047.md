# #47 PDF ingestion v2 spike: OpenRouter file content-part probe

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: OPEN
- Labels: lane:spatial-chat, needs:owner-approval, priority:parked
- Created: 2026-07-02T10:46:43Z
- Updated: 2026-07-02T21:39:34Z
- Closed: not closed
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/47

## Body

Parked pending #38 v1 landing and proving insufficient (refs #38 — the interim fix is an honest per-attachment note telling the owner to convert to .txt/.md or use vision-model screenshots instead of real PDF extraction).

## The spike
A small (~10 line) probe: does OpenRouter's file content-part (base64-encoded PDF passed directly in the request body, per their multimodal file-input shape) work end-to-end against an `anthropic/*` model on this account? If yes, real PDF support may be a request-body change in spatial/voiceLane.ts's `voiceReply`, not a text-extraction library integration. If no (or the account/tier doesn't support it), extraction (e.g. a PDF-to-text step before inlining) is the fallback path.

## Why parked, not built now
#38 v1 ships an honest limitation note instead of guessing at PDF support. This spike only matters once that's proven insufficient — i.e., once a real PDF attachment need comes up that the interim workaround (convert to .txt/.md, or screenshot pages to a vision model) doesn't cover well enough.

## Scope note
Real spend to run the probe (a live OpenRouter call) — same standing rule as any other live model invocation this project: ask the owner first, small as the cost likely is.

## Comments

_No comments._
