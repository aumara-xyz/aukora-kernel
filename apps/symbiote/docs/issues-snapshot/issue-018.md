# #18 Wire wombMemory captures to megaMindBrain's real hash-chained receipt log

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: CLOSED
- Labels: none
- Created: 2026-07-01T15:09:24Z
- Updated: 2026-07-01T15:09:35Z
- Closed: 2026-07-01T15:09:35Z
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/18

## Body

Source: same relayed kira-chat message as issues #16/#17, verified independently first. Confirmed real: `wombMemory.ts` (527 lines, real production code) does not call `megaMindBrain.ts`'s `ingestMemory()`/receipt chain at all — the two are parallel, disconnected infrastructure. (Note: this is a fresh issue created to correct a numbering mistake — the actual work was already committed and I initially mis-referenced GitHub issue #16, which is a different, already-closed issue about `risk.ts`. See the correction comment on #16.)

**What was built** (commits 6dfa07e, a348adc — already pushed before this issue existed, hence closing immediately with evidence rather than tracking as open work):

1. **Test coverage first**: `wombMemory.ts` had ZERO running test coverage — its only test file was entirely quarantined in `deferred-tests/` because one describe block (2 of 57 tests) needs the private `evidence/run-auma-womb-prompt` script. Ported the other 55 tests into `core/tests/wombMemory.test.ts` (commit 6dfa07e). The 2 left behind are tracked under issue #17 (needs a real host-harness, not a quick fix).

2. **The actual wiring**: new module `core/src/wombMemoryReceiptChain.ts`, one entry point `captureWithReceipt()` that calls both `WombMemoryStore.add()` (unchanged, zero regression risk) and `megaMindBrain.ingestMemory()` (real hash-chained receipt) for the same content. Neither existing module was modified — purely additive.

**Proven, not just asserted** (per the kira-chat's own ask: "prove it: capture something, show the actual receipt (sequence, previousHash, id), run verifyBrainState()"):
- A real capture produces `sequence:0, previousHash:'genesis'`, a real minted `id`.
- A second capture's `previousHash` chains to the first receipt's own `id` — a genuine hash chain, not independent entries.
- `verifyBrainState()` returns `ok:true, errors:[]` throughout.
- Kind-mapping (womb's 8-kind vocabulary onto megaMindBrain's 4: `receipt`→`receipt`, `code`→`code_map`, `safety`/`preference`→`note`, rest→`experience`) verified explicitly.
- `advisoryOnly:true`/`grantsAuthority:false` verified at every layer: womb record, atom, receipt, and the returned brain state.

Full suite at time of commit: typecheck clean, 124/124 files, 1745/1745 tests green.

## Comments

### Comment 1 - aumara-xyz - 2026-07-01T15:09:34Z

Closing immediately — this work was already completed and pushed (commits 6dfa07e, a348adc) before this issue was created to fix a numbering mistake. See the issue body for full evidence.
