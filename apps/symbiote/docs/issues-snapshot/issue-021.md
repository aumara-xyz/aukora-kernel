# #21 Round 1: Fusion evidence truncation + Kira brain migration + gate honesty fixes

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: CLOSED
- Labels: none
- Created: 2026-07-02T00:49:50Z
- Updated: 2026-07-02T01:09:53Z
- Closed: 2026-07-02T01:02:31Z
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/21

## Body

Fable's convergence review (2026-07-02, fresh-clone-verified, 6-agent swarm) identified this as the single highest-leverage round: it unblocks Round 2 (wiring the native agent for a real demo).

Scope (exact, per Fable's directive + Codex's addendum — do not drift into Round 2/3 work):

1. **Fusion is blind**: `core/src/aukoraFuEngine.ts:319` truncates the evidence pack to `problem.slice(0, 400)` — council models never see actual proposal file content. Fix: send the full pack (or an engine-config cap >= 12000 chars). Required regression test: mock fetch, run `reviewSelfEditProposal` on a proposal containing a known sentinel string in file content, assert the sentinel appears in the outgoing request body. Must fail on old code.

2. **Kira is amnesiac (rename regression)**: the persisted brain (`state/mega-mind/brain.json`, 75 atoms / 75 receipts, schema `AUKORA_MEGA_MIND_BRAIN_V1`) is orphaned — all consumers now read `state/kira/brain.json` and `kiraBrain.ts:386` hard-rejects the old schema, while `loadBrainState` (`kiraBrain.ts:202`) silently returns an EMPTY brain. Build a one-time migration: load old file, set `schema='AUKORA_KIRA_BRAIN_V1'`, save to `state/kira/brain.json`, assert `verifyBrainState` => `ok:true` with `receiptCount:75`/`atomCount:75`, archive the old directory. Add a guard so "legacy path exists + new path empty" warns loudly instead of silently starting a new genesis chain. Test the migration with a fixture.

3. **Missing-key fail-fast**: if `resolveApiKey()` is null, `reviewSelfEditProposal` currently fires 5 real HTTP requests with an empty Bearer header. Short-circuit to a skipped/NO_QUORUM summary with reason `missing_key`. Test it.

4. **Brittle tests**: `core/tests/proprioceptionSnapshot.test.ts:10` and `core/tests/runtimeTruthManifestSeed.test.ts:23` assert the checkout dir is named `aukora-symbiote` (fails on any other clone dir name). Detect the seed root by marker files (e.g. `core/package.json` + `scripts/test.sh`) instead.

**Completion protocol**: every change committed with `fixes #<this issue>`, `scripts/test.sh` green, push, and this issue gets: (a) the failing-then-passing truncation test output, (b) `kira.sh status` showing 75 atoms post-migration. No markdown-only claims.

**Explicitly out of scope this round**: wiring `runNativeAgent`, deleting `self_edit/`, website/dashboard framing fixes, governance hardening, vision, issues #9/#14/#17.

## Comments

### Comment 1 - aumara-xyz - 2026-07-02T01:03:57Z

Round 1 done, pushed as `313c101`. Evidence per the completion protocol:

**(a) Fusion truncation — fail-before/pass-after, real output:**

With the old `problem.slice(0, 400)` temporarily restored:
```
FAIL  tests/selfEditReviewCouncil.test.ts > selfEditReviewCouncil: Fusion sees the real evidence pack, not a 400-char preamble (issue #21) > a sentinel string placed well past the old 400-char cutoff reaches the outgoing model request body
AssertionError: expected '{"model":"test/a","messages":[{"role"…' to contain 'SENTINEL_9f3a7c_ISSUE21_REGRESSION_MA…'
```
With the fix (`problem.slice(0, MAX_PROBLEM_CHARS)`, 12,000 chars matching the caller's own bound):
```
✓ tests/selfEditReviewCouncil.test.ts  (26 tests) 44ms
```

**(b) Kira migration — real local brain, `kira.sh status` post-migration:**
```json
{
  "statePath": "[REDACTED:local-user-path]",
  "summary": "Kira Brain: 75 atoms, 75 receipts. verification=green. Three perceivers: lexical + glyph/trigram + topology; recall returns cited advisory evidence only. grantsAuthority=false.",
  "verify": { "ok": true, "errors": [], "receiptCount": 75, "atomCount": 75, "advisoryOnly": true, "grantsAuthority": false }
}
```
Old dir archived to `state/mega-mind.archived/` (not deleted). Migration itself is also covered by `core/tests/kiraCli.test.ts` (6 tests, real fixture brains, never the dev machine's real state) — including a test that the CLI *refuses* to run `status`/`ingest`/`recall`/`init` and silently start a new genesis chain while an unmigrated legacy brain exists.

**Missing-key fail-fast** and the **2 brittle dir-name tests**: also done, tested. The dir-name fix was verified by cloning into `/tmp/round1-verify-any-dir-name-xyz123` (deliberately not named `aukora-symbiote`) — full fresh clone, 126 files / 1768 tests green, typecheck clean.

Full local + fresh-clone suite: 126 files / 1768 tests, `tsc` exit 0.

Not touched, as scoped: `runNativeAgent` wiring, `self_edit/` deletion, website/dashboard framing, governance hardening, vision, #9/#14/#17.

Ready for Codex audit.

### Comment 2 - aumara-xyz - 2026-07-02T01:09:53Z

Self-adversarial audit (standing in for Codex, since no live session is available to relay a real one right now) ran against 313c101. Verdict: 3 of 4 fixes clean, one real gap found and fixed.

**Found:** \`kiraCli.ts\`'s legacy-brain guard resolves the legacy path via \`AUKORA_KIRA_LEGACY_STATE\` (env override first), but \`scripts/kiraLoopbackCli.ts\`'s copy of the same guard hardcoded the default path directly — the two could silently disagree about which path is "the legacy brain."

**Fixed** in `2ab2f94`: extracted the same env-var-first resolution into `legacyStatePath()` in both files. Added `core/tests/kiraLoopbackCli.test.ts` (2 tests, real spawned script, throwaway fixture paths) — confirmed fail-before (divergent version silently boots serving an empty brain)/pass-after (fixed version correctly refuses), same discipline as the other Round 1 regression tests.

Everything else the audit checked held up: no second truncation point in `aukoraFuEngine.ts`, migration is idempotent/safe on re-run, missing-key guard can't be bypassed by any falsy value, brittle-test fix is a meaningful marker-file check not a tautology.

Full suite: 127 files / 1770 tests, typecheck clean. Round 1 is done — moving to Round 2 (wiring the native agent for the minimum demo).
