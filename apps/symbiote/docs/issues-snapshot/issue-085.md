# #85 Future brick: Convex/reactive brain event ledger and governed mutation gateway

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: OPEN
- Labels: lane:convex, lane:persistence, lane:safety-rail, priority:parked
- Created: 2026-07-03T00:42:16Z
- Updated: 2026-07-03T06:21:54Z
- Closed: not closed
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/85

## Body

Captured from third-party static architecture/red-team review of the latest GitHub snapshot. Future direction, not current build.

Observation:
The repo has local Kira memory, Convex schema/mirror/loopback shape, and read-only reactive-brain posture. It does not yet have the full live Convex reactive mutation brain.

Future goal:
Build Convex/reactive persistence around an append-only governed event ledger, not only tables/views. Derived views such as Kira atoms, proposals, tests, reviews, applies, rollbacks, capabilities, revocations should be reconstructable from events.

Principles:
- evidence store is not authority
- mutation gateway is governed by the same policy kernel and AUMLOK path
- no backend-specific authority path; backend choice is storage/hosting only
- event ledger records failures and rollbacks as first-class events

Acceptance sketch for future build:
- append-only event schema
- deterministic projection tests for Kira/proposal/test/review/apply views
- governed mutation gateway with policy decision hash
- local loopback and hosted Convex behavior match on authority semantics
- no direct client write path bypasses the gateway.

## Comments

### Comment 1 - aumara-xyz - 2026-07-03T05:40:49Z

**Canonical typed-event vocabulary + convergence principles (planning — status: parked, future brick).**

The append-only governed event ledger (this issue) is the reactive CONTAINER; AUMLOK remains authority; the evidence store is not authority (already stated). Recording the canonical event vocabulary for the inward-recursion loop so projections stay deterministic and honest:

- `turn.received` — an owner turn arrived (typed envelope, #53).
- `fusion.reviewed` — the council returned an advisory verdict (#34).
- `glyph.measured` — a GML measurement was taken (#89) — advisory, plain-English-mirrored.
- `proposal.drafted` — a governed proposal artifact was produced (#35).
- `aumlok.signed` — the owner signed (authority event; the only authorizing one).
- `apply.receipted` — a signed apply produced a receipt (success OR rollback, both first-class).
- `kira.remembered` — a receipt-bound advisory atom was written (#79).

Convergence principles (Auma's law):
- **GML packets are the ground-truth-readable record** — every measurement losslessly renders to plain English at point of use.
- **HRR trace (#87) is a regenerable, lossy index — never the record.**
- **Visual projection is a lossy display, never judgment** — no authority decision reads from a projection.
- A receipt of unreadable model-to-model communication is not meaningful evidence.

Still parked; do not implement.

### Comment 2 - aumara-xyz - 2026-07-03T06:21:54Z

Shadow-node vocabulary note from #90: when the governed event ledger is built, reserve explicit event classes for shadow lifecycle events: shadow.exported, shadow.spawned, shadow.synced, shadow.killed, and shadow.rejected. These events record facts about the shadow lane; they do not grant authority. A memory export leaving the local node should be receipted on the canonical live ledger before any cloud upload.
