# #30 Chat door: agent:/run: default model stops before propose_patch (repro + model knob)

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: OPEN
- Labels: lane:spatial-chat, needs:owner-approval
- Created: 2026-07-02T05:10:05Z
- Updated: 2026-07-02T10:32:03Z
- Closed: not closed
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/30

## Body

Two live `agent:` runs through the spatial chat door (spatial/chat-serve.ts → workbenchCommandLoop → runNativeAgent, default model moonshotai/kimi-k2.7-code) on 2026-07-02:

Run A — goal: add a note line to spatial/README.md
- round 1: read_file → REFUSED (contract validation: secret-shaped value(s) in output at: content) — the firewall pattern-matched README prose about key files; fail-closed as designed, but a false-positive worth a look
- rounds 2-3: list_files, search → ok
- result: stoppedReason=no_tool_calls after 4 rounds, no proposal (honest halt)

Run B — goal: add a clarifying comment in core/src/skillLibrary.ts
- rounds 1-3: read_file ok, search ok, search ok
- result: stoppedReason=no_tool_calls after 4 rounds, no proposal

The plumbing is proven end-to-end (chat → door → loop → model → membrane tools → honest halt), and the propose lane itself works (2 pending proposals from earlier runs exist). The default model is just not reliably emitting the final propose_patch tool call.

Suggested: try AUKORA_AGENT_MODEL overrides (env is already wired), and/or a nudge in the agent system prompt when rounds remain but no proposal has been drafted. Also consider whether the secret-shape read_file false positive (Run A) deserves a docs-prose exemption path.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Comments

### Comment 1 - aumara-xyz - 2026-07-02T08:43:57Z

Code fix pushed as \`3826dc5\`. Built directly against this issue's own repro evidence — both real live runs halted via \`no_tool_calls\` at round 4 of 8, well before a "rounds running low" nudge (fired at round 6) would ever trigger. Implemented two nudges:

1. **No-tool-calls recovery** (the one that actually matches the repro) — if the model returns plain-text prose instead of a tool call, give it exactly one nudge-and-retry before treating it as a genuine, honest halt.
2. **Rounds-running-low** (the originally-suggested mechanism) — kept as a second safety net for a different failure mode (a model that keeps exploring indefinitely, never proposing). Fires once, at round maxRounds-2.

Both proven fail-before/pass-after (independently disabled each nudge, confirmed the corresponding new test fails, restored). Full suite green (130 files/1831 tests), fresh-clone verified, \`status.sh\` \`HEADLESS_READY\`, real Kira brain untouched at 75/75.

**Not closing yet.** Per this issue's own close criterion, the actual proof is one live \`agent:\` run through the chat door reaching \`propose_patch\` — that's real spend and needs an explicit owner decision before I run it. Code + regression tests are the honest state right now; live confirmation is pending.

## Default-model recommendation (for the owner to decide, not changing unilaterally)
\`moonshotai/kimi-k2.7-code\` halted twice in real live runs (this issue) but succeeded in the Round 2 demo transcript — inconsistent, not reliably broken. My recommendation: **keep it as the default and let the nudge fix above do its job first** — it directly targets the exact failure mode observed, at low implementation risk, and \`AUKORA_AGENT_MODEL\` already lets anyone override it per-run without a code change. If the next live run still stalls even after being nudged, that's real evidence to revisit the default; right now there isn't evidence the MODEL itself (versus the missing-nudge behavior) was ever the actual problem.
