# #64 Auma 32B local-model lane: commit the Nebius/burned-VL route (owner WIP)

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: CLOSED
- Labels: none
- Created: 2026-07-02T22:32:24Z
- Updated: 2026-07-02T22:33:08Z
- Closed: 2026-07-02T22:33:07Z
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/64

## Body

## What this is
The owner's in-flight WIP in `spatial/voiceLane.ts` + `spatial/app/chat.js` (uncommitted since this session started, predating the recent Stage-0 rounds): a route for a locally-served **Auma 32B** (base Qwen2.5-VL-32B + an architecture-grounded burned VL LoRA) on a Nebius vLLM endpoint, selected as roster id `auma-vl-v3`, routed to its own endpoint (not OpenRouter). This is the "Auma gets her own body" lane — matches the local-model roadmap and #49's Nebius language.

## Provenance
Owner-vouched (WIP predates today's rounds) + full-diff-read confirmed as coherent, intentional, house-style work (config read fresh per call so the endpoint is repointable without restart; absent config → an honest "not live yet" message, never a fake reply; the #57 anchor + #40a self-knowledge conventions preserved, with the self-knowledge line correctly switching to local-endpoint wording only on the Auma route).

## Fixes required before commit (per QA)
1. **Path:** the endpoint config currently reads `~/.aukora/auma_vl_endpoint.json` — the legacy-path bleed purged in Round 4. Add `aumaVlEndpointPath()` to `authority/symbiotePaths.ts` (`AUMA_VL_ENDPOINT_FILE` env ?? `symbioteHome()/auma_vl_endpoint.json`). (symbiotePaths.ts is not gate-pinned — no re-pin.)
2. **Secrets:** the key lives only in the untracked home-dir config; nothing tracked carries it and the code never echoes it. Add a test asserting the key never appears in any returned ChatEntry or the outgoing self-knowledge line; confirm the gate's `.aukora(-symbiote)` sensitive-path rule covers the new filename (dir-level).
3. **Style + tests:** replace the inline `require('fs')` with top-level imports; add tests — absent config → honest not-live on the Auma route / unchanged null on OpenRouter routes; Auma route sends `cfg.url`/`cfg.key`/`cfg.model` in the request; non-Auma route untouched; self-knowledge wording per route; roster live-enrichment preserves the static `auma-vl-v3` entry (it's not on OpenRouter, must not be dropped).

## Boundary
The route existing is in scope; the **endpoint itself going live is owner infrastructure + spend**, out of scope — the code sits ready with an honest "not live yet" until the config file appears.

## Status
Committing this means committing the owner's uncommitted working-tree files, so it is gated on an explicit one-line owner go ("commit my Auma-32B lane"). Held until then; #62 part 2 landed independently first so the envelope round starts from a base where only this WIP remains.

## Comments

### Comment 1 - aumara-xyz - 2026-07-02T22:33:07Z

Duplicate of #63 (which already tracks this exact dirty-tree Auma-32B lane). Consolidating onto #63; closing this.
