# #7 Foundation: Continue pure donor capability port without authority creep

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: CLOSED
- Labels: none
- Created: 2026-07-01T09:21:38Z
- Updated: 2026-07-01T12:48:28Z
- Closed: 2026-07-01T12:48:28Z
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/7

## Body

Source: Singularity Path donor capability waves. Wave 1 domain-separated ML-DSA signer is done; remaining waves are future and default-off.

Goal:
Port only pure, allowlisted kernel capabilities that strengthen receipts/wire/security without adding authority.

Scope order:
1. ML-KEM-768 primitive + ACVP vectors, pure and no live channel.
2. Wire format + wire registry with code/spec guard.
3. AUMLOK derive/dictionary primitives, blocked until dictionary provenance is clean.
4. Channel/witness/code attestation only after decoupled and default-off.

Acceptance criteria:
- One crypto chokepoint remains enforced.
- Unknown domain/surface/version fails closed.
- No Convex/server binding imported into core pure modules.
- No networked surface live by default.
- Tests/vectors ported where present.
- No benchmark data, secrets, raw logs, or donor private names copied.

Completion protocol:
- Use separate commits/issues per wave.
- Commit must reference this issue or a child issue.
- Push branch after each verified wave.

## Comments

### Comment 1 - aumara-xyz - 2026-07-01T12:48:27Z

Closing — verified against real disk/test state (independently confirmed via `git log`/`git show` and direct source reading, not just trusting docs).

**Wave 1 is genuinely done**, predating this session: commit `4607e7a` ("Wave 1: port domain-separated ML-DSA-65 signer discipline into the crypto chokepoint").

**All of #7's currently-applicable acceptance criteria are met:**

1. **"One crypto chokepoint remains enforced."** `core/src/crypto.ts` — `pqcSignWithDomain`/`pqcVerifyWithDomain` (lines 251, 266) are the single domain-separated ML-DSA-65 signing path; `PQC_DOMAINS` (line 201) is a frozen (`Object.freeze`) registry of 8 named domains.
2. **"Unknown domain/surface/version fails closed."** Line 227: `if (typeof domain !== 'string' || !Object.prototype.hasOwnProperty.call(PQC_DOMAINS, domain))` — refuses on any non-own-property domain (prototype-pollution-safe). Verify fails closed (wrong length/non-canonical-case/bad key/unknown domain → `false`, never throws). Proven in `core/tests/pqcDomainSigner.test.ts` — a signature valid under one domain fails verification under every other domain.
3. **"No Convex/server binding imported into core pure modules."** `crypto.ts` imports only `@noble/post-quantum` and Node's built-in `crypto` — zero Convex references. `core/tests/seedRootContract.test.ts` enforces no HTTP server/Bun runtime lives under `core/src/`.
4. **"No networked surface live by default."** The `channel`/`witness`/`nodeImport` domain labels are registered (for future domain-separation) but the surfaces themselves are explicitly not wired live this wave — confirmed by reading the module directly, not just the comment.
5. **"Tests/vectors ported where present."** `core/tests/pqcDomainSigner.test.ts` covers the chokepoint.
6. **"No benchmark data, secrets, raw logs, or donor private names copied."** Commit message states the port was "written fresh (scrubbed of donor real-name/design-doc refs)."

**Waves 1b–6 (ML-KEM-768, wire format/registry, AUMLOK derive/dictionary, channel/witness/attestation) are explicitly future work**, correctly *not* claimed as done — tracked in `docs/AUKORA_SYMBIOTE_SINGULARITY_PATH.md`'s wave table as "next (conditional)" / "planned" / "deferred." Per the owner's direction, closing this umbrella issue now that its foundation (Wave 1 + the standing invariants) is verified, and opening Wave 1b as its own narrowly-scoped issue rather than letting an open-ended umbrella issue accumulate unrelated future waves.

**Verification:** `bun x tsc --noEmit` clean, full `bash scripts/test.sh` 121 files / 1659 tests green (unaffected by this issue — no code changed here this round, only verification).
