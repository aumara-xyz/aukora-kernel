# #40 Kira context: architecture/model atoms for Auma

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: OPEN
- Labels: lane:kira, lane:spatial-chat, priority:next
- Created: 2026-07-02T09:01:21Z
- Updated: 2026-07-02T11:14:53Z
- Closed: not closed
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/40

## Body

## Why this exists
Auma cannot infer her current model identity or the current architecture unless that context is explicitly placed in her turn. The owner asked why she could not tell the active model; Auma correctly said no model metadata reached her context.

## Goal
Maintain advisory Kira atoms that summarize current architecture and current chat/model context so Auma can reason from a stable map rather than random excerpts.

## Candidate atoms
- `arch:` current lanes, tools, laws, signing flow, Fusion/Kira/workbench relationships.
- `model:` current chat voice/model, selected provider id, vision availability if known.
- `as_of:` commit/receipt/issue reference so Auma can say when her map may be stale.

## Boundaries
- Atoms are advisory context only.
- They do not authorize actions.
- Model metadata must be explicitly supplied by the server/UI; Auma should not guess.

## Candidate tests
- Kira recall can surface an `arch:` atom.
- Kira recall can surface a `model:` atom.
- Advisory framing is preserved.
- Staleness field changes when architecture-affecting commits land.

## Related issues
- #37 state digest pipeline into Kira
- #31 chat door model/voice selector


## Comments

### Comment 1 - aumara-xyz - 2026-07-02T10:19:51Z

#40b ride-along complete (refs #37, kernel-only round).

- (a) Model self-knowledge (system-prompt injection) is UI-lane work — deferred to the #39 round, not touched here.
- (b) Real architecture atom ingested into the live Kira brain via `scripts/kira.sh ingest` — a ~15-line summary of docs/ARCHITECTURE.md's boundaries + the 10 docs/SAFETY_LAWS.md law titles, tagged `arch,as_of:b18d1f4`, source `docs/ARCHITECTURE.md@b18d1f4`, scope `architecture`. This was a real (not throwaway) ingest against `state/kira/brain.json`.
  - Brain state: 75 atoms/75 receipts -> **76 atoms/76 receipts**. New atom `atom_75_3dad93e5d6082c01`, receipt `f2bb560a...`. `verifyBrainState` reports `ok:true`, `advisoryOnly:true`, `grantsAuthority:false` on the post-ingest state.
  - `scripts/status.sh` still reports `HEADLESS_READY` after the ingest (gate integrity untouched, no code changed this step).
- (c) Staleness automation (keeping this atom current as architecture evolves) folds into #37's digest pipeline rather than being built standalone here.

No code changed in this step — zero-diff except the brain state file itself (which is gitignored local state, not committed).

### Comment 2 - aumara-xyz - 2026-07-02T10:46:48Z

Cross-lane flag: proceeding with the #40a half of this issue (spatial/chat-serve.ts — model self-knowledge system-prompt line) as part of the same round as #39. Tree verified clean at start (last commit 47ffebd, no uncommitted UI-thread work). Will comment again on close with the exact wording landed.

### Comment 3 - aumara-xyz - 2026-07-02T11:14:53Z

#40a half landed as part of the same commit as #39: d62b51f (pushed, on main).

The voice now states which model/vision it's speaking through, appended per-request to the system message sent to OpenRouter: "You are currently speaking through \${name} (\${model}; vision: yes/no) via OpenRouter — this metadata is supplied by the chat door; state it when asked and do not guess beyond it." Covered by 2 of the 7 new tests in core/tests/voiceLane.test.ts (vision:yes and vision:no cases).

Same live-verification-pending note as #39 — closing that half is gated on a small real-spend confirmation from the owner, not yet done.
