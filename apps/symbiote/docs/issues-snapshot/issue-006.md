# #6 Foundation: Make Symbiote Convex read-only loopback real and host-agnostic

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: CLOSED
- Labels: none
- Created: 2026-07-01T09:21:22Z
- Updated: 2026-07-01T12:44:36Z
- Closed: 2026-07-01T12:44:36Z
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/6

## Body

Source: Singularity Path M5 / Convex cleanup. Verified current code has convexBrainReadonly.ts with DEFAULT_CONFIG http://127.0.0.1:3220 and allowlist aukoraReceipts:getReceiptChainHeadPublic.

Goal:
Make Symbiote own a minimal local read-only Convex loopback backend or explicit unavailable state. No sibling host coupling.

Acceptance criteria:
- Minimal local schema/function for aukoraReceipts:getReceiptChainHeadPublic exists under this repo if backend is built.
- readLoopbackSurface succeeds against Symbiote-owned loopback or reports honest unavailable.
- No Convex mutation path introduced.
- convexBrainSnapshot/convexTopology remain host-agnostic and public-safe.
- Tests prove read-only invariant and no cloud URL/non-loopback URL is accepted.

Completion protocol:
- Commit must reference this issue.
- Push branch after completion.
- Do not close until local tests and status pass.

## Comments

### Comment 1 - aumara-xyz - 2026-07-01T12:44:35Z

Closing with evidence — verified against real disk/test state via an independent adversarial review (3 parallel lenses: test-port fidelity, criterion-by-criterion check, and safety review of the new allowlist entries), not just my own read.

**Commit:** 2ad59bc (headless port of the pure-logic subset of `deferred-tests/host-coupled/convexBrainReadonly.test.ts` into `core/tests/convexBrainReadonly.test.ts`).

**Criterion-by-criterion:**

1. **"Minimal local schema/function for aukoraReceipts:getReceiptChainHeadPublic exists under this repo if backend is built."** No real Convex backend is built or deployed anywhere in this repo — `convex/schema.ts` is a documented placeholder (`MEGA_MIND_CONVEX_TABLES` constant + a comment saying the live deployment isn't wired), and `convex/README.md` states the real implementation is `core/src/megaMindBrain.ts` instead. A `convex-local-backend` process does run on port 3210, but it's a sibling project (`~/aukora-convex-backend`, unrelated cwd) — `core/src/convexTopology.ts` itself classifies that port as `wrong_backend`/"Not the womb brain." Nothing listens on this repo's own loopback target (3220). Since no backend is built in-repo, this criterion is satisfied vacuously and honestly by the issue's own "if backend is built" framing — not silently skipped.

2. **"readLoopbackSurface succeeds against Symbiote-owned loopback or reports honest unavailable."** `readWithFallback` (`core/src/convexBrainReadonly.ts:193-233`) catches any failure and, with no static snapshot, returns `{source:'unavailable', bridgeMode:'missing', data:null, fallbackReason:...}` rather than throwing or fabricating data. Proven by `core/tests/convexBrainReadonly.test.ts` — "returns unavailable when no snapshot and loopback fails."

3. **"No Convex mutation path introduced."** Repo-wide grep for `.mutation(`/`.insert(`/`.patch(`/`db.insert|patch|delete` across `core/src`, `convex/`, `receiver/`, `self_edit/`, `memory/`, `authority/` returns zero real hits. `core/tests/convexReadOnlyInvariant.test.ts` is a dedicated law-scanner enforcing this repo-wide, plus asserting `memory_write` stays quarantined (`self_edit/opencode/tool/memory.ts`). Both pass.

4. **"convexBrainSnapshot/convexTopology remain host-agnostic and public-safe."** `buildConvexBrainSnapshot()` scans no host tree — returns a fixed "no live brain mounted" snapshot regardless of input, and `validateSnapshot()` actively rejects forbidden fields (apiKey/privateKey/seed/deploymentUrl/cloud URLs) at every nesting level. `convexTopology.ts` contains only loopback URLs; `rejectNonLoopback` gates every probe.

5. **"Tests prove read-only invariant and no cloud URL/non-loopback URL is accepted."** `core/tests/convexBrainReadonly.test.ts` (new, headless, 75/75 green) covers URL validation (accepts 127.0.0.1/localhost/[::1], rejects .convex.cloud/.dev/.site, LAN/10.x IPs, credentialed URLs), query allowlist + mutation-verb rejection, scrubPayload DoS guard, envelope unwrapping, and the honest-unavailable fallback path — together with `convexReadOnlyInvariant.test.ts`.

**Test-port fidelity** (independently verified): 74 of 93 original tests carried with byte-identical assertions; the 19 dropped are each genuinely host-coupled (13 need private `evidence/` scripts, 3 need a private donor file `node-template/convex/aukoraReceipts.ts`, 1 depends on host-specific donor-repo inventory this repo doesn't populate, 1 is a verbatim duplicate kept elsewhere in the port).

**Flagged separately, not folded into this closure:** the same uncommitted concurrent change that motivated re-checking this file's allowlist also added `megaMind:recall`, whose response includes verbatim (280-char-bounded) excerpts of ingested repo content — broader than the allowlist's own "public metadata only" bar. That's real but out of #6's original scope and needs its own explicit decision → filed as issue #8. `megaMind:getHeadPublic` (the other new entry) is clean (counts/hash/timestamp only).

**Verification:** `bun x tsc --noEmit` clean, full `bash scripts/test.sh` 121 files / 1659 tests green, `scripts/status.sh` unchanged and honest.
