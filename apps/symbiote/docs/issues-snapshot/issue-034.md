# #34 Round 6: the review leg becomes real

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: OPEN
- Labels: lane:fusion, needs:owner-approval, priority:next
- Created: 2026-07-02T06:11:27Z
- Updated: 2026-07-03T06:26:23Z
- Closed: not closed
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/34

## Body

Gated on #25's Work Item A follow-up (\`d5af527\`) — status.sh confirmed \`HEADLESS_READY\` before starting this.

## The problem, stated honestly
Every \`run:\` spends up to ~\$1-2, and its Fusion review returns NO_QUORUM almost every time — only 1-2 of 5 council models produce parseable votes. The paren bug (issue #22) is fixed; what remains are per-model format deviations against the strict glyph grammar. Separately, the chain's only in-chain check is the simulated content match — the real typecheck lane (\`sandboxTestRunner.ts\`, already used by the standalone \`run typecheck\` command) exists but is standalone-only, never wired into \`run:\`.

Round 6 fixes both verification legs.

## Scope

**B1 — capture before you fix.** Add an opt-in capture mode: when \`AUKORA_FUSION_CAPTURE=1\`, persist each council model's raw reply to \`${AUKORA_SYMBIOTE_HOME}/fusion-captures/<timestamp>-<model-slug>.txt\`. No capture mode, no blind parser changes.

**B2 — one live council run (real spend).** Run against a real small proposal with capture on. Classify each non-parsing reply: format-tolerable (widen the parser minimally) vs genuinely non-compliant (flag for a roster decision). Never loosen parsing so far that a malformed reply misparses into a vote.

**B3 — fixtures + regression tests.** Scrub captured replies (secret scan + hex-truncation), commit under \`core/tests/fixtures/\`, per-model regression tests with fail-before/pass-after for every parser change.

**B4 — roster configuration.** No \`FUSION_MODELS\`/roster-selection env var currently exists (confirmed by direct code search — only \`FUSION_ENV_FILE\`, which is for API-key file resolution, not roster selection). Will add \`AUKORA_FUSION_MODELS\` (comma-separated model slugs), matching the \`AUKORA_AGENT_MODEL\` naming precedent, documented and tested.

**B5 — wire the real typecheck stage into \`run:\`.** New stage helper wrapping the existing \`runRealTestCommand\` typecheck lane (never reimplemented). Chain order becomes agent → sandbox apply → content check → typecheck → fusion review → write receipt. A typecheck failure stops the chain (\`FAILED_AT: typecheck\`). Reported as its own stage, clearly distinct from \`content check\` (which keeps its \"simulated ... NOT a real test run\" label, unchanged).

## Acceptance criteria
- A live council run achieves quorum (≥3/5 parsed votes), with raw evidence (capture artifacts referenced, verdict/quorum output) posted in this issue's comments. If ≤2/5 models are simply incapable of the format, that's a legitimate outcome, but the comment must say so per-model and propose roster changes — never paper over it.
- Committed per-model fixtures with fail-before/pass-after regression tests for every parser/prompt change.
- Parser still rejects genuinely malformed replies (at least one adversarial fixture proving fail-closed survived).
- \`run:\` chain includes the real typecheck stage, with both failure-path and full-success tests.
- Roster env documented and tested.
- All existing pins unchanged: \`EXPOSED_TOOLS\`, the withheld-4 tool refusal, no gate-pinned file touched (nothing in this round should touch \`authority/gate/\`), live brain 75/75 + backup from A5 intact.
- Full protocol: \`test.sh\` green, \`status.sh\` HEADLESS_READY, \`tsc\` clean, fresh-clone verify.

## Cost disclosure
B2's live council run is real, owner-approved spend — one Fusion Council invocation, several model calls (same order of magnitude as any other \`run fusion review\`, roughly cents to low dollars depending on model pricing). Will not run it without explicit go-ahead.

## Out of scope this round
Nothing in \`authority/\` — no gate changes are needed; if a change there turns out to be necessary, stop and flag it first rather than proceeding. \`nativeLiveApply.ts\`, \`dashboard/fu\`, any owner WIP (\`docs/AUKORA_SOVEREIGN_COMPUTE_MASTER_PLAN.md\`, \`spatial/\`, \`.claude/\`, the icon PNG, \`docs/OPEN_WEAVE_V2.md\`).

## Comments

### Comment 1 - aumara-xyz - 2026-07-02T08:00:54Z

Per-model classification from the first live council run (real network calls, capture mode on, 5 real replies committed as fixtures under \`core/tests/fixtures/fusion-replies/\`):

| Model | Result | Classification |
|---|---|---|
| GLM 5.2 (z-ai/glm-5.2) | ✅ Parsed (YELLOW) | Fully compliant |
| Qwen 3.7 Max (qwen/qwen3.7-max) | ✅ Parsed (GREEN) | Fully compliant |
| DeepSeek v4 Pro (deepseek/deepseek-v4-pro) | ❌ then ✅ | **Real parser bug, fixed** — all 4 required \`DIST\` values present, correctly labeled, summing to 1.0, just in a different key order than the prompt's own example. Parser now extracts by name, any order (still requires all 4 present, still fail-closed). Fail-before/pass-after proven against this exact captured reply. |
| Kimi K2.7 Code (moonshotai/kimi-k2.7-code) | ❌ Empty reply | **Roster finding, not a parser issue** — returned zero bytes. Nothing to widen a parser against. |
| Llama 4 Maverick (meta-llama/llama-4-maverick) | ❌ Prose | **Roster finding, not a parser issue** — ignored the "respond with EXACTLY this format" instruction entirely and wrote multi-paragraph reasoning prose instead (never once attempted a glyph line). This is the adversarial fixture proving fail-closed survived the parser fix — it correctly still gets rejected. |

\`AUKORA_FUSION_MODELS\` (this round's new env var) is the immediate workaround — e.g. \`AUKORA_FUSION_MODELS=z-ai/glm-5.2,qwen/qwen3.7-max,deepseek/deepseek-v4-pro\` runs a 3-compliant-model council today without touching \`defaultCouncil()\`.

Filing a separate small issue for the roster decision itself — not changing the default roster in this commit.

### Comment 2 - aumara-xyz - 2026-07-02T08:06:55Z

Pushed as \`8044f5d\`. Fable's own QA pass on this round found one real test-isolation bug (a cleanup-counting test comparing a SHARED OS tmpdir's count before/after — a real race under parallel execution, not a leak) and two wording/hygiene gaps — all fixed in this same commit, verified with the full suite run **three times** (130 files / 1828 tests, all green each time) plus a fresh-clone verify.

Status: capture mode, the DeepSeek parser fix, \`AUKORA_FUSION_MODELS\`, and the real typecheck stage are all done, tested, and pushed. Per-model classification (GLM/Qwen compliant, DeepSeek compliant after the fix, Kimi/Llama genuine roster findings — filed as #36) is posted above.

**Leaving this issue open.** The remaining acceptance criterion — a *fresh* live council run showing ≥3/5 quorum with the fix deployed — is real spend and is a decision for the owner, not something to close on a retroactive fixture-level proof alone. Will close once that evidence is posted, or on explicit owner direction to close without it.

### Comment 3 - aumara-xyz - 2026-07-03T05:40:30Z

**Fusion inward-recursion: lean internal review path (planning; owner-approved-spend gated).**

This issue is the home for the "review cortex" direction. Current state: the council review is ALREADY wired into the `run:` chain (`reviewSelfEditProposal` → `selfEditReviewCouncil.ts`), routes through OpenRouter, and the roster is configurable (`AUKORA_FUSION_MODELS`, B4). Nothing to "turn on" structurally — only to configure lean and confirm reachability. Smallest safe path:

1. **Free metadata probe first (zero-spend, owner-approved).** `liveRoster()` does an UNAUTHENTICATED `GET https://openrouter.ai/api/v1/models` — no chat completion, no Authorization header, no token cost. It confirms whether `anthropic/claude-fable-5` is served + its prompt/completion price + vision. It CANNOT confirm account balance (account-specific; the probe sends no key). Ready to run on owner go; not run.
2. **Lean roster, not full-force.** `AUKORA_FUSION_MODELS="anthropic/claude-fable-5"` (or +1–2), NOT the 8-model FULL_FORCE — 1× per-token, not 8×.
3. **No paid council call without explicit owner approval.** A real review run is per-token spend.
4. **Egress discipline — tie to #84.** A real run uses the DISCOVERED opencode key (`~/.local/share/opencode/auth.json`), which is exactly #84's concern ("discovered key refuses until consent"). #84 is filed-not-built, so there is no automatic egress gate today — per-run explicit owner approval is the standing control until #84 lands. No hidden egress before #84, except explicit owner-approved runs.

Setting `priority:next`: the fusion review path the owner wants to push, sequenced after the #53 live-validation gate (a real proposal to review doesn't exist until #35, which sits behind #53 → #58).

### Comment 4 - aumara-xyz - 2026-07-03T05:49:14Z

**Ordering note / blocked-by:** although this issue is `priority:next`, it is **not ahead of #53**. The current ladder remains: #53 live validation first, then #58 read-only repo eyes, then this review-leg/council work can become part of the inward-recursion path. Do not treat #34 and #53 as peer-next items; #34 is blocked by #53 for live capability widening.

### Comment 5 - aumara-xyz - 2026-07-03T05:59:10Z

**Correction to my earlier planning comment (roster selection) — verified against code.**

My note above implied `AUKORA_FUSION_MODELS="anthropic/claude-fable-5"` would give a lean Fable-inclusive council. That is **wrong**:

- `resolveFusionCouncil(defaultCouncil())` (`selfEditReviewCouncil.ts:209`) filters requested slugs against `defaultCouncil()` (`aukoraFuEngine.ts:794`), whose members are ONLY: `deepseek/deepseek-v4-pro`, `qwen/qwen3.7-max`, `z-ai/glm-5.2`, `meta-llama/llama-4-maverick`, `moonshotai/kimi-k2.7-code`. **`claude-fable-5` is not a member.**
- So requesting `anthropic/claude-fable-5` via the env → dropped as unknown. And if EVERY requested slug is unknown, `resolveFusionCouncil` falls back to the FULL default council — i.e. requesting *only* Fable silently gives you all 5 models (the opposite of lean, ~5× spend).

**To get a lean Fable-inclusive council, Fable must first be registered as a `CouncilMember` in `defaultCouncil()`** (a ~1-line entry + a test). Only then does `AUKORA_FUSION_MODELS` select a lean subset including it. Small code brick — folds into this issue or #36 (roster composition). Not done this round (planning only); flagged for owner approval, and still behind the #53 gate for any live/spendful use.

Recommended smallest USEFUL roster once Fable is registered (3 members → a real majority quorum, avoiding the #36 empty/prose repliers llama-4 + kimi): **`anthropic/claude-fable-5, z-ai/glm-5.2, deepseek/deepseek-v4-pro`** — Fable's perspective + two reliable diverse parsers. Fable's per-token price is unknown from the static roster; the free /models probe reveals it.

### Comment 6 - aumara-xyz - 2026-07-03T06:26:23Z

**Roster prep landed (commit c7e90ad): a lean Fable-inclusive council is now selectable — but paid Fusion remains BLOCKED.**

What's ready (no spend): Fable is a registered optional council member, and `AUKORA_FUSION_MODELS` now selects a lean roster with fail-closed semantics (all-unknown no longer silently fires the full council). Lean command once unblocked:
```
AUKORA_FUSION_MODELS="anthropic/claude-fable-5,z-ai/glm-5.2,deepseek/deepseek-v4-pro"
```

**Still blocked — two gates, both required:**
1. **#53 live validation** must pass first (this issue is blocked-by #53 for live capability widening; a real proposal to review doesn't exist until #35, behind #53 → #58).
2. **Explicit owner spend approval** per run — a real council call is per-token spend and uses the discovered opencode key (#84 egress, filed-not-built), so per-run owner approval is the standing control.

The free /models probe (unauthenticated GET, zero token spend, reveals Fable's real price) is still owner-egress-gated and has not been run. No paid council call this round.
