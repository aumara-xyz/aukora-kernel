# #4 Round 4: Build per-proposal AUMLOK signed apply lane with rollback

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: CLOSED
- Labels: none
- Created: 2026-07-01T09:20:48Z
- Updated: 2026-07-01T12:11:42Z
- Closed: 2026-07-01T12:11:42Z
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/4

## Body

Source: Singularity Path Step-4 + actual aumlokAuthorityRoot/aumlokSigner state. Current live promotion is locked; signed rehearsal exists, live apply does not.

Goal:
Build a narrow, per-proposal signed apply lane after sandbox tests pass. This is not global live promotion and must not change isLivePromotionUnlocked() to true.

Acceptance criteria:
- Shared proposalHash module used by UI, signer, verifier, apply lane, and receipt.
- Browser displays exact diff/target files/proposal hash/test result before signing.
- Human authorizes exactly one proposal.
- Durable consume-once applied proposal ledger outside repo under AUKORA_SYMBIOTE_HOME.
- Replay of same signed proposal refuses.
- Sacred paths, symlinks, realpath escapes, env/private key paths refuse even with valid signature.
- Git apply/add uses exact file list only, never git add -A or dot.
- Commit identity is distinct and commit message references this issue.
- Rollback uses git revert of the exact apply commit and is tested.
- Temp repo tests first; no real repo apply without explicit owner instruction.

Completion protocol:
- Commit must reference this issue.
- Push branch after completion.
- Do not close until tests prove apply, replay refusal, sacred-path refusal, and rollback.

## Comments

### Comment 1 - aumara-xyz - 2026-07-01T12:11:41Z

Verified complete on disk and in local test run - this is the highest-stakes code in the project's history (first live-write capability), and was adversarially reviewed twice (design, then actual code) before being called done.

Files: core/src/nativeLiveApply.ts, core/src/proposalHash.ts, core/src/appliedProposalLedger.ts, core/src/selfEditProposalArtifact.ts (all new), scripts/aumlok-authority.sh (sign now reads a proposal artifact file instead of bare hash strings), core/src/nativeIdeDispatcher.ts (refactored propose_patch to use the shared proposalHash.ts fn).

Acceptance criteria checked:
- Shared proposalHash module (core/src/proposalHash.ts) used by nativeIdeDispatcher's propose_patch, the CLI sign command, and nativeLiveApply's own re-hash check - one implementation, no drift possible.
- Browser shows the exact goal/files/proposalHash via the proposal artifact file (written by "propose patch"); the CLI sign command also prints the full goal+file list before signing.
- Durable, disk-persisted, consume-once ledger outside the repo (${AUKORA_SYMBIOTE_HOME}/aumlok/applied-ledger.json) - closes a real replay/downgrade vulnerability an adversarial review found (the AUMLOK verifier itself has no nonce-replay check).
- Replay of the same signed proposal hard-refuses, citing the prior commit SHA - tested, including the specific "replay after a legitimate newer edit must not silently downgrade it" case.
- Sacred paths, symlink/realpath escapes all refuse even with a fully valid signature - tested with a real symlinked-directory escape attempt and a real sacred-path (authority/aumlok/...) attempt.
- git add uses "--" + exact file list only, never -A or a dot pattern.
- Commit identity is distinct ("Aukora Symbiote <aukora@local-dev-shim>"); commit message embeds proposalHash/signerKeyId/signedAt (this issue number reference goes in the code commit message per the new protocol - the live-apply commits themselves are per-proposal and carry their own distinct trailer format documented in BUILT_STATE.md).
- git revert rollback tested end-to-end against a real disposable git repo (not the live one).
- All 17 tests run against disposable temp repos + throwaway keypairs (generateKeypair()) - never the real repo or a real key. isLivePromotionUnlocked() is untouched, still literal false.

A real bug was found by the second adversarial-review pass and fixed: relPath "." bypassed the traversal check and crashed with an uncaught EISDIR (no write-outside-repo or corruption, but a real crash path) - now cleanly refused with a regression test.

Tests: core/tests/nativeLiveApply.test.ts (17 tests), core/tests/proposalHash + appliedProposalLedger + selfEditProposalArtifact tests, core/tests/aumlokAuthorityCli.test.ts (updated for the new CLI syntax) - all passing.

No real signed live-apply has been run against the actual aukora-symbiote repo or with a real key - that first real ceremony is the owner's to trigger, deliberately, in their own terminal.

Commit: 68b5481 (predates this issue - see note on #1 about commit-reference protocol going forward).
