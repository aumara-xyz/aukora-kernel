# #87 Future Kira brick: HRR holographic recall perceiver (advisory, snapshot-only)

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: OPEN
- Labels: lane:kira, lane:memory-lab, lane:safety-rail, priority:parked
- Created: 2026-07-03T00:52:25Z
- Updated: 2026-07-03T05:49:15Z
- Closed: not closed
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/87

## Body

## Ground truth

This is not implemented as of the current repo state. `core/src/kiraBrain.ts` still has exactly three advisory recall perceivers: `lexical`, `glyph`, and `topology`; `core/tests/kiraBrain.test.ts` pins that three-perceiver list; no `hrrPerceiver.ts` or HRR/holographic implementation exists in `core/src` or `core/tests`.

## Goal

Port the proven holographic reduced representation (HRR) memory idea into Kira as a fourth advisory recall perceiver alongside lexical / glyph / topology. Reference inspiration: `GOLDEN HORIZON PRINCIPLE 🔱/experiments/skunkworks_holographic_memory/hrr_core.py` (Plate 1995 HRR: d-dimensional vectors, circular-convolution bind, correlation unbind, superposition, cosine cleanup).

## Non-negotiable constraints

- Advisory only. The new perceiver emits recall scores/citations only. It never authorizes.
- Every artifact remains `advisoryOnly: true` and `grantsAuthority: false`.
- Do not touch gate, AUMLOK, receipts authority, live apply lanes, or signing code.
- Build and test against an exported snapshot copy of `state/kira/brain.json`, never the live brain.
- No benchmark/lab data becomes default memory.
- Deterministic seeded behavior.
- Zero egress: no network, in-process only, no new provider calls.
- No phi/Fibonacci/GHP physics claims. HRR is engineering memory, not authority or physics proof.

## Build shape

1. Add `core/src/hrrPerceiver.ts` with pure functions only: seeded 2048-d item-vector generator, circular-convolution bind, correlation unbind, superpose, cosine-resonance scoring. No new dependencies unless explicitly justified.
2. Wire into `core/src/kiraBrain.ts` as `PerceiverName = lexical | glyph | topology | holographic`. Existing three perceivers remain untouched.
3. `recall()` includes the holographic score as a fourth advisory evidence entry feeding `interference()`.
4. Preserve graceful degradation: partial/missing HRR trace down-ranks smoothly and never crashes recall.
5. Add no-upgrade header to new files: “Holographic recall is an engineering perceiver (Plate 1995 HRR); it neither tests nor supports GHP physics, and grants no authority.”

## Acceptance tests

- Bind → unbind round-trip resonance is high for matching role/filler and lower for wrong fillers.
- Holographic recall on an exported snapshot brain returns cited, advisory-only hits.
- Authority flags remain unchanged everywhere.
- Determinism: same seed produces same vectors/scores.
- Graceful degradation: zeroing or removing part of the trace degrades scores without crash or cliff.
- Source/invariant test proves HRR imports no gate/AUMLOK/apply/network authority lane.

## Sequencing

Parked for now. This is a strong Kira memory upgrade, but it should land after the current inside-out recursion rail basics are stable: #53 live close, #68 spatial typecheck, #75 safe repo path resolver, and #58 read-only repo eyes. It strengthens recall; it is not the immediate blocker for safe inward recursion.

## Comments

### Comment 1 - aumara-xyz - 2026-07-03T05:40:50Z

**Convergence principle for HRR (planning — status: parked).**

Pinning the record-vs-index boundary ahead of any HRR build, per Auma's law: **the HRR trace is a regenerable, lossy index — never the record.** The ground-truth-readable record is the plain-English-mirrored GML measurement packet (#89) and the receipt-bound Kira atom (#79); HRR only accelerates recall over those. A trace must be reconstructable from the records, never the sole source of a fact, and never an authority input. "A receipt of unreadable model-to-model communication is not meaningful evidence" — an HRR trace that cannot be re-derived and read in plain English is contamination, not telemetry. Advisory/snapshot-only when built.

### Comment 2 - aumara-xyz - 2026-07-03T05:49:15Z

**Additional HRR retrieval-honesty requirements (planning — still parked).**

Two more constraints before any implementation:

1. **Capacity policy required.** A 2048-d HRR superposition cannot honestly represent “the entire transaction history” forever. The implementation must use an explicit capacity strategy: epoch traces, per-file/per-scope traces, decay, or another bounded policy. When a trace approaches capacity, recall must degrade visibly and never pretend certainty.

2. **Cleanup memory + confidence required.** Circular correlation unbinds noisy approximations. GML-1’s closed vocabulary (#89) should serve as the cleanup codebook for stance/risk/confidence symbols. Every HRR resonance result must include a confidence score and a path back to the raw receipt/GML packet/Kira atom that generated the trace. The trace is an index; raw receipts remain the record.

No code yet. This stays parked until the recursion rail basics are stable (#53 live validation, #58 read-only eyes, then the proposal/receipt loop).
