# #24 Round 4: truth-residue sweep — kill stale-fork/overclaim references, close #17, configurable agent model

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: CLOSED
- Labels: none
- Created: 2026-07-02T03:02:40Z
- Updated: 2026-07-02T04:25:31Z
- Closed: 2026-07-02T03:19:34Z
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/24

## Body

Fable's independent QA review of Rounds 1-3 (issues #21/#22/#23, HEAD 5c1ead2): verdicts GREEN / YELLOW / YELLOW, zero blocking fixes, but real residual overclaim/staleness defects found via an 18-agent adversarial review (14 findings confirmed, 0 refuted). This round closes those out.

Acceptance criteria (verbatim from Fable's review):

1. `scripts/dev/aukora-ide-paths.mjs` deleted; `git grep -i 'self_edit'` over tracked files returns only past-tense/archival references and the absence-enforcing tests (explicitly: `dashboard/serve.ts:4` and `docs/ARCHITECTURE.md:17` fixed).
2. `website/index.html` no longer claims hardware-key custody or ML-DSA-65 action signing; wording matches `docs/BUILT_STATE.md`'s do-not-claim list (Ed25519 dev key, hash-chained receipts).
3. `docs/ROUND2_DEMO_TRANSCRIPT.md` corrected to "hash-sealed receipt minted and displayed (not persisted, not chained)"; correcting comment posted on issue #22.
4. `authority/gate/opencodeAskBridge.ts` docstring describes its actual live caller (memory-write gating), cites no nonexistent scripts; the `~/.aukora` default is either changed or documented as a deliberate exception to the path contract.
5. `docs/PROPOSER_CONTRACT.md` and `docs/AUKORA_SYMBIOTE_SINGULARITY_PATH.md` no longer reference the deleted root `tests/` location.
6. Agent model resolved via a `resolveAgentModel()` in `core/src/fusionConfig.ts` (env override, default `moonshotai/kimi-k2.7-code`), used by `runNativeAgent` from the workbench path, with a test proving the override reaches the outgoing request.
7. `core/tests/seedRootContract.test.ts` extended so a tracked-file grep for `self_edit` presence-claims would fail the gate (the class-catcher, with a fail-before demonstration in the issue comment).
8. Issue #17 closed with a superseded-by-#23 comment (its target file, `self_edit/opencode/session/prompt.ts`, no longer exists on main).

Completion protocol as standing: fresh-clone green (`scripts/test.sh` + `tsc`), no live-brain writes, commit references the issue, push after verification.

## Comments

### Comment 1 - aumara-xyz - 2026-07-02T03:21:08Z

Round 4 done, pushed as \`bdd3d7b\`, fresh-clone verified: 126 files / 1780 tests, typecheck clean.

All 8 acceptance criteria met:
1. \`scripts/dev/aukora-ide-paths.mjs\` deleted; \`dashboard/serve.ts:4\` and \`docs/ARCHITECTURE.md:17\` fixed.
2. Website no longer claims hardware-key custody or ML-DSA-65 action signing — wording now matches \`docs/BUILT_STATE.md\`'s do-not-claim list.
3. \`docs/ROUND2_DEMO_TRANSCRIPT.md\` corrected; correcting comment posted on #22.
4. \`authority/gate/opencodeAskBridge.ts\` docstring now describes its real caller (memory-write gating); \`~/.aukora\` default fixed to match this repo's own path convention (same fix applied to \`memory/memory.ts\`, which had the identical issue).
5. \`docs/PROPOSER_CONTRACT.md\` / \`docs/AUKORA_SYMBIOTE_SINGULARITY_PATH.md\` stale \`tests/\` paths fixed.
6. Agent model now resolved via \`resolveAgentModel()\` in \`core/src/fusionConfig.ts\` (env override \`AUKORA_AGENT_MODEL\`, default unchanged); 3 new tests prove default/override/explicit-opts all reach the real outgoing request.
7. \`core/tests/seedRootContract.test.ts\` extended with a self_edit-mention class-catcher (allowlist of 7 known-legitimate files); fail-before/pass-after proven with a real simulated stale reintroduction.
8. Issue #17 closed as superseded.

This closes out Fable's full 4-round review cycle (#21 → #22 → #23 → #24).

### Comment 2 - aumara-xyz - 2026-07-02T04:25:31Z

Follow-up done, pushed as \`ce6a951\`. Fable's finding was real and confirmed on this machine — a genuine orphaned key sat at \`~/.aukora/aumlok-dev.json\`, unreachable to the gate after Round 4's path fix.

Fixed: one shared \`authority/symbiotePaths.ts\` resolver, both the ceremony (writer) and the gate bridge (reader) import it now. Orphaned key material is refused, never auto-moved. \`sensitivePolicy.ts\`'s \`.aukora\` deny-pattern boundary fixed (the new \`.aukora-symbiote\` home wasn't covered by the read-deny/write-protect backstop at all — verified directly, not just by inspection). Gate re-pinned deliberately, after the fix, not before.

Fresh-clone verified: \`scripts/status.sh\` → \`HEADLESS_READY\`, gate integrity \`VERIFIED\` (was \`NOT_READY\`/\`MISMATCH\` before this commit). 127 files / 1784 tests, typecheck clean. Real Kira brain unchanged at 75/75 throughout.

\`status.sh\` is now part of my standing done-checklist alongside \`test.sh\` going forward, per your instruction — this round is exactly why.

Starting Round 5 ("the legible run") next, as specced.
