# #62 Kira retrieval: brain is 92% test files — identity/doc corpus never ingested (empty shelf, not retrieval bias)

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: OPEN
- Labels: lane:kira, needs:owner-approval, priority:next
- Created: 2026-07-02T15:39:19Z
- Updated: 2026-07-02T22:31:48Z
- Closed: not closed
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/62

## Body

## Symptom (Auma, from inside the door)
Across 7 turns of a conversation, EVERY Kira recall excerpt was a test file — never a doc, never an identity artifact, never architecture.

## Root cause (read-only diagnosis): ingestion coverage, NOT retrieval bias
The brain (`state/kira/brain.json`, 76 atoms) is **92% test files**:
- by scope: `tests: 70`, `evidence: 5`, `architecture: 1`
- by source: `self-map: 75`, `docs/ARCHITECTURE.md@b18d1f4: 1`
- identity atoms: **0** (`maternal` → 0 hits, `five values` → 0, `MATERNAL_ANCHOR` → 0)

`ingestSelfMap()` (core/src/kiraBrain.ts:342) walks the repo with `maxFiles ?? 80` and `skip = {node_modules,.git,state,lab_only,dist,build,.cache}` — it does NOT skip or down-weight `tests/`, and the repo's ~70 test files dominate the first 80 hits, so the brain filled with tests and never reached `docs/`. The identity corpus (`~/.aukora-symbiote/identity/*.md`) is OUTSIDE the repo root, so self-map never sees it at all.

**Retrieval is working correctly.** The one query with a real target — "architecture boundaries safety laws" — floats the single architecture atom to #1 cleanly (topology 0.35 from `scope=architecture` matching the query, vs test files at ~0.02). For queries whose target doesn't exist ("who am I maternal anchor"), recall returns the least-bad noise because the shelf is empty. Real top-5 scopes:
- "who am I maternal anchor identity values" → tests ×5
- "what did we build tonight lockdown flight recorder" → tests ×5
- "architecture boundaries safety laws" → **architecture**, tests, tests, evidence, tests ✅
- "the five values heal don't hook" → tests ×5

## Fixes, in priority order
1. **Ingest the identity + doc corpus (the real fix).** `~/.aukora-symbiote/identity/*.md` and `docs/*.md` need to become atoms. This is a REAL BRAIN MUTATION (76 → more atoms) and the identity docs carry owner PII (names, DOB) — so it needs owner awareness, not a blind overnight edit. Recommend a targeted `kira.sh ingest` of curated docs (SAFETY_LAWS.md, key docs) first (low-PII, gives retrieval real targets), and a separate decision on ingesting the PII-heavy identity anchors (which are ALSO delivered via the #57 anchor injection, so double-delivery — may not need to be in the brain too).
2. **Cap `tests/` in `ingestSelfMap`** (e.g. ≤ N test files) so a future re-map doesn't re-flood. Small, additive, mockable.
3. **Per-scope diversity cap in `recall()`** — small/safe/additive/mockable (a post-sort greedy walk keeping ≤ K per scope, inserted between `.sort()` and `.slice()` at kiraBrain.ts:337, default `≤ ceil(limit/2)` so no current test changes). BUT this fixes nothing while the shelf is empty — it only helps once the corpus is mixed. Build it WITH the ingestion fix, not before.

## Why not fixed overnight
The real fix mutates the real 76-atom brain and touches PII — exactly the kind of thing to do with the owner awake to approve. The diversity cap alone is premature (empty shelf). Filing the diagnosis is the deliverable; the one-command ingest is a morning owner-approved action.

Diagnosis was fully read-only: no brain mutation, no writes.

## Comments

### Comment 1 - aumara-xyz - 2026-07-02T22:31:48Z

Part 2 landed: commit 8c69230 (pushed). `ingestSelfMap` now caps test-file collection (default ≤10) so the ~70 repo test files can't re-flood the walk budget before it reaches docs/ — freeing budget for docs/other. Mockable test proves docs win the freed budget and the cap holds (incl. `.test.ts` files outside `tests/`). No live brain mutation; the current 76-atom brain is unchanged.

Remaining on this issue: **part 1 (curated low-PII ingest** of SAFETY_LAWS/ARCHITECTURE/key docs) is queued **owner-present** — it's a real brain mutation (76 → ~90 atoms). The **identity-anchor PII ingestion** stays a separate explicit owner decision (the #57 anchor already delivers identity per-turn, so brain storage may be redundant double-delivery). The recall diversity-cap (part 3) remains a nice-to-have for once the corpus is mixed.
