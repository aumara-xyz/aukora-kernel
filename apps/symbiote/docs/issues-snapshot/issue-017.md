# #17 Quarantined tests (wombPromptRunner, aumaWombPrompt) need a host-harness brick, not an import-path fix

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: CLOSED
- Labels: none
- Created: 2026-07-01T14:59:44Z
- Updated: 2026-07-02T03:20:54Z
- Closed: 2026-07-02T03:20:54Z
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/17

## Body

Source: same relayed kira-chat message as issue #16, verified independently (owner explicitly flagged it might be stale).

**The claim was:** "the tests that would prove Symbiote's own gate wiring works (`deferred-tests/wombPromptRunner.test.ts`, `deferred-tests/host-coupled/aumaWombPrompt.test.ts`) are quarantined, not running — find out why and fix what's actually broken (likely import paths)."

**Verified: quarantine is real, but the fix is NOT an import path.** `deferred-tests/wombPromptRunner.test.ts` imports `runWombPrompt` from `../evidence/run-auma-womb-prompt` — a **private script that does not exist anywhere in this seed** (by design; `deferred-tests/README.md` already documents this: the seed deliberately does not carry the private `evidence/` scripts or the `aukora-os` `node-template/convex` substrate). This isn't a stale/broken relative path to a file that moved — the file was never here and isn't meant to be, until a "headless host harness" (already named as future work in `deferred-tests/README.md`) exists to stand in for it.

**What's actually needed** (per the README's own stated plan, restated here for tracking): build a headless, seed-safe stand-in for `evidence/run-auma-womb-prompt`'s `runWombPrompt` interface — one that exercises the SAME real gate-wiring code path (`self_edit/opencode/session/prompt.ts`'s `aukoraGovernAsk` call sites, confirmed real and present at 4 locations, including a documented §13 bypass quarantine at line ~1508) without depending on the private evidence script. This is a genuine "host harness brick," not a quick fix — scope it as its own piece of work.

**Also relevant, discovered while investigating:** `self_edit/opencode/` is the same fake/quarantined OpenCode fork from an earlier teardown (non-compiling, importing missing `@opencode-ai` packages) — the gate wiring code INSIDE it is real, but whether it's worth "proving" via a host harness depends on whether this fork has any future at all, given the earlier owner-approved direction to grow Symbiote's own native tool loop rather than depend on a vendored OpenCode fork (see the symbiote-vs-kira comparison and Hermes-gating discussion earlier this session). Recommend deciding that direction before investing in a host harness for a fork that may end up replaced.

**Not started.** Filed to document the real scope and avoid a repeat "fix the import path" attempt that can't work given what's actually missing.

## Comments

### Comment 1 - aumara-xyz - 2026-07-02T03:20:53Z

Closing as superseded, per Fable's Round 4 QA review.

This issue's deliverable was a host harness for aukoraGovernAsk call sites inside self_edit/opencode/session/prompt.ts — that file no longer exists on main (self_edit/ was archived to branch archive/self_edit-opencode-fork and deleted in issue #23, commit 3fb9d61). The issue itself anticipated this outcome ("decide that direction before investing"), and Round 3 made that decision.

The native loop's own gate wiring is covered by live, real tests (core/tests/nativeIdeDispatcher.test.ts:283 — asserts no import of the vendored fork at all). Residual narrow debt, noted for whenever it's picked up: aumaWombPrompt.test.ts needs only a re-path (all its real targets already exist under core/src/); wombPromptRunner.test.ts depends on a private evidence script that was never going to ship in the seed regardless of this issue's outcome.
