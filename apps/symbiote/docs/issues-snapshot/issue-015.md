# #15 Land aukora-fu v8.0.0, superseding fusionGlyphEngine.ts v7.1.0 (fixed a dead-code phase-lock bug)

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: CLOSED
- Labels: none
- Created: 2026-07-01T14:23:22Z
- Updated: 2026-07-01T15:10:18Z
- Closed: 2026-07-01T15:10:18Z
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/15

## Body

Source: owner-provided export \`imported tech (github etc)/aukora-fu-v8.0.0-GITHUB.zip\`, same author/lineage as the v7.1.0 engine landed in issue #10 (now removed, superseded — it was never wired to anything, zero risk). Inspected before landing: no secrets (only placeholder key examples in docs), real self-contained TypeScript, zero dependencies beyond \`fetch\`. Independently re-ran the source export's own test suite before landing: 21/21 passed, matching its README claim exactly.

**What's new over v7.1.0:** the \`~\` (tilde) interference operator between model frameworks, φ-governed contradiction decay (shear never fully resolves, floors at 1/φ), phase-lock/groupthink detection, framework-type awareness (geometric/symbolic/statistical/narrative/embodied/social), and VK Kronos security-incident logging (malformed glyphs, timeouts, phase-lock events all logged as first-class \`SecurityIncident\` objects). Same evidence-never-authority discipline throughout (\`grantsAuthority: false\` hardcoded on every Receipt).

**Two real bugs found and fixed while landing (not silently shipped):**
1. \`runCLI\` referenced \`CODename\`, a second, differently-cased, functionally-duplicate module constant declared later in the file (harmless at runtime since module-level consts are fully initialized before any function executes, but dead/confusing duplication) — collapsed to the single \`CODENAME\` constant.
2. **A real, confirmed-dead safety feature**: \`detectPhaseLock()\`'s threshold was a hardcoded \`0.15\`, but \`tilde()\`'s shear value is \`clamp()\`ed to a floor of \`SHEAR_FLOOR = 1/φ ≈ 0.618\` — meaning \`maxShear < 0.15\` could **never** be true. Phase-lock/groupthink detection — one of v8's headline features — was silently inert in every version of the source zip. Fixed by making the threshold relative to the actual floor (\`SHEAR_FLOOR + 0.05\`) instead of an absolute value the floor makes unreachable. Verified via a test that previously encoded the broken (always-false) behavior as "correct" — updated to reflect genuinely-working phase-lock detection, which now correctly flags identical-distribution "perfect agreement" as suspicious groupthink (escalates GREEN→YELLOW) rather than treating it as a clean pass.

**Status: landed but DORMANT still** (matches the pattern from issue #10) — nothing in this repo calls \`AukoraFuEngine.reason()\`/\`emitGlyph()\` yet, pinned by a structural test. Per the owner's explicit directive, this engine is intended to REPLACE (not augment) the existing GREEN/YELLOW/RED categorical review in \`selfEditReviewCouncil.ts\`/\`fractalFusion.ts\` — resolving issue #12's open design question as Option A. That wiring is tracked as its own follow-up (larger, touches the safety-relevant review pathway, needs its own careful implementation + verification pass), not done in this commit.

**Verification:** full suite 122/122 files, 1677/1677 tests green, typecheck clean. Two other unrelated pre-existing flaky tests hit during this round (sandboxTestRunner.test.ts tmpdir-count race, externalReviewRepair.test.ts timeout timing) both reproduced as passing cleanly in isolation — not regressions from this change.

## Comments

### Comment 1 - aumara-xyz - 2026-07-01T15:10:17Z

Closing — landed and now fully wired (commit da0de47 landed it dormant, commit b9cffdb wired it in as the actual review engine per issue #12, both adversarially verified — see #12's closing evidence for the full 4-lens review).

Summary: aukora-fu v8.0.0 is real, tested (independently re-ran the source export's own 21-test suite before landing, then ported those into a real vitest file), and is now the live engine behind `selfEditReviewCouncil.ts`'s Fusion review. Two real bugs found and fixed during landing (a dead CODename/CODENAME duplicate constant, and a genuinely dead phase-lock/groupthink detector whose threshold was mathematically unreachable given the shear floor).
