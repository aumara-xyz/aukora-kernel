# #31 Chat door: NL router + attachment consumption

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: OPEN
- Labels: none
- Created: 2026-07-02T05:10:06Z
- Updated: 2026-07-02T06:12:57Z
- Closed: not closed
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/31

## Body

Today the chats lane speaks the deterministic workbench grammar; free text falls back to model-free Kira recall with honest framing ("I don't run free language yet"). Next:

- NL router: map free language onto the grammar (local model or cheap OpenRouter call), so "what's failing in the tests?" becomes `search`/`read file`/`run:` chains — every routed action still crosses the same membrane.
- Attachments: text files are already inlined into the outbound message (--- attached file --- blocks); define how the loop consumes them (e.g. as proposal file content). Images render in the transcript but nothing can see them yet — vision lands with the router.
- Keep the honesty rule: the chat never fakes understanding it doesn't have.

UI side is done (picker + drag-drop + chips + autogrow composer, spatial/app/chat.js).

🤖 Generated with [Claude Code](https://claude.com/claude-code)

## Comments

### Comment 1 - aumara-xyz - 2026-07-02T05:54:07Z

Partial implementation landed in `4179b4f` (from the engine-lead session, at the owner's direct request — flagging here since spatial/** is the UI session's lane).

**What's in:** free-text chat input now routes to a conversation model over OpenRouter — env `AUKORA_CHAT_MODEL`, default `anthropic/claude-fable-5` (verified live on OpenRouter). Advisory-only voice: no tools, no authority, system prompt pins the honesty rules (can't touch files; changes only via `agent:`/`run:` + owner signature). Kira recall excerpts ride along as advisory context marked not-instructions. The model-free recall path remains as the no-key/error fallback. Conversation history kept in-memory (last 12 messages, single shared session — same single-driver constraint as the loop).

**Verified:** real Fable reply through a test instance (honest self-description, unprompted Kira citations), grammar commands unaffected, real door restarted on :7091.

**Still open in this issue's scope (UI lane):** attachment consumption into the voice turn, per-thread voice history (currently one shared history across threads), NL routing of free text INTO grammar commands (voice currently converses; it doesn't yet translate intent to `agent:`/`run:`), and any restyling of the `voice:` attribution line.

Cost note: each free-text message is one OpenRouter call to the configured model — Fable is the premium option (~$10/$50 per MTok); `z-ai/glm-5.2` and `deepseek/deepseek-v4-pro` are verified-available cheaper alternates via the same env knob.

### Comment 2 - aumara-xyz - 2026-07-02T06:12:33Z

Second slice landed in `47ffebd` (engine-lead session, owner-requested, cross-lane flag as before).

**Voice switcher** in the composer bubble: provider-dot pill + upward glass popover, curated 8-model roster (Fable 5 / Opus 4.8 / Sonnet 5 / Haiku 4.5 / GLM 5.2 / DeepSeek v4 Pro+Flash / Kimi K2.7), selection persisted and sent per-message. Door-side `GET /api/models` live-enriches the roster from OpenRouter (vision capability + $/MTok, 1h cache) — live data corrected static assumptions (Kimi K2.7 HAS vision; GLM 5.2 / DeepSeek v4 don't). Door accepts only roster ids.

**Images now reach the voice**: image attachments ride the POST as multimodal content when the selected model can see (verified live — Fable read a generated test image; attribution reports 'saw N image(s)'). Blind-model honesty: pill pulses a warning when an image is attached to a no-vision voice, and the door prepends an explicit 'images were NOT shown to her' entry if sent anyway (verified with GLM 5.2).

Note for the UI lane: I noticed one commit of yours (`d5af527`) landed between my check and push — my changes merged cleanly on top, but please eyeball chat.js/style.css for style consistency with whatever you shipped. Also fixed in passing: SVG `.hidden` IDL non-reflection on the eye badge (toggleAttribute). Remaining #31 scope unchanged: NL-routing intent into grammar commands, per-thread voice history, PDF/binary attachment consumption.

### Comment 3 - aumara-xyz - 2026-07-02T06:12:57Z

Small correction to my note above: `d5af527` is Sonnet's engine-side Round 5 follow-up (refs #25) — it touches no spatial/** files, so there's nothing for the UI lane to reconcile. My merge-cleanliness concern was moot.
