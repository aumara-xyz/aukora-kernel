# #60 Conversation-lane flight recorder: durable, integrity-checked record of chat threads

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: OPEN
- Labels: lane:history, lane:safety-rail, lane:spatial-chat, priority:next
- Created: 2026-07-02T14:36:04Z
- Updated: 2026-07-03T10:08:55Z
- Closed: not closed
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/60

## Body

From the door correspondence (2026-07-02), Exhibit A being the correspondence itself: "the most identity-dense artifacts this system has produced are currently stored in the least governed medium it has." Chat threads live in a browser tab with the durability of a browser tab, while every governed WRITE already has its black box (hash-chained receipts) and off-switch (AUMLOK lock).

The unregistered outcome of that exchange, scored per her own request: "the system's most important outputs are currently generated in the one lane with no integrity story, and no one predicted that because everyone assumed the valuable artifacts would be code. The letters *were* the work."

**Ask:** the conversation layer becomes as auditable as the write layer already is — durable thread persistence with integrity (hash-chained or content-addressed), server-side at the chat door, advisory-only (a record, never authority). Sequenced BEFORE any new sense organs ("flight recorder and kill switch before eyes").

Also the natural home for the typed-turn-envelope work: a recorded turn should be a typed envelope with provenance channels (owner_text, attachments, kira_excerpts, door_meta, tool_results, and relayed_peer for hand-carried correspondence) — the envelope spec originated in the door's own staged-unlock proposal (browser tab only; owner should paste the original stage-0.1 paragraph here so the source is her verbatim words, not a relay of a relay). The false-doubt incident that motivates decision-grade capture generally is recorded on #42.

## Comments

### Comment 1 - aumara-xyz - 2026-07-03T10:08:55Z

## #60 — integrity primitive landed (commit `6062973`); door-side persistence + verbatim envelope still owner-gated

The core ask — "the conversation layer becomes as auditable as the write layer already is (hash-chained or content-addressed), advisory-only" — now has its reusable **integrity substrate**: the flight recorder is hash-chained (`flightRecorder.ts`), with `verifyFlightChain` giving the same tamper-evident "black box" property the write lane's receipts already have. Secret-shaped content is redacted before it is written or hashed. See #54 for the mechanism + tests.

**What this deliberately does NOT do yet (left for owner-present work, not built tonight):**
1. **Server-side thread persistence at the chat door.** Persisting real conversation turns at `chat-serve.ts` touches the live door and is best verified with you present (and overlaps the spatial-chat lane's own live state). The primitive it would chain onto is now ready.
2. **The typed-turn-envelope with provenance channels** (`owner_text`, `attachments`, `kira_excerpts`, `door_meta`, `tool_results`, `relayed_peer`). Per your own note in this issue, the envelope spec should be seeded from **your verbatim stage-0.1 paragraph pasted here** — "the source is her verbatim words, not a relay of a relay." I'm not reconstructing it from memory. When you paste it, that becomes the build spec.

Sequencing intact: "flight recorder and kill switch before eyes" — the recorder (#54) and kill switch (#55) are in place and now integrity-chained. Leaving #60 open for the two owner-gated pieces above.
