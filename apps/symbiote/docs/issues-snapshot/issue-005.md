# #5 Round 5: Wire Fusion and Mega Mind as advisory reviewers of sandbox proposals

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: CLOSED
- Labels: none
- Created: 2026-07-01T09:21:06Z
- Updated: 2026-07-01T12:28:34Z
- Closed: 2026-07-01T12:27:34Z
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/5

## Body

Source: Singularity Path Fusion lane + current WIP files core/src/megaMind*.ts and fixed Fusion runner. Fusion and memory are advisory only.

Goal:
After a sandbox proposal and real test result exist, run advisory review through Fusion and Mega Mind read-only memory surfaces. They may recommend, never authorize.

Acceptance criteria:
- Fusion review runs after sandbox apply + tests and emits validated fusion-run-v1 / fusion-self-review-v1 artifacts.
- Mega Mind surface, if present, is read-only/advisory and cannot write memory or grant authority.
- Review output appears in Workbench transcript as evidence, not command.
- No Fusion/Mega Mind output can call tools, sign, unlock, promote, mutate Convex, or bypass AUMLOK.
- Kimi may appear only as a Fusion Council model, never as the internal Aukora engine.
- Tests prove advisory artifacts cannot influence dispatcher/apply without explicit signed proposal flow.

Completion protocol:
- Commit must reference this issue.
- Push branch after completion.
- Do not close until tests pass and artifacts validate fail-closed.

## Comments

### Comment 1 - aumara-xyz - 2026-07-01T12:28:34Z

Note: this issue was already closed (no comment attached) before I could close it with evidence — leaving the verification record here for the audit trail, since disk/test/commit state must always be checkable regardless of who closed the issue.

**Commits this round:** 56d4462 (real fusion-run-v1/fusion-self-review-v1 artifacts + Mega Mind recall wiring in `core/src/selfEditReviewCouncil.ts`), 99e4a7e (two new invariant tests proving advisory output structurally cannot reach the live-apply path).

All 6 acceptance criteria verified against real disk state and passing tests:
1. Fusion review emits validated `fusion-run-v1`/`fusion-self-review-v1` artifacts — proven in `core/tests/selfEditReviewCouncil.test.ts`.
2. Mega Mind recall is read-only/advisory (never calls `ingestMemory`/`saveBrainState`; `advisoryOnly: true, grantsAuthority: false` pinned) — proven with 4 dedicated tests including a real recall against a constructed disposable brain state.
3. Review output only ever appears in the Workbench transcript as a `tool_result` entry — never a command.
4. `WorkbenchSessionState` has exactly 4 fields (`toolCallsUsed`, `lastProposal`, `lastSandboxPath`, `lastTestResult`) — no field exists for a Fusion/Mega Mind verdict to be stashed into, proven directly by a new test.
5. Kimi is not in `DEFAULT_REVIEW_MODELS` (`z-ai/glm-5.2`, `deepseek/deepseek-v4-pro`) — can only ever appear as one optional roster model, never the engine.
6. A new source-scan test confirms the `apply signed proposal` handler never references `review`/`fusion`/`megaMind` — its only inputs are `session.lastProposal` and an externally-signed receipt file, then `dispatchSignedLiveApply`.

Full suite this round: `bun x tsc --noEmit` clean, `bash scripts/test.sh` 120 files / 1584 tests green, `scripts/status.sh` unchanged and honest. Not claimed: a real live-network Fusion call is not exercised in the automated CI suite (would need a real OpenRouter key and wouldn't be deterministic) — only manually via the Workbench.
