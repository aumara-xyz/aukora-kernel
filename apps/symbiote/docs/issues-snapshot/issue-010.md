# #10 Land Fusion Glyph Engine v7.1.0 (dormant, not yet wired)

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: CLOSED
- Labels: none
- Created: 2026-07-01T13:44:34Z
- Updated: 2026-07-01T15:10:06Z
- Closed: 2026-07-01T15:10:06Z
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/10

## Body

Source: owner-provided export `imported tech (github etc)/fusion-engine-v7.1.0-EXPORT (1).zip`, inspected before landing (no secrets found — only parameter names and an `process.env.OPENROUTER_API_KEY` reference; real, self-contained TypeScript, zero dependencies beyond `fetch`).

**What this is:** A glyph-native multi-model reasoning engine (`core/src/fusionGlyphEngine.ts`). Council members communicate via structured probability distributions rather than English prose; a KL-divergence perceiver reads the distributions and produces a GREEN/YELLOW/RED coherence verdict; the engine adapts a handful of numeric run-parameters (maxRounds/explorationDecay/contrarianBoost/per-model weights) between calls based on how much the council disagreed. This is in-memory hyperparameter tuning of the reasoning process itself — not code self-modification, not an authority change. Every `Receipt` it mints is pinned `advisoryOnly: true, grantsAuthority: false`, matching the discipline already enforced in `nativeLiveApply.ts`/`megaMindBrain.ts`/`selfEditReviewCouncil.ts`.

**Status: landed but DORMANT.** Not wired into `workbenchCommandLoop.ts`, `selfEditReviewCouncil.ts`, or any live/authoritative path. Its only network call (`emitGlyph` → OpenRouter) is not invoked by anything in this repo yet — confirmed by a structural test (`core/tests/fusionGlyphEngine.test.ts`, "advisory-only discipline" describe) that pins exactly one `fetch(` call site in the module and zero importers elsewhere in `core/src/`.

**Open decision, NOT made by this issue:** whether this replaces the existing GREEN/YELLOW/RED Fusion Council review (`selfEditReviewCouncil.ts` + `fractalFusion.ts`, which uses categorical voting rather than probability-distribution KL-divergence), runs alongside it as an additional advisory lens, or stays dormant longer. Needs an explicit owner decision before any wiring work starts.

**Verification this round:** ported the original hand-written `run-tests.js` (10 tests, all passing, but re-derived the perceiver math inline rather than testing the shipped module) into a real vitest file that imports and tests the actual shipped code — `core/tests/fusionGlyphEngine.test.ts`, 12/12 passing. Full repo suite: typecheck clean, 121/122 files green (one unrelated, pre-existing flaky test in `sandboxTestRunner.test.ts` — a tmpdir-entry-count assertion racing against concurrent processes on this machine, already independently identified in an earlier review round; not caused by this change, reproduced in isolation).

**Completion protocol:** commit references this issue. Not closing yet — closure requires an explicit decision on the "replace vs. augment vs. stay dormant" question above.

## Comments

### Comment 1 - aumara-xyz - 2026-07-01T15:10:05Z

Superseded — v7.1.0 was entirely removed (commit da0de47) in favor of aukora-fu v8.0.0 (issue #15), landed the same night per the owner's direction to use the newer, more advanced version. Nothing of v7.1.0 remains in the tree; there's nothing left to land under this issue.
