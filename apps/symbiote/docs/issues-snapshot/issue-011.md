# #11 Fix Convex bridge port misconfiguration (3220 -> 3210) + reclassify canonical backend

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: CLOSED
- Labels: none
- Created: 2026-07-01T13:55:39Z
- Updated: 2026-07-01T14:03:16Z
- Closed: 2026-07-01T14:02:55Z
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/11

## Body

Follow-up from issue #6's verification. There IS a real, live Convex backend running right now (port 3210, PID confirmed, real SQLite data, deployed from aukora-os's node-template/convex) that already serves `aukoraReceipts:getReceiptChainHeadPublic` — exactly the query `convexBrainReadonly.ts`'s allowlist expects. Symbiote's bridge code is real and tested but hardcoded to port 3220, where nothing listens.

**Scope:**
1. `core/src/convexBrainReadonly.ts` `DEFAULT_CONFIG.url`: point at the real backend instead of the dead port. Should this be hardcoded to 3210, or read from an env var with 3210 as default (matching `kernelAdapter.ts`'s existing `AUKORA_CONVEX_URL` fallback pattern, which already defaults to 3210 but isn't consulted by this bridge)? Prefer the env-var-with-safe-default pattern for host-agnosticism (issue #6's own bar).
2. `core/src/convexTopology.ts`: reclassifies port 3210 as `wrong_backend` today ("Does not serve the canonical kernel functions") — this is now factually false and needs updating to recognize it as the canonical kernel per the real, verified response shape.
3. Verify live: with the real backend running, confirm `readLoopbackSurface`/`enrichSnapshotWithLoopback` actually pull real data end-to-end (not just mocked-fetch tests) — a manual/scripted smoke check, reported honestly either way.

**Acceptance:** real backend read succeeds with real data through Symbiote's existing (already-tested) loopback path; no change to the write-refusal invariants (`MUTATION_DENY`, `rejectMutation`); mocked-fetch tests plus one live smoke-check evidence in the closing comment.

## Comments

### Comment 1 - aumara-xyz - 2026-07-01T14:03:16Z

Auto-closed by the "fixes #11" commit (7206f6d) — leaving the full evidence here for the record:

- `convexBrainReadonly.ts` DEFAULT_CONFIG now reads `AUKORA_CONVEX_URL`, defaulting to the real live backend on :3210 instead of dead :3220 — matches `kernelAdapter.ts`'s existing env-var pattern.
- `convexTopology.ts`'s stale `TOPOLOGY.labBackend` doc entry fixed (it does serve `getReceiptChainHeadPublic`, contrary to the old note) — role deliberately left `wrong_backend` since the static entry doesn't drive runtime behavior (`classifyBackend()`'s live cryptographic-pin check does, and correctly still refuses to call it canonical without `AUKORA_CANONICAL_PIN_PUBLIC_KEY` configured).
- **Live-proven, not just mocked**: ran a real smoke check against the actual running backend — `source: "convex_loopback"`, real structured response (`chainKey: "organism", count: 0, exists: false` — genuinely connected, that chain just has no receipts yet).
- Full suite: typecheck clean, 122/122 files, 1671/1671 tests green.

**Deliberately not done here** (separate, security-relevant decision): configuring `AUKORA_CANONICAL_PIN_PUBLIC_KEY` so this backend can be cryptographically verified as canonical. That needs the real kernel's public key from the owner/a verified source, not something to fabricate. Until it's set, the topology classifier correctly reports this backend as not-yet-canonical (name-marker-only, spoofable) — which is the safe, honest default.
