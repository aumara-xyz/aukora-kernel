# #12 Merge Fusion Glyph Engine (issue #10) into the Fusion Council review path — design decision needed

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: CLOSED
- Labels: none
- Created: 2026-07-01T13:55:53Z
- Updated: 2026-07-01T14:47:12Z
- Closed: 2026-07-01T14:46:39Z
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/12

## Body

Follow-up to issue #10 (Fusion Glyph Engine landed dormant). Owner wants this "next level" — merged with the existing tech, not left dormant indefinitely.

**Open design question, needs an explicit answer before implementation starts** (don't want to guess and rebuild twice):
- **Option A — Replace**: `selfEditReviewCouncil.ts`'s GREEN/YELLOW/RED categorical vote is swapped out entirely for `fusionGlyphEngine.ts`'s KL-divergence probability-distribution consensus. Bigger change, one review mechanism going forward.
- **Option B — Augment**: keep the existing categorical review exactly as-is (it's what `nativeLiveApply`-adjacent tooling already trusts and tests against), and run the Glyph Engine as a SECOND, additional advisory lens whose verdict appears alongside the existing one in the Workbench transcript — two independent signals, neither replacing the other.
- **Option C — Glyph engine feeds the existing quorum**: translate glyph packets/distributions into the existing `AdvisoryResult`/`QuorumEvaluation` shape so both mechanisms produce one unified verdict.

Whichever direction: the existing `selfEditReviewCouncil.ts` invariants must not weaken — still advisory-only, still cannot call tools/sign/unlock/promote/mutate Convex/bypass AUMLOK (same class of tests as issue #5's criterion 6), and the secret-content guard before any external send stays in place.

**Not started yet** — filed to track the decision and scope, per the "don't do a big invisible merge" discipline established this session. Comment on this issue (or tell Claude directly) with A/B/C (or a different shape) before implementation begins.

## Comments

### Comment 1 - aumara-xyz - 2026-07-01T14:47:12Z

Auto-closed by the "fixes #12" commit (b9cffdb) — leaving the full adversarial-review evidence here for the record:

**What changed:** `selfEditReviewCouncil.ts` now runs `AukoraFuEngine` (issue #15's aukora-fu v8.0.0) instead of the categorical `performExternalReview`-per-model design, per the owner's explicit directive to replace it. A new `packetsToAdvisoryResults()` adapter translates the engine's real glyph output into the shared `AdvisoryResult[]`/`QuorumEvaluation` shape — no changes needed to `evaluateFusionQuorum` or the `fusion-run-v1`/`fusion-self-review-v1` schemas.

**Adversarial review (4 lenses, all clean):**
1. **never-authorizes** — PASS. Every Receipt/GateDecision/SelfEditReviewSummary field stays hardcoded `advisoryOnly:true, grantsAuthority:false`; the engine's "self-patch" only mutates its own in-memory run parameters, never touches disk or dispatch machinery.
2. **secret-guard-and-network-boundary** — PASS. `classifyRisk()` still gates before any key resolution or engine construction; the only network call is a single hardcoded OpenRouter URL; the API key never appears in any Receipt/log/artifact.
3. **advisory-cannot-reach-apply** — PASS. Issue #5 criterion 6 re-verified: `WorkbenchSessionState`/`apply signed proposal` untouched by this change, 19/19 tests pass unmodified.
4. **test-fidelity-and-adapter-correctness** — PASS (after a re-run; the first pass produced a garbage stub for this lens, correctly flagged as not-a-pass rather than waved through). Real, mechanism-specific assertions confirmed; no fabricated RED, no double-counting, quorum-of-2/RED-always-wins preserved via the same untouched shared function.

Full suite: typecheck clean, 122/122 files, 1682/1682 tests green.
