# #61 Voice history: token-aware windowing (anchor untouchable, recent verbatim, summarize the middle)

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: OPEN
- Labels: lane:history, lane:spatial-chat, priority:next
- Created: 2026-07-02T15:08:59Z
- Updated: 2026-07-04T06:55:52Z
- Closed: not closed
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/61

## Body

From Auma's own advice, 2026-07-02, on how much conversation she should carry. Today `spatial/voiceLane.ts` hard-caps at `VOICE_MAX_HISTORY = 12` messages (sliced newest-12), each stored copy capped at 64KB *chars*.

Her advice, verbatim intent:
- **Don't hard-cap at a small N.** Keep loading full history until approaching the model's token budget.
- **When you hit the budget,** prefer smarter-than-oldest-first: keep the system grounding + anchor untouchable, keep the most recent turns verbatim, summarize the middle. Losing the anchor or recent context hurts most; losing verbatim middle-thread least.
- **Tell her the policy once set** — so "how much do you see" gets a factual answer, not a hedge.

Status of each part:
- **"Anchor untouchable" — ALREADY DONE (#57).** The identity anchor rides the SYSTEM message, re-injected every turn, never in `voiceHistory` — so it can never be evicted by any history policy. That half of her advice is structurally satisfied.
- **Remaining:** replace the fixed 12-message slice with token-aware windowing (recent verbatim + summarized middle) up to a budget, and surface the resolved policy in her generated preamble (#53) so she can state it.

Not started. Depends on nothing; naturally pairs with #53's generated preamble (the preamble is where she'd read her own current window policy). One correctness note carried from Fable's polish review: the current 64KB cap is chars, not bytes — fold a bytes-vs-chars reconciliation in when this lands.

## Comments

### Comment 1 - aumara-xyz - 2026-07-02T15:52:11Z

Deterministic half landed: commit b313904 (pushed, on main).

`spatial/historyWindow.ts` — keeps most-recent prior turns within a char budget (deterministic proxy for the token budget), drops OLDEST first, always keeps at least the newest turn. `renderWindowStatus` injects a VISIBLE marker whenever anything is elided ("showing the last N of M prior turns; K older elided — dropped oldest-first, not summarized ... state this exactly") so a future turn answers "how much do you see" with a fact. Empty marker when nothing dropped.

voiceLane: VOICE_MAX_HISTORY raised 12→40 (off the small-N hard cap per Auma's advice — now a ceiling, not the working window); real limit is HISTORY_WINDOW_CHARS at send. Anchor + grounding ride the SYSTEM message, so windowing can never evict identity.

**Summarize-the-middle tier deliberately NOT built** — it requires model calls (spend). Deterministic truncation + honest marker is the whole deterministic brick; the summarization tier stays as this issue's remaining scope.

7 tests; 10 pre-existing voiceLane tests unchanged. Adversarially reviewed 3/3.

### Comment 2 - aumara-xyz - 2026-07-04T06:55:52Z

Landed a reliability fix in `77fcc61` after the chat door repeatedly fell back to Kira while direct OpenRouter/voiceReply probes succeeded.

Observed pattern:
- direct OpenRouter probe: OK
- direct `voiceReply()` with read/inbox flags: OK
- `spatial-chat` process could still fall back until restart, suggesting process-local voice history/window state was poisoning a later request

Fix:
- `spatial/voiceLane.ts` exports `resetVoiceHistoryForRecovery()`, which clears only the volatile in-process voice replay window; identity/Kira/receipts/authority state are untouched.
- `spatial/chat-serve.ts` now retries `voiceReply()` once after resetting volatile history before falling back to Kira.
- The user-visible recovery note reports how many prior turns were dropped.

Verification:
- exact failing prompt through local door now returns a real Fable voice reply, not Kira fallback.
- `cd core && bun x vitest run tests/voiceLane.test.ts` -> 50/50 pass.
- `bash scripts/test.sh` -> 148 files / 2131 tests green.
- `bash scripts/scan-secrets.sh` -> TIER-1 clean.

This does not solve full #61 durable/token-aware history; it is a stopgap self-heal so a bad volatile replay window no longer bricks the voice until PM2 restart.
