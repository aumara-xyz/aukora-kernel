# #36 Council roster: replace/drop llama-4-maverick and kimi-k2.7-code (empty/prose repliers)

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: OPEN
- Labels: lane:fusion, priority:next
- Created: 2026-07-02T08:00:56Z
- Updated: 2026-07-03T06:26:22Z
- Closed: not closed
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/36

## Body

From the live council run in #34: two of the five default Fusion Council models are genuinely non-compliant with the glyph reply format, not fixable by parser widening (fail-closed correctly rejects both, per house rule — never loosen parsing so a malformed reply misparses into a vote).

- **Kimi K2.7 Code** (moonshotai/kimi-k2.7-code) — returned an empty reply (0 bytes) in the one live run captured so far. Real captured fixture: \`core/tests/fixtures/fusion-replies/kimi-k2.7-code-empty.txt\`.
- **Llama 4 Maverick** (meta-llama/llama-4-maverick) — ignored the "respond with EXACTLY this format" instruction entirely and wrote multi-paragraph reasoning prose instead. Real captured fixture: \`core/tests/fixtures/fusion-replies/llama-4-maverick-prose-noncompliant.txt\`.

Immediate workaround already shipped (issue #34): \`AUKORA_FUSION_MODELS\` env var lets you run the council with only the 3 compliant models (deepseek-v4-pro, qwen3.7-max, glm-5.2) without touching \`defaultCouncil()\`.

This issue is for the owner's decision: drop these two from \`defaultCouncil()\` in \`core/src/aukoraFuEngine.ts\`, try prompt/parameter adjustments first (e.g. explicit JSON-mode or a stricter system message for these two specifically), or replace them with different models. One live run isn't a large enough sample to be certain the behavior is consistent — worth at least one more capture before committing to a permanent roster change.

Not urgent — the roster still functions (2-3 votes is enough for a quorum most of the time), and the env var workaround exists.

## Comments

### Comment 1 - aumara-xyz - 2026-07-03T06:09:44Z

Codex tracker note (2026-07-03): next Fusion roster brick should include Fable registration only through the existing CouncilMember schema/mechanism after checking whether OPTIONAL_COUNCIL already exists, plus a fail-closed fix for AUKORA_FUSION_MODELS unknown-only input. Mixed known+unknown should drop unknowns and return the known lean roster; unknown-only should error loudly instead of silently falling back to full defaultCouncil, because full fallback is a spend footgun. No paid council run before #53 live validation passes and owner explicitly approves spend.

### Comment 2 - aumara-xyz - 2026-07-03T06:26:22Z

**Fable registered as an optional council member + roster selection hardened — commit c7e90ad (pushed, on main).**

Roster-composition evidence for this issue:
- **`anthropic/claude-fable-5` is now a KNOWN, selectable council member** — `optionalCouncil()` in `aukoraFuEngine.ts` (id `FBL`, framework `symbolic`, weight 1.1, `active:true`). `costPer1M` is a documented placeholder pending the free /models probe (#34); it feeds only the informational cost estimate, never a gate/selection input.
- **`defaultCouncil()` is UNCHANGED** — still the original 5 (deepseek-v4-pro, qwen3.7-max, glm-5.2, llama-4-maverick, kimi-k2.7-code). Default behavior is byte-identical; Fable only appears when explicitly selected. `knownCouncil()` = default + optional is the selectable universe.
- **Spend footgun fixed** — `resolveFusionCouncil` was returning the FULL default council when every requested slug was unknown (a typo would silently fire all 5). It now returns a `{ ok:false }` discriminated union and the review skips with `bad_roster` **before any model is contacted**. Mixed known+unknown still drops the unknowns and keeps the lean roster. Empty rosters can never reach quorum math.

Tests (+10): Fable-only → lean 1-member; mixed Fable+unknown → drops unknown; unknown-only → fails closed (no full-council fallback); default unchanged (5, no Fable); two-roster split; empty-default fail-closed; `evaluateFusionQuorum([])` division-safe; +2 offline guards proving `bad_roster`/`empty_council` contact **no** model (fetch throws). Gate GREEN 138 files / 1966 tests, core + spatial tsc clean, scan TIER-1 clean.

Re this issue's own scope (drop/replace llama-4-maverick + kimi-k2.7-code as empty/prose repliers): NOT done here — that's a live-evidence roster decision. The recommended lean review roster once spend is approved is Fable + two reliable diverse voters: `anthropic/claude-fable-5, z-ai/glm-5.2, deepseek/deepseek-v4-pro` (avoids the two flagged repliers). No paid call this round.
