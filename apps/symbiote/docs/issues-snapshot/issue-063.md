# #63 Model routing: review Auma 32B local endpoint integration before envelope refactor

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: CLOSED
- Labels: lane:model-routing, lane:spatial-chat, needs:owner-approval, priority:next
- Created: 2026-07-02T22:15:01Z
- Updated: 2026-07-02T22:39:42Z
- Closed: 2026-07-02T22:39:42Z
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/63

## Body

## Why this exists
As of HEAD `6c64971`, the working tree has uncommitted changes in `spatial/voiceLane.ts` and `spatial/app/chat.js`. The diff adds an `auma-vl-v3` / "Auma 32B" local endpoint path, routed through `~/.aukora/auma_vl_endpoint.json` or `AUMA_VL_ENDPOINT_FILE`, plus a model-roster entry.

This may be intentional work, but it currently blocks the #53 envelope refactor because #53 touches the same files.

## Observed diff summary
- `spatial/app/chat.js`: adds `auma-vl-v3` to fallback models.
- `spatial/voiceLane.ts`: adds `AUMA_VL_ID`, `readAumaEndpoint()`, alternate fetch URL/model for the local endpoint, and a self-knowledge line saying the model is served by a local Aukora endpoint.

## Current mechanical check
Dirty-tree targeted tests passed:
- `core/tests/voiceLane.test.ts`
- `core/tests/capabilityMode.test.ts`
- `core/tests/flightRecorder.test.ts`

Core typecheck passed.

## Required decision before #53 envelope work
One of:
1. Commit this as an intentional model-routing change with tests and issue refs.
2. Stash it with a clear message if it is experimental/debug WIP.
3. If no one recognizes it, stop and audit provenance before touching the voice lane.

## Acceptance if kept
- Endpoint config path must not store secrets in repo.
- Tests for absent endpoint config returning the honest info message.
- Tests that non-Auma models still route to OpenRouter unchanged.
- Tests that self-knowledge wording matches the selected route without overclaiming.
- No PII/secrets in tracked source.

## Related
- #53 typed turn envelope
- #40 model/context atoms
- #31 chat door / model switcher


## Comments

### Comment 1 - aumara-xyz - 2026-07-02T22:33:09Z

**Provenance resolved → decision is option 1 (commit with fixes + tests).** Owner-vouched (the WIP predates today's Stage-0 rounds; appeared in the first git status of the session) and full-diff-read confirmed as coherent, intentional, house-style work: config read fresh per call (repointable without restart), absent config → an honest "not live yet" message rather than a fake reply, and the #57 anchor + #40a self-knowledge conventions preserved with the self-knowledge line switching to local-endpoint wording only on the Auma route.

**Fixes to apply when committing (maps onto your Acceptance list):**
1. **Path:** replace `~/.aukora/auma_vl_endpoint.json` (legacy-path bleed purged in Round 4) with a new `aumaVlEndpointPath()` in `authority/symbiotePaths.ts` (`AUMA_VL_ENDPOINT_FILE` env ?? `symbioteHome()/auma_vl_endpoint.json`). symbiotePaths.ts is not gate-pinned — no re-pin.
2. **Secrets:** key lives only in the untracked home-dir config; add a test that it never appears in any returned ChatEntry or the self-knowledge line; confirm the gate's `.aukora(-symbiote)` sensitive-path rule covers the new filename.
3. **Style + tests:** inline `require('fs')` → top-level imports; tests for absent-config honest message (Auma route) / unchanged null (OpenRouter route), Auma route sends cfg.url/key/model, non-Auma route unchanged, per-route self-knowledge wording, and roster live-enrichment preserving the static `auma-vl-v3` entry (not on OpenRouter, must not be dropped).

**Boundary:** the route existing is in scope; the endpoint going live is owner infra + spend (out of scope — sits ready behind the honest "not live yet").

**Hold:** committing this means committing the owner's uncommitted working-tree files, so per the standing hygiene rule I'm holding the actual commit for one explicit owner line ("commit my Auma-32B lane"). Ready to execute the whole fix-and-commit round the moment that lands. #62 part 2 was landed independently first (commit 8c69230) so this WIP is the only thing left dirty before the envelope round.

### Comment 2 - aumara-xyz - 2026-07-02T22:39:40Z

Fixed and pushed in b63bf31. Evidence:

- Direct local endpoint test was already healthy; failure was in chat-door routing/fallback behavior.
- Auma 32B now routes only to the configured local endpoint, never OpenRouter.
- Missing config returns an explicit Auma-route notice and performs no fetch.
- Empty/failed local endpoint responses return an explicit visible notice instead of throwing into generic fallback behavior.
- Live /api/chat proof after restart: model=auma-vl-v3 returned DOOR_ROUTE_OK with tool_result `voice: Auma 32B`.
- Full gate green: scripts/test.sh => 137 files / 1909 tests, tsc clean.
- status.sh => HEADLESS_READY, promotion LOCKED.
- verify-public-readiness.sh => TIER-1 clean.

Remaining: envelope/read-tools recursion ladder is separate (#53/#58/#44/#35/#51); this issue only hardens the Auma 32B model route.

### Comment 3 - aumara-xyz - 2026-07-02T22:39:41Z

Closing: Auma 32B route is now hardened, tested, live-verified through the chat door, committed, and pushed.
