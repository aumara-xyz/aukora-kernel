# #3 Round 3: Replace simulated sandbox test check with bounded real test runner

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: CLOSED
- Labels: none
- Created: 2026-07-01T09:20:34Z
- Updated: 2026-07-01T12:11:18Z
- Closed: 2026-07-01T12:11:18Z
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/3

## Body

Source: verified current nativeIdeDispatcher run_tests is simulated sandbox-content verification only, not scripts/test.sh.

Goal:
Add a real, bounded sandbox test runner so a proposed patch can be tested in a temp workspace before any signed apply.

Acceptance criteria:
- Fixed command allowlist only, e.g. typecheck, targeted test, full scripts/test.sh when explicitly selected.
- No arbitrary command from user text.
- Runs only inside a temp/sandbox copy, never against live repo unless explicitly designed and tested.
- Captures bounded stdout/stderr, exit code, duration, and output hash/summary.
- Receipt records exact test command and result.
- Failing tests block apply readiness.
- Tests prove command injection and non-allowlisted commands refuse.

Completion protocol:
- Commit must reference this issue.
- Push branch after completion.
- Do not close until targeted and relevant full local tests pass.

## Comments

### Comment 1 - aumara-xyz - 2026-07-01T12:11:17Z

Verified complete on disk and in local test run.

Files: core/src/sandboxTestRunner.ts (new) - deliberately kept OUT of nativeIdeDispatcher.ts (which stays subprocess-free; that invariant is proven by a source-scan test).

Acceptance criteria checked:
- Fixed allow-list only: typecheck | targeted_test | full_test_suite - isAllowedTestCommand() closes the enum, tested against arbitrary/injection strings.
- No arbitrary command from user text - execFileSync argv arrays only, shell never true (source-scanned in tests).
- Runs only inside a fresh temp copy of the whole repo root (proposal overlaid on top) - never the live repo. Verified: the live repo is confirmed untouched after every real run in tests.
- Bounded stdout/stderr (20k char cap + truncated flag), exit code, duration, sha256 output hash all captured.
- Receipt integration: workbenchCommandLoop.ts's runRealTestCommand() feeds the exact command + exit code + duration into session.lastTestResult, which write_receipt then records.
- Tests prove command-injection attempts and non-allowlisted commands refuse (core/tests/sandboxTestRunner.test.ts).

A real, reproducible bug was found and fixed during build: running against a live-mutating repo (a concurrent process was active) caused a nondeterministic partial copy; fixed by making the copy defensive (skip-on-transient-error rather than crash) plus 3 correctness fixes (repo name must literally end in "aukora-symbiote" for some existing tests' assertions, .git must be included since a test shells to real git status, node_modules needed a symlink at both the repo root AND core/ level).

Tests: core/tests/sandboxTestRunner.test.ts - 14 tests passing, including real isolated typecheck/targeted-test runs.

Commit: 6f44b0d (predates this issue - see note on #1 about commit-reference protocol going forward).
