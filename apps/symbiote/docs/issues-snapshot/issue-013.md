# #13 Port Kira's real advances into Symbiote: OpenCode+gate wiring pattern + canonical-pin/freshness crypto

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: CLOSED
- Labels: none
- Created: 2026-07-01T13:56:10Z
- Updated: 2026-07-01T15:28:32Z
- Closed: 2026-07-01T15:28:32Z
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/13

## Body

Follow-up from the symbiote-vs-kira comparative audit (2026-07-01). Two genuinely real, verified advances exist in aukora-kira that Symbiote doesn't have yet:

**1. Real agent-loop-through-gate wiring pattern** (`aukora-kira/authority/gate/opencodeAskBridge.ts` + its insertion into `opencode/packages/opencode/src/session/tools.ts` at two call sites). This is the reference implementation for "a real coding agent's tool calls, gated through Aukora's own risk classifier, before any side effect runs" — anti-bypass verified (the MCP wildcard path can't skip the risk decision). Source-complete in kira but uncommitted there and never run end-to-end (no test logs found). This is the DIRECT template for gating Hermes (see the new Hermes-gating issue, filed separately) and for Symbiote's own "real agent, not just a deterministic parser" gap.

**2. Newer canonical-pin/freshness crypto** (`aukora-kira/core/src/convexCanonicalPin.ts`, `convexCanonicalFreshness.ts`, `canonicalPinTypes.ts` — ML-DSA-65 signature verification + anti-replay/rollback/fork checks, 167/167 tests passing including a gold-vector compatibility test against the kernel's own signer). Symbiote's `convexTopology.ts` classifier is real but has weaker canonicality verification than this.

**Scope:**
- Port `convexCanonicalPin.ts`/`convexCanonicalFreshness.ts`/`canonicalPinTypes.ts` into `core/src/`, wired into `convexTopology.ts`'s classification (relates to issue #11's port fix).
- Adapt the `opencodeAskBridge.ts` gating PATTERN (not necessarily the file verbatim, since Symbiote's `authority/gate` may differ from kira's) as the reference design for the Hermes-gating issue rather than porting it standalone with nothing to gate yet.

**Verification required:** same discipline as every other round — real tests ported/adapted, typecheck clean, full suite green, commit references this issue.

## Comments

### Comment 1 - aumara-xyz - 2026-07-01T14:03:28Z

**Correction** (verified while working issue #11): the canonical-pin/freshness crypto claim in this issue's original body is wrong. I diffed the files directly instead of trusting the earlier comparison agent's summary:

- `convexCanonicalFreshness.ts` — byte-identical between aukora-symbiote and aukora-kira.
- `convexCanonicalPin.ts` — differs by exactly ONE line (a type import path: `./convexBrainReadonly` in symbiote vs. a smaller standalone `./canonicalPinTypes` in kira, since kira's core/src/ doesn't have the full convexBrainReadonly.ts module).

Both files have been in Symbiote since its very first commit (2026-06-30, predating Kira's origin). Kira's copies are trivial ports of Symbiote's own code, not something newer or additional. **There is nothing to port on this front — scoping that part of this issue out.**

The only real remaining scope for this issue is the OpenCode+gate wiring pattern (`aukora-kira/authority/gate/opencodeAskBridge.ts`), which IS genuinely real and not present in Symbiote — that part of the issue stands, and its design is now the direct reference for issue #14 (Hermes gating).

### Comment 2 - aumara-xyz - 2026-07-01T15:28:31Z

Closing — the core goal is now substantially achieved via a different, better-suited design, and the other scoped item was already resolved as moot.

**"Real agent-loop-through-gate wiring pattern"**: rather than porting Kira's OpenCode+`opencodeAskBridge` pattern (gate an EXTERNAL agent's open-ended tool calls), issue #19 (commit ac8e442) built a NATIVE tool-calling agent (`core/src/nativeToolCallingEngine.ts`) that achieves the same real goal — a real model autonomously deciding what to call next, safely bounded — through a closed 10-tool contract instead. Since the model can only ever see 6 fixed, already-advisory-only tools, there's no open-ended surface that needs a runtime per-call gate check the way an external agent like OpenCode does. This matches the owner's explicit direction earlier this session: grow Symbiote's own native tool loop rather than attach real OpenCode as a dependency. Kira's pattern remains a useful reference (cited in issue #19), but isn't something to literally port.

**"convexCanonicalPin.ts/convexCanonicalFreshness.ts port"**: already resolved as moot in this issue's own comment thread (2026-07-01) — I diffed the files directly and found Symbiote already has both, byte-identical/near-identical, since its very first commit. Nothing to port.

Nothing left to do under this issue.
