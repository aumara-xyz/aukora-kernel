# #23 Round 3: Cut dead mass, stop the lies

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: CLOSED
- Labels: none
- Created: 2026-07-02T01:48:13Z
- Updated: 2026-07-02T02:27:30Z
- Closed: 2026-07-02T02:16:48Z
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/23

## Body

Fable's convergence review, Round 3 of 3. Only started now that Round 1 (issue #21) and Round 2 (issue #22) are implemented, tested, and pushed.

Scope (per Fable's directive + Codex's addendum):

1. **Archive + delete `self_edit/`**: `git branch archive/self_edit-opencode-fork` then delete `self_edit/` from main. Verified before touching anything: 97 files / 1.0MB, 46 of 64 TS files import `@opencode-ai/*` (installed nowhere in this repo), zero real runtime importers from `core/src`/`core/tests`/`scripts`/`receiver`, and it's outside `core/tsconfig.json`'s `include` scope entirely (`src/**/*`, `tests/**/*` only) — never typechecked as part of this repo. `core/src/rootOrganismRegistry.ts:59` falsely labels it `role: 'active_engine'`, `safeToExpose: true`. 4 coupled test files reference it directly: `registryIntegrity.test.ts`, `nativeIdeDispatcher.test.ts`, `convexReadOnlyInvariant.test.ts`, `seedRootContract.test.ts` (Fable said 3 — verified 4, correcting). Update `scripts/status.sh` and `scripts/self-map.sh` (both reference `self_edit/opencode/...` paths). Fix the registry row. Close #17 as superseded (owner sign-off in the issue, not unilaterally).

2. **Delete root `tests/`**: empty `.gitkeep`, referenced by nothing. Verified.

3. **Fix workbench/README "read-only/sandbox-only" framing lies**: the workbench UI's own framing claims it's read-only/sandbox-only while it exposes signed live apply (`apply signed proposal`, `dispatchSignedLiveApply`).

4. **Strip website fiction**: a fake `aukoraGate.ts` code sample (imports symbols that don't exist) and an unbuilt "$11/mo tier" — public overclaim.

5. **Wire `captureWithReceipt` into the live path**: proposal/review/apply events should land in Kira's chain — memory currently has no live caller for this.

**Accept**: full gate green; grep proves no `self_edit` imports remain anywhere reachable; registry test updated; a workbench round produces new Kira receipts.

Note: item 1 (self_edit deletion) is the highest-risk item in this round (git branch + delete) — executing carefully, verifying the archive branch before deleting from main, running the full suite after each step.

## Comments

### Comment 1 - aumara-xyz - 2026-07-02T02:03:33Z

Correction: commit \`20ea060\` (pushed a moment ago) had a message describing this round's framing/website/captureWithReceipt work, but its actual diff only contained the \`tests/.gitkeep\` deletion — a \`git add\` with an invalid pathspec in the same invocation silently failed to stage anything else, and I didn't catch it before committing. Fixed transparently in \`2af840e\`, which contains the actual 7 files. Flagging this myself rather than letting it stand uncorrected.

Progress so far this round: root \`tests/\` deleted, workbench/README/website framing fixes done and verified in-browser, \`captureWithReceipt\` wired into the workbench pipeline (proposal/review/apply → real Kira receipts, proven by a disk-verified test). Full suite green: 127 files / 1779 tests.

Remaining: \`self_edit/\` archive+delete — doing this last as its own carefully-verified step since it's the highest-risk item in this round.

### Comment 2 - aumara-xyz - 2026-07-02T02:18:10Z

Round 3 done, pushed as \`3fb9d61\`, fresh-clone verified: 126 files / 1776 tests, typecheck clean, \`self_edit/\` genuinely absent from a fresh clone (confirmed \`ls self_edit\` fails on the clone).

\`self_edit/\` (97 files, 1.0MB) archived to branch \`archive/self_edit-opencode-fork\` (pushed to origin, not just local) before deletion from main. Every coupled reference fixed: \`registryIntegrity.test.ts\` deleted (nothing left to check), \`convexReadOnlyInvariant.test.ts\` and \`seedRootContract.test.ts\` updated to assert the stronger now-true invariants, the registry's false \`active_engine\` row removed, \`scripts/status.sh\`/\`scripts/self-map.sh\` cleaned up, and the Anatomy graph's "SELF-EDIT" node removed and verified gone in-browser.

All 3 rounds of Fable's convergence plan are now done: #21 (Fusion truncation + Kira migration), #22 (the native-agent demo, real and live), #23 (this one). Full picture across all three: 126 files / 1776 tests, typecheck clean, fresh-clone verified at every step.

**Not closed here**: issue #17 (quarantined host-harness tests) — per Fable's own note, self_edit's deletion supersedes its original scope, but that's the owner's call to make, not mine to close unilaterally. Leaving it open with this context for whenever the owner reviews it.

### Comment 3 - aumara-xyz - 2026-07-02T02:27:30Z

Adversarial audit found 4 stale docs still describing self_edit as present. Fixed in `5c1ead2`, fresh-clone verified: 126 files / 1776 tests, typecheck clean. All 3 rounds now genuinely closed out.
