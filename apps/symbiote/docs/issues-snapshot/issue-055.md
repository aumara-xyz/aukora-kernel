# #55 Stage 0: owner lockdown command returns Auma to advisory-only

Generated by `scripts/export-issues-snapshot.sh`; do not edit by hand.

- State: OPEN
- Labels: lane:safety-rail, lane:spatial-chat, priority:next, stage:0
- Created: 2026-07-02T13:10:27Z
- Updated: 2026-07-02T15:28:23Z
- Closed: not closed
- URL: https://github.com/aumara-xyz/aukora-symbiote/issues/55

## Body

## Why this exists
Every future unlock stage should have an immediate owner-controlled downgrade path. Auma requested `voice: lockdown` before read-only tools land.

## Goal
Implement an owner command that drops Auma back to advisory-only mode immediately, without restarting the local server.

Candidate command:
- `voice: lockdown`
- optionally a UI button later, but command first is enough.

## Expected behavior
- Disables promoted Auma capabilities for the current session/mode.
- Keeps ordinary advisory chat available.
- Writes an event to the flight recorder once #54 exists.
- Displays a clear confirmation in chat/UI.

## Boundaries
- This does not touch AUMLOK key material.
- This is a local capability-mode switch, not a cryptographic revoke/rotate ceremony.
- Demotion should be instant; promotion remains deliberate and tested.

## Candidate tests
- Command disables capabilities immediately.
- Disabled capability attempts return advisory-only error.
- Lockdown does not require model interpretation; it is parsed by the door.
- Works in a throwaway test environment without touching owner keys.

## Related
- #53 typed turn envelope and capability preamble
- #54 capability flight recorder


## Comments

### Comment 1 - aumara-xyz - 2026-07-02T15:09:31Z

**Scope edits before build (Fable QA) — building against these:**

1. **Parse placement is load-bearing:** today `voice: lockdown` falls through the grammar to `voiceReply` — **a billed model call interpreting your kill switch**. Parse it in the door's POST handler AFTER body-parse and BEFORE `ensureLoop()` — before grammar, before any `core/` import, before any network. (Confirmed: chat-serve.ts parses the body at ~L116-134, calls ensureLoop at ~L136; the intercept goes between them.)

2. **Mode state must be a FILE, not memory.** Door restarts are routine in this repo; in-memory lockdown would silently re-arm (i.e. clear) on restart. "Demotion instant, promotion deliberate" is only true if the lockdown persists. New path helper in `authority/symbiotePaths.ts`.

3. **Honest confirmation message:** today nothing is promoted, so lockdown *revokes nothing* — it persists the mode that future capabilities (starting #44) will check. Enforcement lands at #44's dispatch point. The confirmation must say this plainly, not imply it just disabled a live capability.

### Comment 2 - aumara-xyz - 2026-07-02T15:28:23Z

Code landed: commit c88767b (pushed, on main). Live confirmation transcript pending owner spend.

- `voice: lockdown` parsed in the door's POST handler AFTER body-parse, BEFORE ensureLoop — deterministic, never reaches a billed model call (source-asserted in tests).
- Mode FILE-persisted (`~/.aukora-symbiote/capability-mode.json`, gitignored) — survives door restarts; absent/corrupt = advisory (safe default; corrupt can't fake a lockdown).
- Honest Stage-0 confirmation: revokes nothing yet (nothing promoted); persists the mode #44+ will check. Clearing is owner-terminal only, not a chat command.
- First flight-recorder event wired (#54).

9 tests. Full suite 136/1891 green, tsc clean, scan-secrets TIER-1 clean, fresh-clone verified. Adversarially reviewed (spec+regression+security) — 3/3 PASS.
