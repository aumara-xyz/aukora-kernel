# Generated Fusion Council run artifacts — local output, never committed.
runs/*.json
!runs/.gitkeep

# Local secrets/config.
.env
*.log
node_modules/
coverage/
dist/

# Optional local review target folder.
target/
